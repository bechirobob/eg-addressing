# Expand–Migrate–Contract Convergence and Scale Plan

Built only after the Review 06 transformation groups are loaded. NLI-WO-002B remains unauthorized; this is a reviewed migration-unit design source for later approval, not executable migration authority.

| Unit | Transform group | Complete source fields | Target rows/fields | Source keys | Crosswalk/exception schema | Read/write behavior | Idempotency | Conflict precedence | Validation/tolerance | Recovery point | Monitoring window | Cutover/abort gate |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| MU01 | WO002-R06-correction-case-address_corrections | address_corrections.correction_type; address_corrections.public_code; address_corrections.status | correction_case.case_state; correction_case.correction_type; correction_case.target_public_code | address_corrections.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(address_corrections.correction_type) == count(correction_case.correction_type) + count(source_payload_archive where field_path='address_corrections.correction_type') + count(owned_migration_exception where source_field='address_corrections.correction_type') \| ASSERT count(address_corrections.public_code) == count(correction_case.target_public_code) + count(source_payload_archive where field_path='address_corrections.public_code') + count(owned_migration_exception where source_field='address_corrections.public_code') \| ASSERT count(address_corrections.status) == count(correction_case.case_state) + count(source_payload_archive where field_path='address_corrections.status') + count(owned_migration_exception where source_field='address_corrections.status') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU02 | WO002-R06-correction-case-archive-address_corrections | address_corrections.note; address_corrections.query; address_corrections.reason; address_corrections.reporter_name; address_corrections.reviewer_note | source_payload_archive.payload_uri | address_corrections.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(address_corrections.note) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='address_corrections.note') + count(owned_migration_exception where source_field='address_corrections.note') \| ASSERT count(address_corrections.query) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='address_corrections.query') + count(owned_migration_exception where source_field='address_corrections.query') \| ASSERT count(address_corrections.reason) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='address_corrections.reason') + count(owned_migration_exception where source_field='address_corrections.reason') \| ASSERT count(address_corrections.reporter_name) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='address_corrections.reporter_name') + count(owned_migration_exception where source_field='address_corrections.reporter_name') \| ASSERT count(address_corrections.reviewer_note) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='address_corrections.reviewer_note') + count(owned_migration_exception where source_field='address_corrections.reviewer_note') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU03 | WO002-R06-geometry-observation-address_points | address_points.accuracy_meters; address_points.latitude; address_points.longitude | geometry_observation.observed_geom | address_points.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(address_points.accuracy_meters) == count(geometry_observation.observed_geom) + count(source_payload_archive where field_path='address_points.accuracy_meters') + count(owned_migration_exception where source_field='address_points.accuracy_meters') \| ASSERT count(address_points.latitude) == count(geometry_observation.observed_geom) + count(source_payload_archive where field_path='address_points.latitude') + count(owned_migration_exception where source_field='address_points.latitude') \| ASSERT count(address_points.longitude) == count(geometry_observation.observed_geom) + count(source_payload_archive where field_path='address_points.longitude') + count(owned_migration_exception where source_field='address_points.longitude') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU04 | WO002-R06-geometry-observation-address_records | address_records.accuracy_meters; address_records.latitude; address_records.longitude | geometry_observation.observed_geom | address_records.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(address_records.accuracy_meters) == count(geometry_observation.observed_geom) + count(source_payload_archive where field_path='address_records.accuracy_meters') + count(owned_migration_exception where source_field='address_records.accuracy_meters') \| ASSERT count(address_records.latitude) == count(geometry_observation.observed_geom) + count(source_payload_archive where field_path='address_records.latitude') + count(owned_migration_exception where source_field='address_records.latitude') \| ASSERT count(address_records.longitude) == count(geometry_observation.observed_geom) + count(source_payload_archive where field_path='address_records.longitude') + count(owned_migration_exception where source_field='address_records.longitude') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU05 | WO002-R06-geometry-observation-citizen_geotag_submissions | citizen_geotag_submissions.accuracy_meters; citizen_geotag_submissions.latitude; citizen_geotag_submissions.longitude | geometry_observation.observed_geom | citizen_geotag_submissions.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(citizen_geotag_submissions.accuracy_meters) == count(geometry_observation.observed_geom) + count(source_payload_archive where field_path='citizen_geotag_submissions.accuracy_meters') + count(owned_migration_exception where source_field='citizen_geotag_submissions.accuracy_meters') \| ASSERT count(citizen_geotag_submissions.latitude) == count(geometry_observation.observed_geom) + count(source_payload_archive where field_path='citizen_geotag_submissions.latitude') + count(owned_migration_exception where source_field='citizen_geotag_submissions.latitude') \| ASSERT count(citizen_geotag_submissions.longitude) == count(geometry_observation.observed_geom) + count(source_payload_archive where field_path='citizen_geotag_submissions.longitude') + count(owned_migration_exception where source_field='citizen_geotag_submissions.longitude') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU06 | WO002-R06-identity-crosswalk-address_corrections | address_corrections.address_id; address_corrections.id | legacy_crosswalk.legacy_id | address_corrections.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(address_corrections.address_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='address_corrections.address_id') + count(owned_migration_exception where source_field='address_corrections.address_id') \| ASSERT count(address_corrections.id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='address_corrections.id') + count(owned_migration_exception where source_field='address_corrections.id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU07 | WO002-R06-identity-crosswalk-address_points | address_points.address_id; address_points.id | legacy_crosswalk.legacy_id | address_points.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(address_points.address_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='address_points.address_id') + count(owned_migration_exception where source_field='address_points.address_id') \| ASSERT count(address_points.id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='address_points.id') + count(owned_migration_exception where source_field='address_points.id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU08 | WO002-R06-identity-crosswalk-address_record_events | address_record_events.actor_id; address_record_events.address_record_id; address_record_events.id | legacy_crosswalk.legacy_id | address_record_events.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(address_record_events.actor_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='address_record_events.actor_id') + count(owned_migration_exception where source_field='address_record_events.actor_id') \| ASSERT count(address_record_events.address_record_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='address_record_events.address_record_id') + count(owned_migration_exception where source_field='address_record_events.address_record_id') \| ASSERT count(address_record_events.id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='address_record_events.id') + count(owned_migration_exception where source_field='address_record_events.id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU09 | WO002-R06-identity-crosswalk-address_records | address_records.id; address_records.source_submission_id; address_records.territory_id | legacy_crosswalk.legacy_id | address_records.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(address_records.id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='address_records.id') + count(owned_migration_exception where source_field='address_records.id') \| ASSERT count(address_records.source_submission_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='address_records.source_submission_id') + count(owned_migration_exception where source_field='address_records.source_submission_id') \| ASSERT count(address_records.territory_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='address_records.territory_id') + count(owned_migration_exception where source_field='address_records.territory_id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU10 | WO002-R06-identity-crosswalk-addresses | addresses.building_id; addresses.id; addresses.road_id; addresses.superseded_by_address_id; addresses.territory_id | legacy_crosswalk.legacy_id | addresses.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(addresses.building_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='addresses.building_id') + count(owned_migration_exception where source_field='addresses.building_id') \| ASSERT count(addresses.id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='addresses.id') + count(owned_migration_exception where source_field='addresses.id') \| ASSERT count(addresses.road_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='addresses.road_id') + count(owned_migration_exception where source_field='addresses.road_id') \| ASSERT count(addresses.superseded_by_address_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='addresses.superseded_by_address_id') + count(owned_migration_exception where source_field='addresses.superseded_by_address_id') \| ASSERT count(addresses.territory_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='addresses.territory_id') + count(owned_migration_exception where source_field='addresses.territory_id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU11 | WO002-R06-identity-crosswalk-admin_units | admin_units.id; admin_units.parent_id | legacy_crosswalk.legacy_id | admin_units.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(admin_units.id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='admin_units.id') + count(owned_migration_exception where source_field='admin_units.id') \| ASSERT count(admin_units.parent_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='admin_units.parent_id') + count(owned_migration_exception where source_field='admin_units.parent_id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU12 | WO002-R06-identity-crosswalk-audit_logs | audit_logs.actor_user_id; audit_logs.entity_id; audit_logs.id | legacy_crosswalk.legacy_id | audit_logs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(audit_logs.actor_user_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='audit_logs.actor_user_id') + count(owned_migration_exception where source_field='audit_logs.actor_user_id') \| ASSERT count(audit_logs.entity_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='audit_logs.entity_id') + count(owned_migration_exception where source_field='audit_logs.entity_id') \| ASSERT count(audit_logs.id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='audit_logs.id') + count(owned_migration_exception where source_field='audit_logs.id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU13 | WO002-R06-identity-crosswalk-auth_tokens | auth_tokens.user_id | legacy_crosswalk.legacy_id | auth_tokens.user_id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(auth_tokens.user_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='auth_tokens.user_id') + count(owned_migration_exception where source_field='auth_tokens.user_id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU14 | WO002-R06-identity-crosswalk-buildings | buildings.id; buildings.road_id; buildings.territory_id | legacy_crosswalk.legacy_id | buildings.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(buildings.id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='buildings.id') + count(owned_migration_exception where source_field='buildings.id') \| ASSERT count(buildings.road_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='buildings.road_id') + count(owned_migration_exception where source_field='buildings.road_id') \| ASSERT count(buildings.territory_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='buildings.territory_id') + count(owned_migration_exception where source_field='buildings.territory_id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU15 | WO002-R06-identity-crosswalk-citizen_geotag_submissions | citizen_geotag_submissions.field_submission_id; citizen_geotag_submissions.id; citizen_geotag_submissions.territory_id | legacy_crosswalk.legacy_id | citizen_geotag_submissions.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(citizen_geotag_submissions.field_submission_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='citizen_geotag_submissions.field_submission_id') + count(owned_migration_exception where source_field='citizen_geotag_submissions.field_submission_id') \| ASSERT count(citizen_geotag_submissions.id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='citizen_geotag_submissions.id') + count(owned_migration_exception where source_field='citizen_geotag_submissions.id') \| ASSERT count(citizen_geotag_submissions.territory_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='citizen_geotag_submissions.territory_id') + count(owned_migration_exception where source_field='citizen_geotag_submissions.territory_id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU16 | WO002-R06-identity-crosswalk-development_fixture_batches | development_fixture_batches.batch_id | legacy_crosswalk.legacy_id | development_fixture_batches.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(development_fixture_batches.batch_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='development_fixture_batches.batch_id') + count(owned_migration_exception where source_field='development_fixture_batches.batch_id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU17 | WO002-R06-identity-crosswalk-development_fixture_records | development_fixture_records.batch_id; development_fixture_records.record_id | legacy_crosswalk.legacy_id | development_fixture_records.record_id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(development_fixture_records.batch_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='development_fixture_records.batch_id') + count(owned_migration_exception where source_field='development_fixture_records.batch_id') \| ASSERT count(development_fixture_records.record_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='development_fixture_records.record_id') + count(owned_migration_exception where source_field='development_fixture_records.record_id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU18 | WO002-R06-identity-crosswalk-field_assignments | field_assignments.assignment_id; field_assignments.territory_id | legacy_crosswalk.legacy_id | field_assignments.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(field_assignments.assignment_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='field_assignments.assignment_id') + count(owned_migration_exception where source_field='field_assignments.assignment_id') \| ASSERT count(field_assignments.territory_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='field_assignments.territory_id') + count(owned_migration_exception where source_field='field_assignments.territory_id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU19 | WO002-R06-identity-crosswalk-field_submissions | field_submissions.assignment_id; field_submissions.id; field_submissions.registry_entity_id; field_submissions.territory_id | legacy_crosswalk.legacy_id | field_submissions.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(field_submissions.assignment_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='field_submissions.assignment_id') + count(owned_migration_exception where source_field='field_submissions.assignment_id') \| ASSERT count(field_submissions.id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='field_submissions.id') + count(owned_migration_exception where source_field='field_submissions.id') \| ASSERT count(field_submissions.registry_entity_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='field_submissions.registry_entity_id') + count(owned_migration_exception where source_field='field_submissions.registry_entity_id') \| ASSERT count(field_submissions.territory_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='field_submissions.territory_id') + count(owned_migration_exception where source_field='field_submissions.territory_id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU20 | WO002-R06-identity-crosswalk-import_jobs | import_jobs.id | legacy_crosswalk.legacy_id | import_jobs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(import_jobs.id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='import_jobs.id') + count(owned_migration_exception where source_field='import_jobs.id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU21 | WO002-R06-identity-crosswalk-import_rows | import_rows.committed_submission_id; import_rows.job_id; import_rows.territory_id | legacy_crosswalk.legacy_id | import_rows.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(import_rows.committed_submission_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='import_rows.committed_submission_id') + count(owned_migration_exception where source_field='import_rows.committed_submission_id') \| ASSERT count(import_rows.job_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='import_rows.job_id') + count(owned_migration_exception where source_field='import_rows.job_id') \| ASSERT count(import_rows.territory_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='import_rows.territory_id') + count(owned_migration_exception where source_field='import_rows.territory_id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU22 | WO002-R06-identity-crosswalk-publication_pack_addresses | publication_pack_addresses.address_id; publication_pack_addresses.publication_pack_id | legacy_crosswalk.legacy_id | publication_pack_addresses.address_id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(publication_pack_addresses.address_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='publication_pack_addresses.address_id') + count(owned_migration_exception where source_field='publication_pack_addresses.address_id') \| ASSERT count(publication_pack_addresses.publication_pack_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='publication_pack_addresses.publication_pack_id') + count(owned_migration_exception where source_field='publication_pack_addresses.publication_pack_id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU23 | WO002-R06-identity-crosswalk-publication_packs | publication_packs.id | legacy_crosswalk.legacy_id | publication_packs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(publication_packs.id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='publication_packs.id') + count(owned_migration_exception where source_field='publication_packs.id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU24 | WO002-R06-identity-crosswalk-reference_data_load_history | reference_data_load_history.id; reference_data_load_history.package_id | legacy_crosswalk.legacy_id | reference_data_load_history.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(reference_data_load_history.id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='reference_data_load_history.id') + count(owned_migration_exception where source_field='reference_data_load_history.id') \| ASSERT count(reference_data_load_history.package_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='reference_data_load_history.package_id') + count(owned_migration_exception where source_field='reference_data_load_history.package_id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU25 | WO002-R06-identity-crosswalk-reference_data_loads | reference_data_loads.package_id | legacy_crosswalk.legacy_id | reference_data_loads.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(reference_data_loads.package_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='reference_data_loads.package_id') + count(owned_migration_exception where source_field='reference_data_loads.package_id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU26 | WO002-R06-identity-crosswalk-roads | roads.id; roads.territory_id | legacy_crosswalk.legacy_id | roads.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(roads.id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='roads.id') + count(owned_migration_exception where source_field='roads.id') \| ASSERT count(roads.territory_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='roads.territory_id') + count(owned_migration_exception where source_field='roads.territory_id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU27 | WO002-R06-identity-crosswalk-territories | territories.admin_unit_id; territories.id | legacy_crosswalk.legacy_id | territories.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(territories.admin_unit_id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='territories.admin_unit_id') + count(owned_migration_exception where source_field='territories.admin_unit_id') \| ASSERT count(territories.id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='territories.id') + count(owned_migration_exception where source_field='territories.id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU28 | WO002-R06-identity-crosswalk-users | users.id | legacy_crosswalk.legacy_id | users.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(users.id) == count(legacy_crosswalk.legacy_id) + count(source_payload_archive where field_path='users.id') + count(owned_migration_exception where source_field='users.id') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU29 | WO002-R06-name-record-address_record_events | address_record_events.actor_username | name_record.name_text | address_record_events.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(address_record_events.actor_username) == count(name_record.name_text) + count(source_payload_archive where field_path='address_record_events.actor_username') + count(owned_migration_exception where source_field='address_record_events.actor_username') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU30 | WO002-R06-name-record-address_records | address_records.address_label | name_record.name_text | address_records.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(address_records.address_label) == count(name_record.name_text) + count(source_payload_archive where field_path='address_records.address_label') + count(owned_migration_exception where source_field='address_records.address_label') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU31 | WO002-R06-name-record-admin_units | admin_units.name_en; admin_units.name_es | name_record.name_text | admin_units.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(admin_units.name_en) == count(name_record.name_text) + count(source_payload_archive where field_path='admin_units.name_en') + count(owned_migration_exception where source_field='admin_units.name_en') \| ASSERT count(admin_units.name_es) == count(name_record.name_text) + count(source_payload_archive where field_path='admin_units.name_es') + count(owned_migration_exception where source_field='admin_units.name_es') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU32 | WO002-R06-name-record-audit_logs | audit_logs.actor_username | name_record.name_text | audit_logs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(audit_logs.actor_username) == count(name_record.name_text) + count(source_payload_archive where field_path='audit_logs.actor_username') + count(owned_migration_exception where source_field='audit_logs.actor_username') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU33 | WO002-R06-name-record-buildings | buildings.label | name_record.name_text | buildings.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(buildings.label) == count(name_record.name_text) + count(source_payload_archive where field_path='buildings.label') + count(owned_migration_exception where source_field='buildings.label') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU34 | WO002-R06-name-record-citizen_geotag_submissions | citizen_geotag_submissions.address_label; citizen_geotag_submissions.citizen_name; citizen_geotag_submissions.map_display_name; citizen_geotag_submissions.reviewed_road_name; citizen_geotag_submissions.suggested_place_name; citizen_geotag_submissions.suggested_road_name | name_record.name_text | citizen_geotag_submissions.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(citizen_geotag_submissions.address_label) == count(name_record.name_text) + count(source_payload_archive where field_path='citizen_geotag_submissions.address_label') + count(owned_migration_exception where source_field='citizen_geotag_submissions.address_label') \| ASSERT count(citizen_geotag_submissions.citizen_name) == count(name_record.name_text) + count(source_payload_archive where field_path='citizen_geotag_submissions.citizen_name') + count(owned_migration_exception where source_field='citizen_geotag_submissions.citizen_name') \| ASSERT count(citizen_geotag_submissions.map_display_name) == count(name_record.name_text) + count(source_payload_archive where field_path='citizen_geotag_submissions.map_display_name') + count(owned_migration_exception where source_field='citizen_geotag_submissions.map_display_name') \| ASSERT count(citizen_geotag_submissions.reviewed_road_name) == count(name_record.name_text) + count(source_payload_archive where field_path='citizen_geotag_submissions.reviewed_road_name') + count(owned_migration_exception where source_field='citizen_geotag_submissions.reviewed_road_name') \| ASSERT count(citizen_geotag_submissions.suggested_place_name) == count(name_record.name_text) + count(source_payload_archive where field_path='citizen_geotag_submissions.suggested_place_name') + count(owned_migration_exception where source_field='citizen_geotag_submissions.suggested_place_name') \| ASSERT count(citizen_geotag_submissions.suggested_road_name) == count(name_record.name_text) + count(source_payload_archive where field_path='citizen_geotag_submissions.suggested_road_name') + count(owned_migration_exception where source_field='citizen_geotag_submissions.suggested_road_name') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU35 | WO002-R06-name-record-development_fixture_records | development_fixture_records.table_name | name_record.name_text | development_fixture_records.record_id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(development_fixture_records.table_name) == count(name_record.name_text) + count(source_payload_archive where field_path='development_fixture_records.table_name') + count(owned_migration_exception where source_field='development_fixture_records.table_name') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU36 | WO002-R06-name-record-field_submissions | field_submissions.candidate_name | name_record.name_text | field_submissions.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(field_submissions.candidate_name) == count(name_record.name_text) + count(source_payload_archive where field_path='field_submissions.candidate_name') + count(owned_migration_exception where source_field='field_submissions.candidate_name') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU37 | WO002-R06-name-record-import_jobs | import_jobs.name; import_jobs.source_name | name_record.name_text | import_jobs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(import_jobs.name) == count(name_record.name_text) + count(source_payload_archive where field_path='import_jobs.name') + count(owned_migration_exception where source_field='import_jobs.name') \| ASSERT count(import_jobs.source_name) == count(name_record.name_text) + count(source_payload_archive where field_path='import_jobs.source_name') + count(owned_migration_exception where source_field='import_jobs.source_name') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU38 | WO002-R06-name-record-import_rows | import_rows.candidate_name | name_record.name_text | import_rows.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(import_rows.candidate_name) == count(name_record.name_text) + count(source_payload_archive where field_path='import_rows.candidate_name') + count(owned_migration_exception where source_field='import_rows.candidate_name') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU39 | WO002-R06-name-record-provinces | provinces.name | name_record.name_text | provinces.code | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(provinces.name) == count(name_record.name_text) + count(source_payload_archive where field_path='provinces.name') + count(owned_migration_exception where source_field='provinces.name') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU40 | WO002-R06-name-record-publication_packs | publication_packs.name | name_record.name_text | publication_packs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(publication_packs.name) == count(name_record.name_text) + count(source_payload_archive where field_path='publication_packs.name') + count(owned_migration_exception where source_field='publication_packs.name') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU41 | WO002-R06-name-record-roads | roads.name | name_record.name_text | roads.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(roads.name) == count(name_record.name_text) + count(source_payload_archive where field_path='roads.name') + count(owned_migration_exception where source_field='roads.name') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU42 | WO002-R06-name-record-territories | territories.name | name_record.name_text | territories.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(territories.name) == count(name_record.name_text) + count(source_payload_archive where field_path='territories.name') + count(owned_migration_exception where source_field='territories.name') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU43 | WO002-R06-name-record-users | users.full_name; users.username | name_record.name_text | users.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(users.full_name) == count(name_record.name_text) + count(source_payload_archive where field_path='users.full_name') + count(owned_migration_exception where source_field='users.full_name') \| ASSERT count(users.username) == count(name_record.name_text) + count(source_payload_archive where field_path='users.username') + count(owned_migration_exception where source_field='users.username') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU44 | WO002-R06-public-code-alias-addresses | addresses.public_code | public_code_alias.public_code | addresses.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(addresses.public_code) == count(public_code_alias.public_code) + count(source_payload_archive where field_path='addresses.public_code') + count(owned_migration_exception where source_field='addresses.public_code') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU45 | WO002-R06-public-code-alias-admin_units | admin_units.code | public_code_alias.public_code | admin_units.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(admin_units.code) == count(public_code_alias.public_code) + count(source_payload_archive where field_path='admin_units.code') + count(owned_migration_exception where source_field='admin_units.code') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU46 | WO002-R06-public-code-alias-provinces | provinces.code | public_code_alias.public_code | provinces.code | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(provinces.code) == count(public_code_alias.public_code) + count(source_payload_archive where field_path='provinces.code') + count(owned_migration_exception where source_field='provinces.code') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU47 | WO002-R06-recorded-time-address_corrections | address_corrections.created_at; address_corrections.updated_at | decision_event.recorded_at | address_corrections.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(address_corrections.created_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='address_corrections.created_at') + count(owned_migration_exception where source_field='address_corrections.created_at') \| ASSERT count(address_corrections.updated_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='address_corrections.updated_at') + count(owned_migration_exception where source_field='address_corrections.updated_at') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU48 | WO002-R06-recorded-time-address_points | address_points.created_at; address_points.updated_at | decision_event.recorded_at | address_points.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(address_points.created_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='address_points.created_at') + count(owned_migration_exception where source_field='address_points.created_at') \| ASSERT count(address_points.updated_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='address_points.updated_at') + count(owned_migration_exception where source_field='address_points.updated_at') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU49 | WO002-R06-recorded-time-address_record_events | address_record_events.created_at | decision_event.recorded_at | address_record_events.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(address_record_events.created_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='address_record_events.created_at') + count(owned_migration_exception where source_field='address_record_events.created_at') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU50 | WO002-R06-recorded-time-address_records | address_records.created_at; address_records.updated_at | decision_event.recorded_at | address_records.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(address_records.created_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='address_records.created_at') + count(owned_migration_exception where source_field='address_records.created_at') \| ASSERT count(address_records.updated_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='address_records.updated_at') + count(owned_migration_exception where source_field='address_records.updated_at') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU51 | WO002-R06-recorded-time-addresses | addresses.created_at; addresses.updated_at | decision_event.recorded_at | addresses.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(addresses.created_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='addresses.created_at') + count(owned_migration_exception where source_field='addresses.created_at') \| ASSERT count(addresses.updated_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='addresses.updated_at') + count(owned_migration_exception where source_field='addresses.updated_at') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU52 | WO002-R06-recorded-time-admin_units | admin_units.created_at; admin_units.updated_at | decision_event.recorded_at | admin_units.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(admin_units.created_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='admin_units.created_at') + count(owned_migration_exception where source_field='admin_units.created_at') \| ASSERT count(admin_units.updated_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='admin_units.updated_at') + count(owned_migration_exception where source_field='admin_units.updated_at') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU53 | WO002-R06-recorded-time-audit_logs | audit_logs.created_at | decision_event.recorded_at | audit_logs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(audit_logs.created_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='audit_logs.created_at') + count(owned_migration_exception where source_field='audit_logs.created_at') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU54 | WO002-R06-recorded-time-auth_tokens | auth_tokens.created_at; auth_tokens.expires_at; auth_tokens.last_seen_at; auth_tokens.revoked_at | decision_event.recorded_at | auth_tokens.user_id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(auth_tokens.created_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='auth_tokens.created_at') + count(owned_migration_exception where source_field='auth_tokens.created_at') \| ASSERT count(auth_tokens.expires_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='auth_tokens.expires_at') + count(owned_migration_exception where source_field='auth_tokens.expires_at') \| ASSERT count(auth_tokens.last_seen_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='auth_tokens.last_seen_at') + count(owned_migration_exception where source_field='auth_tokens.last_seen_at') \| ASSERT count(auth_tokens.revoked_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='auth_tokens.revoked_at') + count(owned_migration_exception where source_field='auth_tokens.revoked_at') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU55 | WO002-R06-recorded-time-buildings | buildings.created_at; buildings.updated_at | decision_event.recorded_at | buildings.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(buildings.created_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='buildings.created_at') + count(owned_migration_exception where source_field='buildings.created_at') \| ASSERT count(buildings.updated_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='buildings.updated_at') + count(owned_migration_exception where source_field='buildings.updated_at') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU56 | WO002-R06-recorded-time-citizen_geotag_submissions | citizen_geotag_submissions.created_at; citizen_geotag_submissions.field_verified_at; citizen_geotag_submissions.identity_verified_at; citizen_geotag_submissions.updated_at | decision_event.recorded_at | citizen_geotag_submissions.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(citizen_geotag_submissions.created_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='citizen_geotag_submissions.created_at') + count(owned_migration_exception where source_field='citizen_geotag_submissions.created_at') \| ASSERT count(citizen_geotag_submissions.field_verified_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='citizen_geotag_submissions.field_verified_at') + count(owned_migration_exception where source_field='citizen_geotag_submissions.field_verified_at') \| ASSERT count(citizen_geotag_submissions.identity_verified_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='citizen_geotag_submissions.identity_verified_at') + count(owned_migration_exception where source_field='citizen_geotag_submissions.identity_verified_at') \| ASSERT count(citizen_geotag_submissions.updated_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='citizen_geotag_submissions.updated_at') + count(owned_migration_exception where source_field='citizen_geotag_submissions.updated_at') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU57 | WO002-R06-recorded-time-development_fixture_batches | development_fixture_batches.cleaned_at; development_fixture_batches.loaded_at | decision_event.recorded_at | development_fixture_batches.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(development_fixture_batches.cleaned_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='development_fixture_batches.cleaned_at') + count(owned_migration_exception where source_field='development_fixture_batches.cleaned_at') \| ASSERT count(development_fixture_batches.loaded_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='development_fixture_batches.loaded_at') + count(owned_migration_exception where source_field='development_fixture_batches.loaded_at') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU58 | WO002-R06-recorded-time-development_fixture_records | development_fixture_records.created_by_batch; development_fixture_records.recorded_at | decision_event.recorded_at | development_fixture_records.record_id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(development_fixture_records.created_by_batch) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='development_fixture_records.created_by_batch') + count(owned_migration_exception where source_field='development_fixture_records.created_by_batch') \| ASSERT count(development_fixture_records.recorded_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='development_fixture_records.recorded_at') + count(owned_migration_exception where source_field='development_fixture_records.recorded_at') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU59 | WO002-R06-recorded-time-field_assignments | field_assignments.created_at | decision_event.recorded_at | field_assignments.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(field_assignments.created_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='field_assignments.created_at') + count(owned_migration_exception where source_field='field_assignments.created_at') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU60 | WO002-R06-recorded-time-field_submissions | field_submissions.created_at; field_submissions.submitted_by; field_submissions.updated_at | decision_event.recorded_at | field_submissions.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(field_submissions.created_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='field_submissions.created_at') + count(owned_migration_exception where source_field='field_submissions.created_at') \| ASSERT count(field_submissions.submitted_by) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='field_submissions.submitted_by') + count(owned_migration_exception where source_field='field_submissions.submitted_by') \| ASSERT count(field_submissions.updated_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='field_submissions.updated_at') + count(owned_migration_exception where source_field='field_submissions.updated_at') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU61 | WO002-R06-recorded-time-import_jobs | import_jobs.created_at; import_jobs.updated_at | decision_event.recorded_at | import_jobs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(import_jobs.created_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='import_jobs.created_at') + count(owned_migration_exception where source_field='import_jobs.created_at') \| ASSERT count(import_jobs.updated_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='import_jobs.updated_at') + count(owned_migration_exception where source_field='import_jobs.updated_at') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU62 | WO002-R06-recorded-time-provinces | provinces.created_at | decision_event.recorded_at | provinces.code | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(provinces.created_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='provinces.created_at') + count(owned_migration_exception where source_field='provinces.created_at') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU63 | WO002-R06-recorded-time-publication_packs | publication_packs.created_at; publication_packs.updated_at | decision_event.recorded_at | publication_packs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(publication_packs.created_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='publication_packs.created_at') + count(owned_migration_exception where source_field='publication_packs.created_at') \| ASSERT count(publication_packs.updated_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='publication_packs.updated_at') + count(owned_migration_exception where source_field='publication_packs.updated_at') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU64 | WO002-R06-recorded-time-reference_data_load_history | reference_data_load_history.loaded_at | decision_event.recorded_at | reference_data_load_history.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(reference_data_load_history.loaded_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='reference_data_load_history.loaded_at') + count(owned_migration_exception where source_field='reference_data_load_history.loaded_at') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU65 | WO002-R06-recorded-time-reference_data_loads | reference_data_loads.loaded_at | decision_event.recorded_at | reference_data_loads.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(reference_data_loads.loaded_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='reference_data_loads.loaded_at') + count(owned_migration_exception where source_field='reference_data_loads.loaded_at') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU66 | WO002-R06-recorded-time-roads | roads.created_at; roads.updated_at | decision_event.recorded_at | roads.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(roads.created_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='roads.created_at') + count(owned_migration_exception where source_field='roads.created_at') \| ASSERT count(roads.updated_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='roads.updated_at') + count(owned_migration_exception where source_field='roads.updated_at') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU67 | WO002-R06-recorded-time-territories | territories.created_at; territories.updated_at | decision_event.recorded_at | territories.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(territories.created_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='territories.created_at') + count(owned_migration_exception where source_field='territories.created_at') \| ASSERT count(territories.updated_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='territories.updated_at') + count(owned_migration_exception where source_field='territories.updated_at') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU68 | WO002-R06-recorded-time-users | users.created_at | decision_event.recorded_at | users.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(users.created_at) == count(decision_event.recorded_at) + count(source_payload_archive where field_path='users.created_at') + count(owned_migration_exception where source_field='users.created_at') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU69 | WO002-R06-source-archive-address_corrections | address_corrections.reporter_contact | source_payload_archive.payload_uri | address_corrections.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(address_corrections.reporter_contact) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='address_corrections.reporter_contact') + count(owned_migration_exception where source_field='address_corrections.reporter_contact') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU70 | WO002-R06-source-archive-address_points | address_points.is_active; address_points.source_method | source_payload_archive.payload_uri | address_points.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(address_points.is_active) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='address_points.is_active') + count(owned_migration_exception where source_field='address_points.is_active') \| ASSERT count(address_points.source_method) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='address_points.source_method') + count(owned_migration_exception where source_field='address_points.source_method') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU71 | WO002-R06-source-archive-address_record_events | address_record_events.actor_role; address_record_events.details; address_record_events.event_type | source_payload_archive.payload_uri | address_record_events.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(address_record_events.actor_role) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='address_record_events.actor_role') + count(owned_migration_exception where source_field='address_record_events.actor_role') \| ASSERT count(address_record_events.details) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='address_record_events.details') + count(owned_migration_exception where source_field='address_record_events.details') \| ASSERT count(address_record_events.event_type) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='address_record_events.event_type') + count(owned_migration_exception where source_field='address_record_events.event_type') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU72 | WO002-R06-source-archive-address_records | address_records.address_code; address_records.geom; address_records.is_archived; address_records.province_code; address_records.publication_state; address_records.record_bundle; address_records.search_text; address_records.status | source_payload_archive.payload_uri | address_records.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(address_records.address_code) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='address_records.address_code') + count(owned_migration_exception where source_field='address_records.address_code') \| ASSERT count(address_records.geom) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='address_records.geom') + count(owned_migration_exception where source_field='address_records.geom') \| ASSERT count(address_records.is_archived) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='address_records.is_archived') + count(owned_migration_exception where source_field='address_records.is_archived') \| ASSERT count(address_records.province_code) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='address_records.province_code') + count(owned_migration_exception where source_field='address_records.province_code') \| ASSERT count(address_records.publication_state) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='address_records.publication_state') + count(owned_migration_exception where source_field='address_records.publication_state') \| ASSERT count(address_records.record_bundle) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='address_records.record_bundle') + count(owned_migration_exception where source_field='address_records.record_bundle') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU73 | WO002-R06-source-archive-addresses | addresses.formatted; addresses.is_archived; addresses.issuance_method; addresses.province_code; addresses.publication_state; addresses.source; addresses.status; addresses.verification_status | source_payload_archive.payload_uri | addresses.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(addresses.formatted) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='addresses.formatted') + count(owned_migration_exception where source_field='addresses.formatted') \| ASSERT count(addresses.is_archived) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='addresses.is_archived') + count(owned_migration_exception where source_field='addresses.is_archived') \| ASSERT count(addresses.issuance_method) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='addresses.issuance_method') + count(owned_migration_exception where source_field='addresses.issuance_method') \| ASSERT count(addresses.province_code) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='addresses.province_code') + count(owned_migration_exception where source_field='addresses.province_code') \| ASSERT count(addresses.publication_state) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='addresses.publication_state') + count(owned_migration_exception where source_field='addresses.publication_state') \| ASSERT count(addresses.source) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='addresses.source') + count(owned_migration_exception where source_field='addresses.source') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU74 | WO002-R06-source-archive-admin_units | admin_units.level; admin_units.province_code; admin_units.sort_order; admin_units.status | source_payload_archive.payload_uri | admin_units.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(admin_units.level) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='admin_units.level') + count(owned_migration_exception where source_field='admin_units.level') \| ASSERT count(admin_units.province_code) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='admin_units.province_code') + count(owned_migration_exception where source_field='admin_units.province_code') \| ASSERT count(admin_units.sort_order) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='admin_units.sort_order') + count(owned_migration_exception where source_field='admin_units.sort_order') \| ASSERT count(admin_units.status) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='admin_units.status') + count(owned_migration_exception where source_field='admin_units.status') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU75 | WO002-R06-source-archive-audit_logs | audit_logs.action; audit_logs.actor_role; audit_logs.details; audit_logs.entity_type | source_payload_archive.payload_uri | audit_logs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(audit_logs.action) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='audit_logs.action') + count(owned_migration_exception where source_field='audit_logs.action') \| ASSERT count(audit_logs.actor_role) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='audit_logs.actor_role') + count(owned_migration_exception where source_field='audit_logs.actor_role') \| ASSERT count(audit_logs.details) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='audit_logs.details') + count(owned_migration_exception where source_field='audit_logs.details') \| ASSERT count(audit_logs.entity_type) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='audit_logs.entity_type') + count(owned_migration_exception where source_field='audit_logs.entity_type') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU76 | WO002-R06-source-archive-auth_tokens | auth_tokens.token | source_payload_archive.payload_uri | auth_tokens.user_id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(auth_tokens.token) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='auth_tokens.token') + count(owned_migration_exception where source_field='auth_tokens.token') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU77 | WO002-R06-source-archive-buildings | buildings.is_archived; buildings.spatial_evidence; buildings.status; buildings.usage | source_payload_archive.payload_uri | buildings.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(buildings.is_archived) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='buildings.is_archived') + count(owned_migration_exception where source_field='buildings.is_archived') \| ASSERT count(buildings.spatial_evidence) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='buildings.spatial_evidence') + count(owned_migration_exception where source_field='buildings.spatial_evidence') \| ASSERT count(buildings.status) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='buildings.status') + count(owned_migration_exception where source_field='buildings.status') \| ASSERT count(buildings.usage) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='buildings.usage') + count(owned_migration_exception where source_field='buildings.usage') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU78 | WO002-R06-source-archive-citizen_geotag_submissions | citizen_geotag_submissions.capture_method; citizen_geotag_submissions.citizen_contact; citizen_geotag_submissions.dip_last4; citizen_geotag_submissions.duplicate_hint; citizen_geotag_submissions.field_note; citizen_geotag_submissions.field_status; citizen_geotag_submissions.grid_code; citizen_geotag_submissions.identity_document_verified; citizen_geotag_submissions.identity_verification_status; citizen_geotag_submissions.landmark; citizen_geotag_submissions.reviewer_note; citizen_geotag_submissions.road_suggestion_attribution; citizen_geotag_submissions.road_suggestion_source; citizen_geotag_submissions.road_suggestion_status; citizen_geotag_submissions.signage_batch; citizen_geotag_submissions.status; citizen_geotag_submissions.suggested_local_area | source_payload_archive.payload_uri | citizen_geotag_submissions.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(citizen_geotag_submissions.capture_method) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='citizen_geotag_submissions.capture_method') + count(owned_migration_exception where source_field='citizen_geotag_submissions.capture_method') \| ASSERT count(citizen_geotag_submissions.citizen_contact) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='citizen_geotag_submissions.citizen_contact') + count(owned_migration_exception where source_field='citizen_geotag_submissions.citizen_contact') \| ASSERT count(citizen_geotag_submissions.dip_last4) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='citizen_geotag_submissions.dip_last4') + count(owned_migration_exception where source_field='citizen_geotag_submissions.dip_last4') \| ASSERT count(citizen_geotag_submissions.duplicate_hint) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='citizen_geotag_submissions.duplicate_hint') + count(owned_migration_exception where source_field='citizen_geotag_submissions.duplicate_hint') \| ASSERT count(citizen_geotag_submissions.field_note) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='citizen_geotag_submissions.field_note') + count(owned_migration_exception where source_field='citizen_geotag_submissions.field_note') \| ASSERT count(citizen_geotag_submissions.field_status) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='citizen_geotag_submissions.field_status') + count(owned_migration_exception where source_field='citizen_geotag_submissions.field_status') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU79 | WO002-R06-source-archive-development_fixture_batches | development_fixture_batches.environment; development_fixture_batches.execution_context; development_fixture_batches.fixture_version | source_payload_archive.payload_uri | development_fixture_batches.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(development_fixture_batches.environment) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='development_fixture_batches.environment') + count(owned_migration_exception where source_field='development_fixture_batches.environment') \| ASSERT count(development_fixture_batches.execution_context) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='development_fixture_batches.execution_context') + count(owned_migration_exception where source_field='development_fixture_batches.execution_context') \| ASSERT count(development_fixture_batches.fixture_version) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='development_fixture_batches.fixture_version') + count(owned_migration_exception where source_field='development_fixture_batches.fixture_version') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU80 | WO002-R06-source-archive-field_assignments | field_assignments.priority; field_assignments.task; field_assignments.team; field_assignments.territory | source_payload_archive.payload_uri | field_assignments.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(field_assignments.priority) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='field_assignments.priority') + count(owned_migration_exception where source_field='field_assignments.priority') \| ASSERT count(field_assignments.task) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='field_assignments.task') + count(owned_migration_exception where source_field='field_assignments.task') \| ASSERT count(field_assignments.team) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='field_assignments.team') + count(owned_migration_exception where source_field='field_assignments.team') \| ASSERT count(field_assignments.territory) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='field_assignments.territory') + count(owned_migration_exception where source_field='field_assignments.territory') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU81 | WO002-R06-source-archive-field_submissions | field_submissions.candidate_status; field_submissions.notes; field_submissions.review_status; field_submissions.reviewer_note; field_submissions.spatial_evidence; field_submissions.submission_type | source_payload_archive.payload_uri | field_submissions.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(field_submissions.candidate_status) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='field_submissions.candidate_status') + count(owned_migration_exception where source_field='field_submissions.candidate_status') \| ASSERT count(field_submissions.notes) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='field_submissions.notes') + count(owned_migration_exception where source_field='field_submissions.notes') \| ASSERT count(field_submissions.review_status) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='field_submissions.review_status') + count(owned_migration_exception where source_field='field_submissions.review_status') \| ASSERT count(field_submissions.reviewer_note) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='field_submissions.reviewer_note') + count(owned_migration_exception where source_field='field_submissions.reviewer_note') \| ASSERT count(field_submissions.spatial_evidence) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='field_submissions.spatial_evidence') + count(owned_migration_exception where source_field='field_submissions.spatial_evidence') \| ASSERT count(field_submissions.submission_type) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='field_submissions.submission_type') + count(owned_migration_exception where source_field='field_submissions.submission_type') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU82 | WO002-R06-source-archive-import_jobs | import_jobs.imported_count; import_jobs.status | source_payload_archive.payload_uri | import_jobs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(import_jobs.imported_count) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='import_jobs.imported_count') + count(owned_migration_exception where source_field='import_jobs.imported_count') \| ASSERT count(import_jobs.status) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='import_jobs.status') + count(owned_migration_exception where source_field='import_jobs.status') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU83 | WO002-R06-source-archive-import_rows | import_rows.candidate_status; import_rows.notes; import_rows.row_number; import_rows.submission_type; import_rows.validation_message; import_rows.validation_status | source_payload_archive.payload_uri | import_rows.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(import_rows.candidate_status) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='import_rows.candidate_status') + count(owned_migration_exception where source_field='import_rows.candidate_status') \| ASSERT count(import_rows.notes) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='import_rows.notes') + count(owned_migration_exception where source_field='import_rows.notes') \| ASSERT count(import_rows.row_number) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='import_rows.row_number') + count(owned_migration_exception where source_field='import_rows.row_number') \| ASSERT count(import_rows.submission_type) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='import_rows.submission_type') + count(owned_migration_exception where source_field='import_rows.submission_type') \| ASSERT count(import_rows.validation_message) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='import_rows.validation_message') + count(owned_migration_exception where source_field='import_rows.validation_message') \| ASSERT count(import_rows.validation_status) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='import_rows.validation_status') + count(owned_migration_exception where source_field='import_rows.validation_status') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU84 | WO002-R06-source-archive-publication_packs | publication_packs.audience; publication_packs.status | source_payload_archive.payload_uri | publication_packs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(publication_packs.audience) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='publication_packs.audience') + count(owned_migration_exception where source_field='publication_packs.audience') \| ASSERT count(publication_packs.status) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='publication_packs.status') + count(owned_migration_exception where source_field='publication_packs.status') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU85 | WO002-R06-source-archive-reference_data_load_history | reference_data_load_history.authority_status; reference_data_load_history.execution_context; reference_data_load_history.package_checksum; reference_data_load_history.package_version; reference_data_load_history.source | source_payload_archive.payload_uri | reference_data_load_history.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(reference_data_load_history.authority_status) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='reference_data_load_history.authority_status') + count(owned_migration_exception where source_field='reference_data_load_history.authority_status') \| ASSERT count(reference_data_load_history.execution_context) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='reference_data_load_history.execution_context') + count(owned_migration_exception where source_field='reference_data_load_history.execution_context') \| ASSERT count(reference_data_load_history.package_checksum) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='reference_data_load_history.package_checksum') + count(owned_migration_exception where source_field='reference_data_load_history.package_checksum') \| ASSERT count(reference_data_load_history.package_version) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='reference_data_load_history.package_version') + count(owned_migration_exception where source_field='reference_data_load_history.package_version') \| ASSERT count(reference_data_load_history.source) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='reference_data_load_history.source') + count(owned_migration_exception where source_field='reference_data_load_history.source') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU86 | WO002-R06-source-archive-reference_data_loads | reference_data_loads.authority_status; reference_data_loads.execution_context; reference_data_loads.package_checksum; reference_data_loads.package_version; reference_data_loads.source | source_payload_archive.payload_uri | reference_data_loads.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(reference_data_loads.authority_status) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='reference_data_loads.authority_status') + count(owned_migration_exception where source_field='reference_data_loads.authority_status') \| ASSERT count(reference_data_loads.execution_context) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='reference_data_loads.execution_context') + count(owned_migration_exception where source_field='reference_data_loads.execution_context') \| ASSERT count(reference_data_loads.package_checksum) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='reference_data_loads.package_checksum') + count(owned_migration_exception where source_field='reference_data_loads.package_checksum') \| ASSERT count(reference_data_loads.package_version) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='reference_data_loads.package_version') + count(owned_migration_exception where source_field='reference_data_loads.package_version') \| ASSERT count(reference_data_loads.source) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='reference_data_loads.source') + count(owned_migration_exception where source_field='reference_data_loads.source') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU87 | WO002-R06-source-archive-roads | roads.is_archived; roads.length_km; roads.spatial_evidence; roads.status | source_payload_archive.payload_uri | roads.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(roads.is_archived) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='roads.is_archived') + count(owned_migration_exception where source_field='roads.is_archived') \| ASSERT count(roads.length_km) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='roads.length_km') + count(owned_migration_exception where source_field='roads.length_km') \| ASSERT count(roads.spatial_evidence) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='roads.spatial_evidence') + count(owned_migration_exception where source_field='roads.spatial_evidence') \| ASSERT count(roads.status) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='roads.status') + count(owned_migration_exception where source_field='roads.status') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU88 | WO002-R06-source-archive-schema_migrations | schema_migrations.applied_at; schema_migrations.checksum; schema_migrations.execution_context; schema_migrations.filename; schema_migrations.version | source_payload_archive.payload_uri | schema_migrations.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(schema_migrations.applied_at) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='schema_migrations.applied_at') + count(owned_migration_exception where source_field='schema_migrations.applied_at') \| ASSERT count(schema_migrations.checksum) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='schema_migrations.checksum') + count(owned_migration_exception where source_field='schema_migrations.checksum') \| ASSERT count(schema_migrations.execution_context) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='schema_migrations.execution_context') + count(owned_migration_exception where source_field='schema_migrations.execution_context') \| ASSERT count(schema_migrations.filename) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='schema_migrations.filename') + count(owned_migration_exception where source_field='schema_migrations.filename') \| ASSERT count(schema_migrations.version) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='schema_migrations.version') + count(owned_migration_exception where source_field='schema_migrations.version') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU89 | WO002-R06-source-archive-territories | territories.is_archived; territories.province_code; territories.readiness; territories.type | source_payload_archive.payload_uri | territories.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(territories.is_archived) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='territories.is_archived') + count(owned_migration_exception where source_field='territories.is_archived') \| ASSERT count(territories.province_code) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='territories.province_code') + count(owned_migration_exception where source_field='territories.province_code') \| ASSERT count(territories.readiness) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='territories.readiness') + count(owned_migration_exception where source_field='territories.readiness') \| ASSERT count(territories.type) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='territories.type') + count(owned_migration_exception where source_field='territories.type') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU90 | WO002-R06-source-archive-users | users.is_active; users.password_hash; users.role | source_payload_archive.payload_uri | users.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | ASSERT count(users.is_active) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='users.is_active') + count(owned_migration_exception where source_field='users.is_active') \| ASSERT count(users.password_hash) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='users.password_hash') + count(owned_migration_exception where source_field='users.password_hash') \| ASSERT count(users.role) == count(source_payload_archive.payload_uri) + count(source_payload_archive where field_path='users.role') + count(owned_migration_exception where source_field='users.role') | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |

## Workload, storage, retention, concurrency and query assumptions

| Area | Source | Baseline date | Confidence range/horizon | Workload/capacity model | Approving owner |
|---|---|---|---|---|---|
| Location records | Current pilot extrapolation + EG province rollout planning | 2026-07-14 | low 300k / base 1.2M / high 2.5M records over five years | public lookup 50 QPS base / 500 surge; operator writes batch-windowed | Registry Authority approval pending |
| Geometry observations | Field capture model: 2-4 observations per canonical subject plus correction history | 2026-07-14 | low 600k / base 3M / high 8M observations | PostGIS GiST by subject/role/current plus province/year partition candidate above 10M | GIS/Data Authority approval pending |
| Evidence/archive | Restricted evidence retention assumption; media-heavy workflows separated from registry rows | 2026-07-14 | low 2TB / base 8TB / high 20TB object storage | encrypted object store; DB stores hash/URI/classification only | Publication/Records Authority approval pending |
