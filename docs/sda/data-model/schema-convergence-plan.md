# Expand–Migrate–Contract Convergence and Scale Plan

Built only after the Review 06 transformation groups are loaded. NLI-WO-002B remains unauthorized; this is a reviewed migration-unit design source for later approval, not executable migration authority.

| Unit | Transform group | Complete source fields | Target rows/fields | Source keys | Crosswalk/exception schema | Read/write behavior | Idempotency | Conflict precedence | Validation/tolerance | Recovery point | Monitoring window | Cutover/abort gate |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| MU01 | WO002-R06-correction-case-address_corrections | address_corrections.correction_type; address_corrections.public_code; address_corrections.status | correction_case.case_state; correction_case.correction_type; correction_case.target_public_code | address_corrections.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM address_corrections WHERE correction_type IS NOT NULL) = (SELECT COUNT(*) FROM proposed_correction_case WHERE correction_type IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_corrections.correction_type') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_corrections.correction_type') THEN true ELSE raise_exception('no-loss assertion failed for address_corrections.correction_type') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_corrections WHERE public_code IS NOT NULL) = (SELECT COUNT(*) FROM proposed_correction_case WHERE target_public_code IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_corrections.public_code') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_corrections.public_code') THEN true ELSE raise_exception('no-loss assertion failed for address_corrections.public_code') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_corrections WHERE status IS NOT NULL) = (SELECT COUNT(*) FROM proposed_correction_case WHERE case_state IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_corrections.status') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_corrections.status') THEN true ELSE raise_exception('no-loss assertion failed for address_corrections.status') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU02 | WO002-R06-correction-case-archive-address_corrections | address_corrections.note; address_corrections.query; address_corrections.reason; address_corrections.reporter_name; address_corrections.reviewer_note | source_payload_archive.payload_uri | address_corrections.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM address_corrections WHERE note IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_corrections.note') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_corrections.note') THEN true ELSE raise_exception('no-loss assertion failed for address_corrections.note') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_corrections WHERE query IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_corrections.query') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_corrections.query') THEN true ELSE raise_exception('no-loss assertion failed for address_corrections.query') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_corrections WHERE reason IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_corrections.reason') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_corrections.reason') THEN true ELSE raise_exception('no-loss assertion failed for address_corrections.reason') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_corrections WHERE reporter_name IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_corrections.reporter_name') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_corrections.reporter_name') THEN true ELSE raise_exception('no-loss assertion failed for address_corrections.reporter_name') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_corrections WHERE reviewer_note IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_corrections.reviewer_note') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_corrections.reviewer_note') THEN true ELSE raise_exception('no-loss assertion failed for address_corrections.reviewer_note') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU03 | WO002-R06-geometry-observation-address_points | address_points.accuracy_meters; address_points.latitude; address_points.longitude | geometry_observation.observed_geom | address_points.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM address_points WHERE accuracy_meters IS NOT NULL) = (SELECT COUNT(*) FROM proposed_geometry_observation WHERE observed_geom IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_points.accuracy_meters') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_points.accuracy_meters') THEN true ELSE raise_exception('no-loss assertion failed for address_points.accuracy_meters') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_points WHERE latitude IS NOT NULL) = (SELECT COUNT(*) FROM proposed_geometry_observation WHERE observed_geom IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_points.latitude') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_points.latitude') THEN true ELSE raise_exception('no-loss assertion failed for address_points.latitude') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_points WHERE longitude IS NOT NULL) = (SELECT COUNT(*) FROM proposed_geometry_observation WHERE observed_geom IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_points.longitude') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_points.longitude') THEN true ELSE raise_exception('no-loss assertion failed for address_points.longitude') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU04 | WO002-R06-geometry-observation-address_records | address_records.accuracy_meters; address_records.latitude; address_records.longitude | geometry_observation.observed_geom | address_records.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM address_records WHERE accuracy_meters IS NOT NULL) = (SELECT COUNT(*) FROM proposed_geometry_observation WHERE observed_geom IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_records.accuracy_meters') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_records.accuracy_meters') THEN true ELSE raise_exception('no-loss assertion failed for address_records.accuracy_meters') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_records WHERE latitude IS NOT NULL) = (SELECT COUNT(*) FROM proposed_geometry_observation WHERE observed_geom IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_records.latitude') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_records.latitude') THEN true ELSE raise_exception('no-loss assertion failed for address_records.latitude') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_records WHERE longitude IS NOT NULL) = (SELECT COUNT(*) FROM proposed_geometry_observation WHERE observed_geom IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_records.longitude') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_records.longitude') THEN true ELSE raise_exception('no-loss assertion failed for address_records.longitude') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU05 | WO002-R06-geometry-observation-citizen_geotag_submissions | citizen_geotag_submissions.accuracy_meters; citizen_geotag_submissions.latitude; citizen_geotag_submissions.longitude | geometry_observation.observed_geom | citizen_geotag_submissions.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM citizen_geotag_submissions WHERE accuracy_meters IS NOT NULL) = (SELECT COUNT(*) FROM proposed_geometry_observation WHERE observed_geom IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'citizen_geotag_submissions.accuracy_meters') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'citizen_geotag_submissions.accuracy_meters') THEN true ELSE raise_exception('no-loss assertion failed for citizen_geotag_submissions.accuracy_meters') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM citizen_geotag_submissions WHERE latitude IS NOT NULL) = (SELECT COUNT(*) FROM proposed_geometry_observation WHERE observed_geom IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'citizen_geotag_submissions.latitude') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'citizen_geotag_submissions.latitude') THEN true ELSE raise_exception('no-loss assertion failed for citizen_geotag_submissions.latitude') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM citizen_geotag_submissions WHERE longitude IS NOT NULL) = (SELECT COUNT(*) FROM proposed_geometry_observation WHERE observed_geom IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'citizen_geotag_submissions.longitude') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'citizen_geotag_submissions.longitude') THEN true ELSE raise_exception('no-loss assertion failed for citizen_geotag_submissions.longitude') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU06 | WO002-R06-identity-crosswalk-address_corrections | address_corrections.address_id; address_corrections.id | legacy_crosswalk.legacy_id | address_corrections.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM address_corrections WHERE address_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_corrections.address_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_corrections.address_id') THEN true ELSE raise_exception('no-loss assertion failed for address_corrections.address_id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_corrections WHERE id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_corrections.id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_corrections.id') THEN true ELSE raise_exception('no-loss assertion failed for address_corrections.id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU07 | WO002-R06-identity-crosswalk-address_points | address_points.address_id; address_points.id | legacy_crosswalk.legacy_id | address_points.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM address_points WHERE address_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_points.address_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_points.address_id') THEN true ELSE raise_exception('no-loss assertion failed for address_points.address_id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_points WHERE id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_points.id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_points.id') THEN true ELSE raise_exception('no-loss assertion failed for address_points.id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU08 | WO002-R06-identity-crosswalk-address_record_events | address_record_events.actor_id; address_record_events.address_record_id; address_record_events.id | legacy_crosswalk.legacy_id; migration_exception.migration_exception_id | address_record_events.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM address_record_events WHERE actor_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_record_events.actor_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_record_events.actor_id') THEN true ELSE raise_exception('no-loss assertion failed for address_record_events.actor_id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_record_events WHERE address_record_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_record_events.address_record_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_record_events.address_record_id') THEN true ELSE raise_exception('no-loss assertion failed for address_record_events.address_record_id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_record_events WHERE id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_record_events.id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_record_events.id') THEN true ELSE raise_exception('no-loss assertion failed for address_record_events.id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU09 | WO002-R06-identity-crosswalk-address_records | address_records.id; address_records.source_submission_id; address_records.territory_id | legacy_crosswalk.legacy_id; migration_exception.migration_exception_id | address_records.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM address_records WHERE id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_records.id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_records.id') THEN true ELSE raise_exception('no-loss assertion failed for address_records.id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_records WHERE source_submission_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_records.source_submission_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_records.source_submission_id') THEN true ELSE raise_exception('no-loss assertion failed for address_records.source_submission_id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_records WHERE territory_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_records.territory_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_records.territory_id') THEN true ELSE raise_exception('no-loss assertion failed for address_records.territory_id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU10 | WO002-R06-identity-crosswalk-addresses | addresses.building_id; addresses.id; addresses.road_id; addresses.superseded_by_address_id; addresses.territory_id | legacy_crosswalk.legacy_id; migration_exception.migration_exception_id | addresses.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM addresses WHERE building_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'addresses.building_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'addresses.building_id') THEN true ELSE raise_exception('no-loss assertion failed for addresses.building_id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM addresses WHERE id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'addresses.id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'addresses.id') THEN true ELSE raise_exception('no-loss assertion failed for addresses.id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM addresses WHERE road_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'addresses.road_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'addresses.road_id') THEN true ELSE raise_exception('no-loss assertion failed for addresses.road_id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM addresses WHERE superseded_by_address_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'addresses.superseded_by_address_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'addresses.superseded_by_address_id') THEN true ELSE raise_exception('no-loss assertion failed for addresses.superseded_by_address_id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM addresses WHERE territory_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'addresses.territory_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'addresses.territory_id') THEN true ELSE raise_exception('no-loss assertion failed for addresses.territory_id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU11 | WO002-R06-identity-crosswalk-admin_units | admin_units.id; admin_units.parent_id | legacy_crosswalk.legacy_id; migration_exception.migration_exception_id | admin_units.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM admin_units WHERE id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'admin_units.id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'admin_units.id') THEN true ELSE raise_exception('no-loss assertion failed for admin_units.id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM admin_units WHERE parent_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'admin_units.parent_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'admin_units.parent_id') THEN true ELSE raise_exception('no-loss assertion failed for admin_units.parent_id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU12 | WO002-R06-identity-crosswalk-audit_logs | audit_logs.actor_user_id; audit_logs.entity_id; audit_logs.id | legacy_crosswalk.legacy_id; migration_exception.migration_exception_id | audit_logs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM audit_logs WHERE actor_user_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'audit_logs.actor_user_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'audit_logs.actor_user_id') THEN true ELSE raise_exception('no-loss assertion failed for audit_logs.actor_user_id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM audit_logs WHERE entity_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'audit_logs.entity_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'audit_logs.entity_id') THEN true ELSE raise_exception('no-loss assertion failed for audit_logs.entity_id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM audit_logs WHERE id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'audit_logs.id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'audit_logs.id') THEN true ELSE raise_exception('no-loss assertion failed for audit_logs.id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU13 | WO002-R06-identity-crosswalk-auth_tokens | auth_tokens.user_id | legacy_crosswalk.legacy_id | auth_tokens.user_id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM auth_tokens WHERE user_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'auth_tokens.user_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'auth_tokens.user_id') THEN true ELSE raise_exception('no-loss assertion failed for auth_tokens.user_id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU14 | WO002-R06-identity-crosswalk-buildings | buildings.id; buildings.road_id; buildings.territory_id | legacy_crosswalk.legacy_id | buildings.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM buildings WHERE id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'buildings.id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'buildings.id') THEN true ELSE raise_exception('no-loss assertion failed for buildings.id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM buildings WHERE road_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'buildings.road_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'buildings.road_id') THEN true ELSE raise_exception('no-loss assertion failed for buildings.road_id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM buildings WHERE territory_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'buildings.territory_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'buildings.territory_id') THEN true ELSE raise_exception('no-loss assertion failed for buildings.territory_id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU15 | WO002-R06-identity-crosswalk-citizen_geotag_submissions | citizen_geotag_submissions.field_submission_id; citizen_geotag_submissions.id; citizen_geotag_submissions.territory_id | legacy_crosswalk.legacy_id; migration_exception.migration_exception_id | citizen_geotag_submissions.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM citizen_geotag_submissions WHERE field_submission_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'citizen_geotag_submissions.field_submission_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'citizen_geotag_submissions.field_submission_id') THEN true ELSE raise_exception('no-loss assertion failed for citizen_geotag_submissions.field_submission_id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM citizen_geotag_submissions WHERE id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'citizen_geotag_submissions.id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'citizen_geotag_submissions.id') THEN true ELSE raise_exception('no-loss assertion failed for citizen_geotag_submissions.id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM citizen_geotag_submissions WHERE territory_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'citizen_geotag_submissions.territory_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'citizen_geotag_submissions.territory_id') THEN true ELSE raise_exception('no-loss assertion failed for citizen_geotag_submissions.territory_id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU16 | WO002-R06-identity-crosswalk-development_fixture_batches | development_fixture_batches.batch_id | migration_exception.migration_exception_id | development_fixture_batches.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM development_fixture_batches WHERE batch_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'development_fixture_batches.batch_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'development_fixture_batches.batch_id') THEN true ELSE raise_exception('no-loss assertion failed for development_fixture_batches.batch_id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU17 | WO002-R06-identity-crosswalk-development_fixture_records | development_fixture_records.batch_id; development_fixture_records.record_id | migration_exception.migration_exception_id | development_fixture_records.record_id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM development_fixture_records WHERE batch_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'development_fixture_records.batch_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'development_fixture_records.batch_id') THEN true ELSE raise_exception('no-loss assertion failed for development_fixture_records.batch_id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM development_fixture_records WHERE record_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'development_fixture_records.record_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'development_fixture_records.record_id') THEN true ELSE raise_exception('no-loss assertion failed for development_fixture_records.record_id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU18 | WO002-R06-identity-crosswalk-field_assignments | field_assignments.assignment_id; field_assignments.territory_id | legacy_crosswalk.legacy_id; migration_exception.migration_exception_id | field_assignments.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM field_assignments WHERE assignment_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'field_assignments.assignment_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'field_assignments.assignment_id') THEN true ELSE raise_exception('no-loss assertion failed for field_assignments.assignment_id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM field_assignments WHERE territory_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'field_assignments.territory_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'field_assignments.territory_id') THEN true ELSE raise_exception('no-loss assertion failed for field_assignments.territory_id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU19 | WO002-R06-identity-crosswalk-field_submissions | field_submissions.assignment_id; field_submissions.id; field_submissions.registry_entity_id; field_submissions.territory_id | legacy_crosswalk.legacy_id; migration_exception.migration_exception_id | field_submissions.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM field_submissions WHERE assignment_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'field_submissions.assignment_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'field_submissions.assignment_id') THEN true ELSE raise_exception('no-loss assertion failed for field_submissions.assignment_id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM field_submissions WHERE id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'field_submissions.id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'field_submissions.id') THEN true ELSE raise_exception('no-loss assertion failed for field_submissions.id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM field_submissions WHERE registry_entity_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'field_submissions.registry_entity_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'field_submissions.registry_entity_id') THEN true ELSE raise_exception('no-loss assertion failed for field_submissions.registry_entity_id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM field_submissions WHERE territory_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'field_submissions.territory_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'field_submissions.territory_id') THEN true ELSE raise_exception('no-loss assertion failed for field_submissions.territory_id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU20 | WO002-R06-identity-crosswalk-import_jobs | import_jobs.id | legacy_crosswalk.legacy_id | import_jobs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM import_jobs WHERE id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'import_jobs.id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'import_jobs.id') THEN true ELSE raise_exception('no-loss assertion failed for import_jobs.id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU21 | WO002-R06-identity-crosswalk-import_rows | import_rows.committed_submission_id; import_rows.job_id; import_rows.territory_id | legacy_crosswalk.legacy_id; migration_exception.migration_exception_id | import_rows.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM import_rows WHERE committed_submission_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'import_rows.committed_submission_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'import_rows.committed_submission_id') THEN true ELSE raise_exception('no-loss assertion failed for import_rows.committed_submission_id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM import_rows WHERE job_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'import_rows.job_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'import_rows.job_id') THEN true ELSE raise_exception('no-loss assertion failed for import_rows.job_id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM import_rows WHERE territory_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'import_rows.territory_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'import_rows.territory_id') THEN true ELSE raise_exception('no-loss assertion failed for import_rows.territory_id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU22 | WO002-R06-identity-crosswalk-publication_pack_addresses | publication_pack_addresses.address_id; publication_pack_addresses.publication_pack_id | legacy_crosswalk.legacy_id; migration_exception.migration_exception_id | publication_pack_addresses.address_id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM publication_pack_addresses WHERE address_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'publication_pack_addresses.address_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'publication_pack_addresses.address_id') THEN true ELSE raise_exception('no-loss assertion failed for publication_pack_addresses.address_id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM publication_pack_addresses WHERE publication_pack_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'publication_pack_addresses.publication_pack_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'publication_pack_addresses.publication_pack_id') THEN true ELSE raise_exception('no-loss assertion failed for publication_pack_addresses.publication_pack_id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU23 | WO002-R06-identity-crosswalk-publication_packs | publication_packs.id | legacy_crosswalk.legacy_id | publication_packs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM publication_packs WHERE id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'publication_packs.id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'publication_packs.id') THEN true ELSE raise_exception('no-loss assertion failed for publication_packs.id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU24 | WO002-R06-identity-crosswalk-reference_data_load_history | reference_data_load_history.id; reference_data_load_history.package_id | legacy_crosswalk.legacy_id; migration_exception.migration_exception_id | reference_data_load_history.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM reference_data_load_history WHERE id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'reference_data_load_history.id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'reference_data_load_history.id') THEN true ELSE raise_exception('no-loss assertion failed for reference_data_load_history.id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM reference_data_load_history WHERE package_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'reference_data_load_history.package_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'reference_data_load_history.package_id') THEN true ELSE raise_exception('no-loss assertion failed for reference_data_load_history.package_id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU25 | WO002-R06-identity-crosswalk-reference_data_loads | reference_data_loads.package_id | migration_exception.migration_exception_id | reference_data_loads.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM reference_data_loads WHERE package_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'reference_data_loads.package_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'reference_data_loads.package_id') THEN true ELSE raise_exception('no-loss assertion failed for reference_data_loads.package_id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU26 | WO002-R06-identity-crosswalk-roads | roads.id; roads.territory_id | legacy_crosswalk.legacy_id | roads.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM roads WHERE id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'roads.id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'roads.id') THEN true ELSE raise_exception('no-loss assertion failed for roads.id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM roads WHERE territory_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'roads.territory_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'roads.territory_id') THEN true ELSE raise_exception('no-loss assertion failed for roads.territory_id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU27 | WO002-R06-identity-crosswalk-territories | territories.admin_unit_id; territories.id | legacy_crosswalk.legacy_id | territories.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM territories WHERE admin_unit_id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'territories.admin_unit_id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'territories.admin_unit_id') THEN true ELSE raise_exception('no-loss assertion failed for territories.admin_unit_id') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM territories WHERE id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'territories.id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'territories.id') THEN true ELSE raise_exception('no-loss assertion failed for territories.id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU28 | WO002-R06-identity-crosswalk-users | users.id | legacy_crosswalk.legacy_id | users.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM users WHERE id IS NOT NULL) = (SELECT COUNT(*) FROM proposed_legacy_crosswalk WHERE legacy_id IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'users.id') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'users.id') THEN true ELSE raise_exception('no-loss assertion failed for users.id') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU29 | WO002-R06-name-record-address_record_events | address_record_events.actor_username | name_record.name_text | address_record_events.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM address_record_events WHERE actor_username IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_record_events.actor_username') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_record_events.actor_username') THEN true ELSE raise_exception('no-loss assertion failed for address_record_events.actor_username') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU30 | WO002-R06-name-record-address_records | address_records.address_label | name_record.name_text | address_records.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM address_records WHERE address_label IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_records.address_label') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_records.address_label') THEN true ELSE raise_exception('no-loss assertion failed for address_records.address_label') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU31 | WO002-R06-name-record-admin_units | admin_units.name_en; admin_units.name_es | name_record.name_text | admin_units.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM admin_units WHERE name_en IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'admin_units.name_en') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'admin_units.name_en') THEN true ELSE raise_exception('no-loss assertion failed for admin_units.name_en') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM admin_units WHERE name_es IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'admin_units.name_es') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'admin_units.name_es') THEN true ELSE raise_exception('no-loss assertion failed for admin_units.name_es') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU32 | WO002-R06-name-record-audit_logs | audit_logs.actor_username | name_record.name_text | audit_logs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM audit_logs WHERE actor_username IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'audit_logs.actor_username') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'audit_logs.actor_username') THEN true ELSE raise_exception('no-loss assertion failed for audit_logs.actor_username') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU33 | WO002-R06-name-record-buildings | buildings.label | name_record.name_text | buildings.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM buildings WHERE label IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'buildings.label') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'buildings.label') THEN true ELSE raise_exception('no-loss assertion failed for buildings.label') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU34 | WO002-R06-name-record-citizen_geotag_submissions | citizen_geotag_submissions.address_label; citizen_geotag_submissions.citizen_name; citizen_geotag_submissions.map_display_name; citizen_geotag_submissions.reviewed_road_name; citizen_geotag_submissions.suggested_place_name; citizen_geotag_submissions.suggested_road_name | name_record.name_text | citizen_geotag_submissions.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM citizen_geotag_submissions WHERE address_label IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'citizen_geotag_submissions.address_label') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'citizen_geotag_submissions.address_label') THEN true ELSE raise_exception('no-loss assertion failed for citizen_geotag_submissions.address_label') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM citizen_geotag_submissions WHERE citizen_name IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'citizen_geotag_submissions.citizen_name') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'citizen_geotag_submissions.citizen_name') THEN true ELSE raise_exception('no-loss assertion failed for citizen_geotag_submissions.citizen_name') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM citizen_geotag_submissions WHERE map_display_name IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'citizen_geotag_submissions.map_display_name') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'citizen_geotag_submissions.map_display_name') THEN true ELSE raise_exception('no-loss assertion failed for citizen_geotag_submissions.map_display_name') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM citizen_geotag_submissions WHERE reviewed_road_name IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'citizen_geotag_submissions.reviewed_road_name') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'citizen_geotag_submissions.reviewed_road_name') THEN true ELSE raise_exception('no-loss assertion failed for citizen_geotag_submissions.reviewed_road_name') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM citizen_geotag_submissions WHERE suggested_place_name IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'citizen_geotag_submissions.suggested_place_name') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'citizen_geotag_submissions.suggested_place_name') THEN true ELSE raise_exception('no-loss assertion failed for citizen_geotag_submissions.suggested_place_name') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM citizen_geotag_submissions WHERE suggested_road_name IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'citizen_geotag_submissions.suggested_road_name') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'citizen_geotag_submissions.suggested_road_name') THEN true ELSE raise_exception('no-loss assertion failed for citizen_geotag_submissions.suggested_road_name') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU35 | WO002-R06-name-record-development_fixture_records | development_fixture_records.table_name | name_record.name_text | development_fixture_records.record_id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM development_fixture_records WHERE table_name IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'development_fixture_records.table_name') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'development_fixture_records.table_name') THEN true ELSE raise_exception('no-loss assertion failed for development_fixture_records.table_name') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU36 | WO002-R06-name-record-field_submissions | field_submissions.candidate_name | name_record.name_text | field_submissions.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM field_submissions WHERE candidate_name IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'field_submissions.candidate_name') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'field_submissions.candidate_name') THEN true ELSE raise_exception('no-loss assertion failed for field_submissions.candidate_name') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU37 | WO002-R06-name-record-import_jobs | import_jobs.name; import_jobs.source_name | name_record.name_text | import_jobs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM import_jobs WHERE name IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'import_jobs.name') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'import_jobs.name') THEN true ELSE raise_exception('no-loss assertion failed for import_jobs.name') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM import_jobs WHERE source_name IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'import_jobs.source_name') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'import_jobs.source_name') THEN true ELSE raise_exception('no-loss assertion failed for import_jobs.source_name') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU38 | WO002-R06-name-record-import_rows | import_rows.candidate_name | name_record.name_text | import_rows.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM import_rows WHERE candidate_name IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'import_rows.candidate_name') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'import_rows.candidate_name') THEN true ELSE raise_exception('no-loss assertion failed for import_rows.candidate_name') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU39 | WO002-R06-name-record-provinces | provinces.name | name_record.name_text | provinces.code | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM provinces WHERE name IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'provinces.name') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'provinces.name') THEN true ELSE raise_exception('no-loss assertion failed for provinces.name') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU40 | WO002-R06-name-record-publication_packs | publication_packs.name | name_record.name_text | publication_packs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM publication_packs WHERE name IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'publication_packs.name') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'publication_packs.name') THEN true ELSE raise_exception('no-loss assertion failed for publication_packs.name') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU41 | WO002-R06-name-record-roads | roads.name | name_record.name_text | roads.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM roads WHERE name IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'roads.name') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'roads.name') THEN true ELSE raise_exception('no-loss assertion failed for roads.name') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU42 | WO002-R06-name-record-territories | territories.name | name_record.name_text | territories.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM territories WHERE name IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'territories.name') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'territories.name') THEN true ELSE raise_exception('no-loss assertion failed for territories.name') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU43 | WO002-R06-name-record-users | users.full_name; users.username | name_record.name_text | users.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM users WHERE full_name IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'users.full_name') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'users.full_name') THEN true ELSE raise_exception('no-loss assertion failed for users.full_name') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM users WHERE username IS NOT NULL) = (SELECT COUNT(*) FROM proposed_name_record WHERE name_text IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'users.username') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'users.username') THEN true ELSE raise_exception('no-loss assertion failed for users.username') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU44 | WO002-R06-public-code-alias-addresses | addresses.public_code | public_code_alias.public_code | addresses.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM addresses WHERE public_code IS NOT NULL) = (SELECT COUNT(*) FROM proposed_public_code_alias WHERE public_code IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'addresses.public_code') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'addresses.public_code') THEN true ELSE raise_exception('no-loss assertion failed for addresses.public_code') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU45 | WO002-R06-public-code-alias-admin_units | admin_units.code | public_code_alias.public_code | admin_units.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM admin_units WHERE code IS NOT NULL) = (SELECT COUNT(*) FROM proposed_public_code_alias WHERE public_code IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'admin_units.code') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'admin_units.code') THEN true ELSE raise_exception('no-loss assertion failed for admin_units.code') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU46 | WO002-R06-public-code-alias-provinces | provinces.code | public_code_alias.public_code | provinces.code | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM provinces WHERE code IS NOT NULL) = (SELECT COUNT(*) FROM proposed_public_code_alias WHERE public_code IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'provinces.code') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'provinces.code') THEN true ELSE raise_exception('no-loss assertion failed for provinces.code') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU47 | WO002-R06-recorded-time-address_corrections | address_corrections.created_at; address_corrections.updated_at | decision_event.recorded_at | address_corrections.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM address_corrections WHERE created_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_corrections.created_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_corrections.created_at') THEN true ELSE raise_exception('no-loss assertion failed for address_corrections.created_at') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_corrections WHERE updated_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_corrections.updated_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_corrections.updated_at') THEN true ELSE raise_exception('no-loss assertion failed for address_corrections.updated_at') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU48 | WO002-R06-recorded-time-address_points | address_points.created_at; address_points.updated_at | decision_event.recorded_at | address_points.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM address_points WHERE created_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_points.created_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_points.created_at') THEN true ELSE raise_exception('no-loss assertion failed for address_points.created_at') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_points WHERE updated_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_points.updated_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_points.updated_at') THEN true ELSE raise_exception('no-loss assertion failed for address_points.updated_at') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU49 | WO002-R06-recorded-time-address_record_events | address_record_events.created_at | decision_event.recorded_at | address_record_events.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM address_record_events WHERE created_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_record_events.created_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_record_events.created_at') THEN true ELSE raise_exception('no-loss assertion failed for address_record_events.created_at') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU50 | WO002-R06-recorded-time-address_records | address_records.created_at; address_records.updated_at | decision_event.recorded_at | address_records.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM address_records WHERE created_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_records.created_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_records.created_at') THEN true ELSE raise_exception('no-loss assertion failed for address_records.created_at') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_records WHERE updated_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_records.updated_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_records.updated_at') THEN true ELSE raise_exception('no-loss assertion failed for address_records.updated_at') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU51 | WO002-R06-recorded-time-addresses | addresses.created_at; addresses.updated_at | decision_event.recorded_at | addresses.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM addresses WHERE created_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'addresses.created_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'addresses.created_at') THEN true ELSE raise_exception('no-loss assertion failed for addresses.created_at') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM addresses WHERE updated_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'addresses.updated_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'addresses.updated_at') THEN true ELSE raise_exception('no-loss assertion failed for addresses.updated_at') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU52 | WO002-R06-recorded-time-admin_units | admin_units.created_at; admin_units.updated_at | decision_event.recorded_at | admin_units.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM admin_units WHERE created_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'admin_units.created_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'admin_units.created_at') THEN true ELSE raise_exception('no-loss assertion failed for admin_units.created_at') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM admin_units WHERE updated_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'admin_units.updated_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'admin_units.updated_at') THEN true ELSE raise_exception('no-loss assertion failed for admin_units.updated_at') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU53 | WO002-R06-recorded-time-audit_logs | audit_logs.created_at | decision_event.recorded_at | audit_logs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM audit_logs WHERE created_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'audit_logs.created_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'audit_logs.created_at') THEN true ELSE raise_exception('no-loss assertion failed for audit_logs.created_at') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU54 | WO002-R06-recorded-time-auth_tokens | auth_tokens.created_at; auth_tokens.expires_at; auth_tokens.last_seen_at; auth_tokens.revoked_at | decision_event.recorded_at | auth_tokens.user_id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM auth_tokens WHERE created_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'auth_tokens.created_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'auth_tokens.created_at') THEN true ELSE raise_exception('no-loss assertion failed for auth_tokens.created_at') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM auth_tokens WHERE expires_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'auth_tokens.expires_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'auth_tokens.expires_at') THEN true ELSE raise_exception('no-loss assertion failed for auth_tokens.expires_at') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM auth_tokens WHERE last_seen_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'auth_tokens.last_seen_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'auth_tokens.last_seen_at') THEN true ELSE raise_exception('no-loss assertion failed for auth_tokens.last_seen_at') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM auth_tokens WHERE revoked_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'auth_tokens.revoked_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'auth_tokens.revoked_at') THEN true ELSE raise_exception('no-loss assertion failed for auth_tokens.revoked_at') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU55 | WO002-R06-recorded-time-buildings | buildings.created_at; buildings.updated_at | decision_event.recorded_at | buildings.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM buildings WHERE created_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'buildings.created_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'buildings.created_at') THEN true ELSE raise_exception('no-loss assertion failed for buildings.created_at') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM buildings WHERE updated_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'buildings.updated_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'buildings.updated_at') THEN true ELSE raise_exception('no-loss assertion failed for buildings.updated_at') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU56 | WO002-R06-recorded-time-citizen_geotag_submissions | citizen_geotag_submissions.created_at; citizen_geotag_submissions.field_verified_at; citizen_geotag_submissions.identity_verified_at; citizen_geotag_submissions.updated_at | decision_event.recorded_at | citizen_geotag_submissions.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM citizen_geotag_submissions WHERE created_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'citizen_geotag_submissions.created_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'citizen_geotag_submissions.created_at') THEN true ELSE raise_exception('no-loss assertion failed for citizen_geotag_submissions.created_at') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM citizen_geotag_submissions WHERE field_verified_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'citizen_geotag_submissions.field_verified_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'citizen_geotag_submissions.field_verified_at') THEN true ELSE raise_exception('no-loss assertion failed for citizen_geotag_submissions.field_verified_at') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM citizen_geotag_submissions WHERE identity_verified_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'citizen_geotag_submissions.identity_verified_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'citizen_geotag_submissions.identity_verified_at') THEN true ELSE raise_exception('no-loss assertion failed for citizen_geotag_submissions.identity_verified_at') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM citizen_geotag_submissions WHERE updated_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'citizen_geotag_submissions.updated_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'citizen_geotag_submissions.updated_at') THEN true ELSE raise_exception('no-loss assertion failed for citizen_geotag_submissions.updated_at') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU57 | WO002-R06-recorded-time-development_fixture_batches | development_fixture_batches.cleaned_at; development_fixture_batches.loaded_at | decision_event.recorded_at | development_fixture_batches.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM development_fixture_batches WHERE cleaned_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'development_fixture_batches.cleaned_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'development_fixture_batches.cleaned_at') THEN true ELSE raise_exception('no-loss assertion failed for development_fixture_batches.cleaned_at') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM development_fixture_batches WHERE loaded_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'development_fixture_batches.loaded_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'development_fixture_batches.loaded_at') THEN true ELSE raise_exception('no-loss assertion failed for development_fixture_batches.loaded_at') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU58 | WO002-R06-recorded-time-development_fixture_records | development_fixture_records.created_by_batch; development_fixture_records.recorded_at | decision_event.recorded_at | development_fixture_records.record_id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM development_fixture_records WHERE created_by_batch IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'development_fixture_records.created_by_batch') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'development_fixture_records.created_by_batch') THEN true ELSE raise_exception('no-loss assertion failed for development_fixture_records.created_by_batch') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM development_fixture_records WHERE recorded_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'development_fixture_records.recorded_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'development_fixture_records.recorded_at') THEN true ELSE raise_exception('no-loss assertion failed for development_fixture_records.recorded_at') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU59 | WO002-R06-recorded-time-field_assignments | field_assignments.created_at | decision_event.recorded_at | field_assignments.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM field_assignments WHERE created_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'field_assignments.created_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'field_assignments.created_at') THEN true ELSE raise_exception('no-loss assertion failed for field_assignments.created_at') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU60 | WO002-R06-recorded-time-field_submissions | field_submissions.created_at; field_submissions.submitted_by; field_submissions.updated_at | decision_event.recorded_at | field_submissions.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM field_submissions WHERE created_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'field_submissions.created_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'field_submissions.created_at') THEN true ELSE raise_exception('no-loss assertion failed for field_submissions.created_at') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM field_submissions WHERE submitted_by IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'field_submissions.submitted_by') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'field_submissions.submitted_by') THEN true ELSE raise_exception('no-loss assertion failed for field_submissions.submitted_by') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM field_submissions WHERE updated_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'field_submissions.updated_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'field_submissions.updated_at') THEN true ELSE raise_exception('no-loss assertion failed for field_submissions.updated_at') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU61 | WO002-R06-recorded-time-import_jobs | import_jobs.created_at; import_jobs.updated_at | decision_event.recorded_at | import_jobs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM import_jobs WHERE created_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'import_jobs.created_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'import_jobs.created_at') THEN true ELSE raise_exception('no-loss assertion failed for import_jobs.created_at') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM import_jobs WHERE updated_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'import_jobs.updated_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'import_jobs.updated_at') THEN true ELSE raise_exception('no-loss assertion failed for import_jobs.updated_at') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU62 | WO002-R06-recorded-time-provinces | provinces.created_at | decision_event.recorded_at | provinces.code | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM provinces WHERE created_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'provinces.created_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'provinces.created_at') THEN true ELSE raise_exception('no-loss assertion failed for provinces.created_at') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU63 | WO002-R06-recorded-time-publication_packs | publication_packs.created_at; publication_packs.updated_at | decision_event.recorded_at | publication_packs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM publication_packs WHERE created_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'publication_packs.created_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'publication_packs.created_at') THEN true ELSE raise_exception('no-loss assertion failed for publication_packs.created_at') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM publication_packs WHERE updated_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'publication_packs.updated_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'publication_packs.updated_at') THEN true ELSE raise_exception('no-loss assertion failed for publication_packs.updated_at') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU64 | WO002-R06-recorded-time-reference_data_load_history | reference_data_load_history.loaded_at | decision_event.recorded_at | reference_data_load_history.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM reference_data_load_history WHERE loaded_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'reference_data_load_history.loaded_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'reference_data_load_history.loaded_at') THEN true ELSE raise_exception('no-loss assertion failed for reference_data_load_history.loaded_at') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU65 | WO002-R06-recorded-time-reference_data_loads | reference_data_loads.loaded_at | decision_event.recorded_at | reference_data_loads.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM reference_data_loads WHERE loaded_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'reference_data_loads.loaded_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'reference_data_loads.loaded_at') THEN true ELSE raise_exception('no-loss assertion failed for reference_data_loads.loaded_at') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU66 | WO002-R06-recorded-time-roads | roads.created_at; roads.updated_at | decision_event.recorded_at | roads.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM roads WHERE created_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'roads.created_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'roads.created_at') THEN true ELSE raise_exception('no-loss assertion failed for roads.created_at') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM roads WHERE updated_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'roads.updated_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'roads.updated_at') THEN true ELSE raise_exception('no-loss assertion failed for roads.updated_at') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU67 | WO002-R06-recorded-time-territories | territories.created_at; territories.updated_at | decision_event.recorded_at | territories.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM territories WHERE created_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'territories.created_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'territories.created_at') THEN true ELSE raise_exception('no-loss assertion failed for territories.created_at') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM territories WHERE updated_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'territories.updated_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'territories.updated_at') THEN true ELSE raise_exception('no-loss assertion failed for territories.updated_at') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU68 | WO002-R06-recorded-time-users | users.created_at | decision_event.recorded_at | users.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM users WHERE created_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_decision_event WHERE recorded_at IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'users.created_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'users.created_at') THEN true ELSE raise_exception('no-loss assertion failed for users.created_at') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU69 | WO002-R06-source-archive-address_corrections | address_corrections.reporter_contact | source_payload_archive.payload_uri | address_corrections.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM address_corrections WHERE reporter_contact IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_corrections.reporter_contact') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_corrections.reporter_contact') THEN true ELSE raise_exception('no-loss assertion failed for address_corrections.reporter_contact') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU70 | WO002-R06-source-archive-address_points | address_points.is_active; address_points.source_method | source_payload_archive.payload_uri | address_points.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM address_points WHERE is_active IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_points.is_active') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_points.is_active') THEN true ELSE raise_exception('no-loss assertion failed for address_points.is_active') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_points WHERE source_method IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_points.source_method') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_points.source_method') THEN true ELSE raise_exception('no-loss assertion failed for address_points.source_method') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU71 | WO002-R06-source-archive-address_record_events | address_record_events.actor_role; address_record_events.details; address_record_events.event_type | source_payload_archive.payload_uri | address_record_events.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM address_record_events WHERE actor_role IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_record_events.actor_role') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_record_events.actor_role') THEN true ELSE raise_exception('no-loss assertion failed for address_record_events.actor_role') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_record_events WHERE details IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_record_events.details') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_record_events.details') THEN true ELSE raise_exception('no-loss assertion failed for address_record_events.details') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_record_events WHERE event_type IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_record_events.event_type') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_record_events.event_type') THEN true ELSE raise_exception('no-loss assertion failed for address_record_events.event_type') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU72 | WO002-R06-source-archive-address_records | address_records.address_code; address_records.geom; address_records.is_archived; address_records.province_code; address_records.publication_state; address_records.record_bundle; address_records.search_text; address_records.status | source_payload_archive.payload_uri | address_records.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM address_records WHERE address_code IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_records.address_code') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_records.address_code') THEN true ELSE raise_exception('no-loss assertion failed for address_records.address_code') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_records WHERE geom IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_records.geom') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_records.geom') THEN true ELSE raise_exception('no-loss assertion failed for address_records.geom') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_records WHERE is_archived IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_records.is_archived') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_records.is_archived') THEN true ELSE raise_exception('no-loss assertion failed for address_records.is_archived') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_records WHERE province_code IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_records.province_code') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_records.province_code') THEN true ELSE raise_exception('no-loss assertion failed for address_records.province_code') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_records WHERE publication_state IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_records.publication_state') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_records.publication_state') THEN true ELSE raise_exception('no-loss assertion failed for address_records.publication_state') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM address_records WHERE record_bundle IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'address_records.record_bundle') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'address_records.record_bundle') THEN true ELSE raise_exception('no-loss assertion failed for address_records.record_bundle') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU73 | WO002-R06-source-archive-addresses | addresses.formatted; addresses.is_archived; addresses.issuance_method; addresses.province_code; addresses.publication_state; addresses.source; addresses.status; addresses.verification_status | source_payload_archive.payload_uri | addresses.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM addresses WHERE formatted IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'addresses.formatted') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'addresses.formatted') THEN true ELSE raise_exception('no-loss assertion failed for addresses.formatted') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM addresses WHERE is_archived IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'addresses.is_archived') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'addresses.is_archived') THEN true ELSE raise_exception('no-loss assertion failed for addresses.is_archived') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM addresses WHERE issuance_method IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'addresses.issuance_method') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'addresses.issuance_method') THEN true ELSE raise_exception('no-loss assertion failed for addresses.issuance_method') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM addresses WHERE province_code IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'addresses.province_code') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'addresses.province_code') THEN true ELSE raise_exception('no-loss assertion failed for addresses.province_code') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM addresses WHERE publication_state IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'addresses.publication_state') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'addresses.publication_state') THEN true ELSE raise_exception('no-loss assertion failed for addresses.publication_state') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM addresses WHERE source IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'addresses.source') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'addresses.source') THEN true ELSE raise_exception('no-loss assertion failed for addresses.source') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU74 | WO002-R06-source-archive-admin_units | admin_units.level; admin_units.province_code; admin_units.sort_order; admin_units.status | source_payload_archive.payload_uri | admin_units.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM admin_units WHERE level IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'admin_units.level') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'admin_units.level') THEN true ELSE raise_exception('no-loss assertion failed for admin_units.level') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM admin_units WHERE province_code IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'admin_units.province_code') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'admin_units.province_code') THEN true ELSE raise_exception('no-loss assertion failed for admin_units.province_code') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM admin_units WHERE sort_order IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'admin_units.sort_order') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'admin_units.sort_order') THEN true ELSE raise_exception('no-loss assertion failed for admin_units.sort_order') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM admin_units WHERE status IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'admin_units.status') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'admin_units.status') THEN true ELSE raise_exception('no-loss assertion failed for admin_units.status') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU75 | WO002-R06-source-archive-audit_logs | audit_logs.action; audit_logs.actor_role; audit_logs.details; audit_logs.entity_type | source_payload_archive.payload_uri | audit_logs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM audit_logs WHERE action IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'audit_logs.action') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'audit_logs.action') THEN true ELSE raise_exception('no-loss assertion failed for audit_logs.action') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM audit_logs WHERE actor_role IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'audit_logs.actor_role') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'audit_logs.actor_role') THEN true ELSE raise_exception('no-loss assertion failed for audit_logs.actor_role') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM audit_logs WHERE details IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'audit_logs.details') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'audit_logs.details') THEN true ELSE raise_exception('no-loss assertion failed for audit_logs.details') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM audit_logs WHERE entity_type IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'audit_logs.entity_type') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'audit_logs.entity_type') THEN true ELSE raise_exception('no-loss assertion failed for audit_logs.entity_type') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU76 | WO002-R06-source-archive-auth_tokens | auth_tokens.token | source_payload_archive.payload_uri | auth_tokens.user_id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM auth_tokens WHERE token IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'auth_tokens.token') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'auth_tokens.token') THEN true ELSE raise_exception('no-loss assertion failed for auth_tokens.token') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU77 | WO002-R06-source-archive-buildings | buildings.is_archived; buildings.spatial_evidence; buildings.status; buildings.usage | source_payload_archive.payload_uri | buildings.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM buildings WHERE is_archived IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'buildings.is_archived') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'buildings.is_archived') THEN true ELSE raise_exception('no-loss assertion failed for buildings.is_archived') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM buildings WHERE spatial_evidence IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'buildings.spatial_evidence') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'buildings.spatial_evidence') THEN true ELSE raise_exception('no-loss assertion failed for buildings.spatial_evidence') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM buildings WHERE status IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'buildings.status') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'buildings.status') THEN true ELSE raise_exception('no-loss assertion failed for buildings.status') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM buildings WHERE usage IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'buildings.usage') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'buildings.usage') THEN true ELSE raise_exception('no-loss assertion failed for buildings.usage') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU78 | WO002-R06-source-archive-citizen_geotag_submissions | citizen_geotag_submissions.capture_method; citizen_geotag_submissions.citizen_contact; citizen_geotag_submissions.dip_last4; citizen_geotag_submissions.duplicate_hint; citizen_geotag_submissions.field_note; citizen_geotag_submissions.field_status; citizen_geotag_submissions.grid_code; citizen_geotag_submissions.identity_document_verified; citizen_geotag_submissions.identity_verification_status; citizen_geotag_submissions.landmark; citizen_geotag_submissions.reviewer_note; citizen_geotag_submissions.road_suggestion_attribution; citizen_geotag_submissions.road_suggestion_source; citizen_geotag_submissions.road_suggestion_status; citizen_geotag_submissions.signage_batch; citizen_geotag_submissions.status; citizen_geotag_submissions.suggested_local_area | source_payload_archive.payload_uri | citizen_geotag_submissions.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM citizen_geotag_submissions WHERE capture_method IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'citizen_geotag_submissions.capture_method') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'citizen_geotag_submissions.capture_method') THEN true ELSE raise_exception('no-loss assertion failed for citizen_geotag_submissions.capture_method') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM citizen_geotag_submissions WHERE citizen_contact IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'citizen_geotag_submissions.citizen_contact') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'citizen_geotag_submissions.citizen_contact') THEN true ELSE raise_exception('no-loss assertion failed for citizen_geotag_submissions.citizen_contact') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM citizen_geotag_submissions WHERE dip_last4 IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'citizen_geotag_submissions.dip_last4') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'citizen_geotag_submissions.dip_last4') THEN true ELSE raise_exception('no-loss assertion failed for citizen_geotag_submissions.dip_last4') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM citizen_geotag_submissions WHERE duplicate_hint IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'citizen_geotag_submissions.duplicate_hint') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'citizen_geotag_submissions.duplicate_hint') THEN true ELSE raise_exception('no-loss assertion failed for citizen_geotag_submissions.duplicate_hint') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM citizen_geotag_submissions WHERE field_note IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'citizen_geotag_submissions.field_note') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'citizen_geotag_submissions.field_note') THEN true ELSE raise_exception('no-loss assertion failed for citizen_geotag_submissions.field_note') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM citizen_geotag_submissions WHERE field_status IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'citizen_geotag_submissions.field_status') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'citizen_geotag_submissions.field_status') THEN true ELSE raise_exception('no-loss assertion failed for citizen_geotag_submissions.field_status') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU79 | WO002-R06-source-archive-development_fixture_batches | development_fixture_batches.environment; development_fixture_batches.execution_context; development_fixture_batches.fixture_version | source_payload_archive.payload_uri | development_fixture_batches.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM development_fixture_batches WHERE environment IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'development_fixture_batches.environment') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'development_fixture_batches.environment') THEN true ELSE raise_exception('no-loss assertion failed for development_fixture_batches.environment') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM development_fixture_batches WHERE execution_context IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'development_fixture_batches.execution_context') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'development_fixture_batches.execution_context') THEN true ELSE raise_exception('no-loss assertion failed for development_fixture_batches.execution_context') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM development_fixture_batches WHERE fixture_version IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'development_fixture_batches.fixture_version') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'development_fixture_batches.fixture_version') THEN true ELSE raise_exception('no-loss assertion failed for development_fixture_batches.fixture_version') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU80 | WO002-R06-source-archive-field_assignments | field_assignments.priority; field_assignments.task; field_assignments.team; field_assignments.territory | source_payload_archive.payload_uri | field_assignments.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM field_assignments WHERE priority IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'field_assignments.priority') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'field_assignments.priority') THEN true ELSE raise_exception('no-loss assertion failed for field_assignments.priority') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM field_assignments WHERE task IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'field_assignments.task') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'field_assignments.task') THEN true ELSE raise_exception('no-loss assertion failed for field_assignments.task') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM field_assignments WHERE team IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'field_assignments.team') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'field_assignments.team') THEN true ELSE raise_exception('no-loss assertion failed for field_assignments.team') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM field_assignments WHERE territory IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'field_assignments.territory') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'field_assignments.territory') THEN true ELSE raise_exception('no-loss assertion failed for field_assignments.territory') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU81 | WO002-R06-source-archive-field_submissions | field_submissions.candidate_status; field_submissions.notes; field_submissions.review_status; field_submissions.reviewer_note; field_submissions.spatial_evidence; field_submissions.submission_type | source_payload_archive.payload_uri | field_submissions.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM field_submissions WHERE candidate_status IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'field_submissions.candidate_status') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'field_submissions.candidate_status') THEN true ELSE raise_exception('no-loss assertion failed for field_submissions.candidate_status') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM field_submissions WHERE notes IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'field_submissions.notes') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'field_submissions.notes') THEN true ELSE raise_exception('no-loss assertion failed for field_submissions.notes') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM field_submissions WHERE review_status IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'field_submissions.review_status') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'field_submissions.review_status') THEN true ELSE raise_exception('no-loss assertion failed for field_submissions.review_status') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM field_submissions WHERE reviewer_note IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'field_submissions.reviewer_note') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'field_submissions.reviewer_note') THEN true ELSE raise_exception('no-loss assertion failed for field_submissions.reviewer_note') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM field_submissions WHERE spatial_evidence IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'field_submissions.spatial_evidence') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'field_submissions.spatial_evidence') THEN true ELSE raise_exception('no-loss assertion failed for field_submissions.spatial_evidence') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM field_submissions WHERE submission_type IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'field_submissions.submission_type') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'field_submissions.submission_type') THEN true ELSE raise_exception('no-loss assertion failed for field_submissions.submission_type') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU82 | WO002-R06-source-archive-import_jobs | import_jobs.imported_count; import_jobs.status | source_payload_archive.payload_uri | import_jobs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM import_jobs WHERE imported_count IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'import_jobs.imported_count') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'import_jobs.imported_count') THEN true ELSE raise_exception('no-loss assertion failed for import_jobs.imported_count') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM import_jobs WHERE status IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'import_jobs.status') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'import_jobs.status') THEN true ELSE raise_exception('no-loss assertion failed for import_jobs.status') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU83 | WO002-R06-source-archive-import_rows | import_rows.candidate_status; import_rows.notes; import_rows.row_number; import_rows.submission_type; import_rows.validation_message; import_rows.validation_status | source_payload_archive.payload_uri | import_rows.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM import_rows WHERE candidate_status IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'import_rows.candidate_status') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'import_rows.candidate_status') THEN true ELSE raise_exception('no-loss assertion failed for import_rows.candidate_status') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM import_rows WHERE notes IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'import_rows.notes') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'import_rows.notes') THEN true ELSE raise_exception('no-loss assertion failed for import_rows.notes') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM import_rows WHERE row_number IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'import_rows.row_number') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'import_rows.row_number') THEN true ELSE raise_exception('no-loss assertion failed for import_rows.row_number') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM import_rows WHERE submission_type IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'import_rows.submission_type') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'import_rows.submission_type') THEN true ELSE raise_exception('no-loss assertion failed for import_rows.submission_type') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM import_rows WHERE validation_message IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'import_rows.validation_message') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'import_rows.validation_message') THEN true ELSE raise_exception('no-loss assertion failed for import_rows.validation_message') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM import_rows WHERE validation_status IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'import_rows.validation_status') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'import_rows.validation_status') THEN true ELSE raise_exception('no-loss assertion failed for import_rows.validation_status') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU84 | WO002-R06-source-archive-publication_packs | publication_packs.audience; publication_packs.status | source_payload_archive.payload_uri | publication_packs.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM publication_packs WHERE audience IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'publication_packs.audience') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'publication_packs.audience') THEN true ELSE raise_exception('no-loss assertion failed for publication_packs.audience') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM publication_packs WHERE status IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'publication_packs.status') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'publication_packs.status') THEN true ELSE raise_exception('no-loss assertion failed for publication_packs.status') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU85 | WO002-R06-source-archive-reference_data_load_history | reference_data_load_history.authority_status; reference_data_load_history.execution_context; reference_data_load_history.package_checksum; reference_data_load_history.package_version; reference_data_load_history.source | source_payload_archive.payload_uri | reference_data_load_history.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM reference_data_load_history WHERE authority_status IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'reference_data_load_history.authority_status') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'reference_data_load_history.authority_status') THEN true ELSE raise_exception('no-loss assertion failed for reference_data_load_history.authority_status') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM reference_data_load_history WHERE execution_context IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'reference_data_load_history.execution_context') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'reference_data_load_history.execution_context') THEN true ELSE raise_exception('no-loss assertion failed for reference_data_load_history.execution_context') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM reference_data_load_history WHERE package_checksum IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'reference_data_load_history.package_checksum') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'reference_data_load_history.package_checksum') THEN true ELSE raise_exception('no-loss assertion failed for reference_data_load_history.package_checksum') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM reference_data_load_history WHERE package_version IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'reference_data_load_history.package_version') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'reference_data_load_history.package_version') THEN true ELSE raise_exception('no-loss assertion failed for reference_data_load_history.package_version') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM reference_data_load_history WHERE source IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'reference_data_load_history.source') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'reference_data_load_history.source') THEN true ELSE raise_exception('no-loss assertion failed for reference_data_load_history.source') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU86 | WO002-R06-source-archive-reference_data_loads | reference_data_loads.authority_status; reference_data_loads.execution_context; reference_data_loads.package_checksum; reference_data_loads.package_version; reference_data_loads.source | source_payload_archive.payload_uri | reference_data_loads.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM reference_data_loads WHERE authority_status IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'reference_data_loads.authority_status') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'reference_data_loads.authority_status') THEN true ELSE raise_exception('no-loss assertion failed for reference_data_loads.authority_status') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM reference_data_loads WHERE execution_context IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'reference_data_loads.execution_context') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'reference_data_loads.execution_context') THEN true ELSE raise_exception('no-loss assertion failed for reference_data_loads.execution_context') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM reference_data_loads WHERE package_checksum IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'reference_data_loads.package_checksum') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'reference_data_loads.package_checksum') THEN true ELSE raise_exception('no-loss assertion failed for reference_data_loads.package_checksum') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM reference_data_loads WHERE package_version IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'reference_data_loads.package_version') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'reference_data_loads.package_version') THEN true ELSE raise_exception('no-loss assertion failed for reference_data_loads.package_version') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM reference_data_loads WHERE source IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'reference_data_loads.source') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'reference_data_loads.source') THEN true ELSE raise_exception('no-loss assertion failed for reference_data_loads.source') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU87 | WO002-R06-source-archive-roads | roads.is_archived; roads.length_km; roads.spatial_evidence; roads.status | source_payload_archive.payload_uri | roads.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM roads WHERE is_archived IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'roads.is_archived') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'roads.is_archived') THEN true ELSE raise_exception('no-loss assertion failed for roads.is_archived') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM roads WHERE length_km IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'roads.length_km') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'roads.length_km') THEN true ELSE raise_exception('no-loss assertion failed for roads.length_km') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM roads WHERE spatial_evidence IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'roads.spatial_evidence') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'roads.spatial_evidence') THEN true ELSE raise_exception('no-loss assertion failed for roads.spatial_evidence') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM roads WHERE status IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'roads.status') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'roads.status') THEN true ELSE raise_exception('no-loss assertion failed for roads.status') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU88 | WO002-R06-source-archive-schema_migrations | schema_migrations.applied_at; schema_migrations.checksum; schema_migrations.execution_context; schema_migrations.filename; schema_migrations.version | source_payload_archive.payload_uri | schema_migrations.<stable-row-hash> | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM schema_migrations WHERE applied_at IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'schema_migrations.applied_at') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'schema_migrations.applied_at') THEN true ELSE raise_exception('no-loss assertion failed for schema_migrations.applied_at') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM schema_migrations WHERE checksum IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'schema_migrations.checksum') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'schema_migrations.checksum') THEN true ELSE raise_exception('no-loss assertion failed for schema_migrations.checksum') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM schema_migrations WHERE execution_context IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'schema_migrations.execution_context') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'schema_migrations.execution_context') THEN true ELSE raise_exception('no-loss assertion failed for schema_migrations.execution_context') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM schema_migrations WHERE filename IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'schema_migrations.filename') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'schema_migrations.filename') THEN true ELSE raise_exception('no-loss assertion failed for schema_migrations.filename') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM schema_migrations WHERE version IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'schema_migrations.version') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'schema_migrations.version') THEN true ELSE raise_exception('no-loss assertion failed for schema_migrations.version') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU89 | WO002-R06-source-archive-territories | territories.is_archived; territories.province_code; territories.readiness; territories.type | source_payload_archive.payload_uri | territories.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM territories WHERE is_archived IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'territories.is_archived') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'territories.is_archived') THEN true ELSE raise_exception('no-loss assertion failed for territories.is_archived') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM territories WHERE province_code IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'territories.province_code') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'territories.province_code') THEN true ELSE raise_exception('no-loss assertion failed for territories.province_code') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM territories WHERE readiness IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'territories.readiness') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'territories.readiness') THEN true ELSE raise_exception('no-loss assertion failed for territories.readiness') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM territories WHERE type IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'territories.type') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'territories.type') THEN true ELSE raise_exception('no-loss assertion failed for territories.type') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |
| MU90 | WO002-R06-source-archive-users | users.is_active; users.password_hash; users.role | source_payload_archive.payload_uri | users.id | legacy_crosswalk(source_table, legacy_id, target_entity, target_id) + source_payload_archive(field_path) + owned migration_exception | WO-002: read-only design; WO-002B future dual-read, then dual-write only after SDA authorization | idempotent by transform_group_id + source_row_key + target field; reruns update only matching target draft rows | source typed value wins when authoritative; controlled map wins for lifecycle/status; archive preserves restricted/raw original; unowned conflicts abort | SELECT CASE WHEN (SELECT COUNT(*) FROM users WHERE is_active IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'users.is_active') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'users.is_active') THEN true ELSE raise_exception('no-loss assertion failed for users.is_active') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM users WHERE password_hash IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'users.password_hash') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'users.password_hash') THEN true ELSE raise_exception('no-loss assertion failed for users.password_hash') END AS ok \| SELECT CASE WHEN (SELECT COUNT(*) FROM users WHERE role IS NOT NULL) = (SELECT COUNT(*) FROM proposed_source_payload_archive WHERE payload_uri IS NOT NULL) + (SELECT COUNT(*) FROM source_payload_archive WHERE field_path = 'users.role') + (SELECT COUNT(*) FROM owned_migration_exception WHERE source_field = 'users.role') THEN true ELSE raise_exception('no-loss assertion failed for users.role') END AS ok | backup before migration unit; forward recovery through re-running idempotent group; abort on any unowned exception or no-loss mismatch | observe exception count, row parity, target projection parity and publication lock for one release window | cut only when zero unowned exceptions, dual-read parity passes, and SDA accepts Review 06+ implementation plan |

## Workload, storage, retention, concurrency and query assumptions

| Area | Source | Baseline date | Confidence range/horizon | Workload/capacity model | Approving owner |
|---|---|---|---|---|---|
| Location records | Current pilot extrapolation + EG province rollout planning | 2026-07-14 | low 300k / base 1.2M / high 2.5M records over five years | public lookup 50 QPS base / 500 surge; operator writes batch-windowed | Registry Authority approval pending |
| Geometry observations | Field capture model: 2-4 observations per canonical subject plus correction history | 2026-07-14 | low 600k / base 3M / high 8M observations | PostGIS GiST by subject/role/current plus province/year partition candidate above 10M | GIS/Data Authority approval pending |
| Evidence/archive | Restricted evidence retention assumption; media-heavy workflows separated from registry rows | 2026-07-14 | low 2TB / base 8TB / high 20TB object storage | encrypted object store; DB stores hash/URI/classification only | Publication/Records Authority approval pending |
