# Expand–Migrate–Contract Convergence and Scale Plan

Built from the corrected transformation registry. NLI-WO-002B remains unauthorized; this is implementation authority detail for later approval.

| Batch | Target owner | Current fields | Crosswalk/exception schemas | Read/write behavior | Idempotency key | Validation/tolerance | Recovery point | Owner | Compatible app versions | Cutover gate |
|---|---|---|---|---|---|---|---|---|---|---|
| B01 | administrative_code_history | admin_units.code; admin_units.province_code; provinces.code; territories.province_code | legacy_crosswalk + source_payload_archive + migration_exception | dual-read legacy+canonical; dual-write only after WO-002B authorization | legacy table/pk/field + target id | zero missing required rows; archive count equals restricted source count; geometry validity 100%; tolerances documented per batch | backup before batch; forward-only correction batch; abort on exception threshold | Registry Authority | app NLI-WO-002B adapter through NLI-WO-002B+2 | cut when exception queue <0.1% and public projections match release snapshots |
| B02 | decision_event | address_corrections.created_at; address_corrections.updated_at; address_points.created_at; address_points.updated_at; address_record_events.created_at; address_records.created_at; address_records.updated_at; addresses.created_at; addresses.updated_at; admin_units.created_at; admin_units.updated_at; buildings.created_at | legacy_crosswalk + source_payload_archive + migration_exception | dual-read legacy+canonical; dual-write only after WO-002B authorization | legacy table/pk/field + target id | zero missing required rows; archive count equals restricted source count; geometry validity 100%; tolerances documented per batch | backup before batch; forward-only correction batch; abort on exception threshold | Registry Authority | app NLI-WO-002B adapter through NLI-WO-002B+2 | cut when exception queue <0.1% and public projections match release snapshots |
| B03 | geometry_observation | address_points.accuracy_meters; address_points.latitude; address_points.longitude; address_records.accuracy_meters; address_records.latitude; address_records.longitude; buildings.spatial_evidence; citizen_geotag_submissions.accuracy_meters; citizen_geotag_submissions.latitude; citizen_geotag_submissions.longitude; field_submissions.spatial_evidence; roads.spatial_evidence | legacy_crosswalk + source_payload_archive + migration_exception | dual-read legacy+canonical; dual-write only after WO-002B authorization | legacy table/pk/field + target id | zero missing required rows; archive count equals restricted source count; geometry validity 100%; tolerances documented per batch | backup before batch; forward-only correction batch; abort on exception threshold | Registry Authority | app NLI-WO-002B adapter through NLI-WO-002B+2 | cut when exception queue <0.1% and public projections match release snapshots |
| B04 | geometry_version | address_records.geom | legacy_crosswalk + source_payload_archive + migration_exception | dual-read legacy+canonical; dual-write only after WO-002B authorization | legacy table/pk/field + target id | zero missing required rows; archive count equals restricted source count; geometry validity 100%; tolerances documented per batch | backup before batch; forward-only correction batch; abort on exception threshold | Registry Authority | app NLI-WO-002B adapter through NLI-WO-002B+2 | cut when exception queue <0.1% and public projections match release snapshots |
| B05 | legacy_crosswalk | address_corrections.address_id; address_corrections.id; address_points.address_id; address_points.id; address_record_events.actor_id; address_record_events.address_record_id; address_record_events.id; address_records.id; address_records.territory_id; addresses.building_id; addresses.id; addresses.road_id | legacy_crosswalk + source_payload_archive + migration_exception | dual-read legacy+canonical; dual-write only after WO-002B authorization | legacy table/pk/field + target id | zero missing required rows; archive count equals restricted source count; geometry validity 100%; tolerances documented per batch | backup before batch; forward-only correction batch; abort on exception threshold | Registry Authority | app NLI-WO-002B adapter through NLI-WO-002B+2 | cut when exception queue <0.1% and public projections match release snapshots |
| B06 | location_record_version | address_records.is_archived | legacy_crosswalk + source_payload_archive + migration_exception | dual-read legacy+canonical; dual-write only after WO-002B authorization | legacy table/pk/field + target id | zero missing required rows; archive count equals restricted source count; geometry validity 100%; tolerances documented per batch | backup before batch; forward-only correction batch; abort on exception threshold | Registry Authority | app NLI-WO-002B adapter through NLI-WO-002B+2 | cut when exception queue <0.1% and public projections match release snapshots |
| B07 | migration_exception | address_corrections.note; address_corrections.reporter_name; address_corrections.reviewer_note; address_corrections.status; address_record_events.actor_username; address_record_events.details; address_records.address_label; address_records.publication_state; address_records.source_submission_id; address_records.status; addresses.formatted; addresses.publication_state | legacy_crosswalk + source_payload_archive + migration_exception | dual-read legacy+canonical; dual-write only after WO-002B authorization | legacy table/pk/field + target id | zero missing required rows; archive count equals restricted source count; geometry validity 100%; tolerances documented per batch | backup before batch; forward-only correction batch; abort on exception threshold | Registry Authority | app NLI-WO-002B adapter through NLI-WO-002B+2 | cut when exception queue <0.1% and public projections match release snapshots |
| B08 | public_code_alias | address_corrections.public_code; address_records.address_code; address_records.province_code; addresses.province_code; addresses.public_code; citizen_geotag_submissions.grid_code | legacy_crosswalk + source_payload_archive + migration_exception | dual-read legacy+canonical; dual-write only after WO-002B authorization | legacy table/pk/field + target id | zero missing required rows; archive count equals restricted source count; geometry validity 100%; tolerances documented per batch | backup before batch; forward-only correction batch; abort on exception threshold | Registry Authority | app NLI-WO-002B adapter through NLI-WO-002B+2 | cut when exception queue <0.1% and public projections match release snapshots |
| B09 | source_payload_archive | address_corrections.correction_type; address_corrections.query; address_corrections.reason; address_corrections.reporter_contact; address_points.is_active; address_points.source_method; address_record_events.actor_role; address_record_events.event_type; address_records.record_bundle; address_records.search_text; addresses.is_archived; addresses.issuance_method | legacy_crosswalk + source_payload_archive + migration_exception | dual-read legacy+canonical; dual-write only after WO-002B authorization | legacy table/pk/field + target id | zero missing required rows; archive count equals restricted source count; geometry validity 100%; tolerances documented per batch | backup before batch; forward-only correction batch; abort on exception threshold | Registry Authority | app NLI-WO-002B adapter through NLI-WO-002B+2 | cut when exception queue <0.1% and public projections match release snapshots |
| B10 | source_record | schema_migrations.applied_at; schema_migrations.checksum; schema_migrations.version | legacy_crosswalk + source_payload_archive + migration_exception | dual-read legacy+canonical; dual-write only after WO-002B authorization | legacy table/pk/field + target id | zero missing required rows; archive count equals restricted source count; geometry validity 100%; tolerances documented per batch | backup before batch; forward-only correction batch; abort on exception threshold | Registry Authority | app NLI-WO-002B adapter through NLI-WO-002B+2 | cut when exception queue <0.1% and public projections match release snapshots |

## Workload, storage, retention, concurrency and query assumptions

| Area | Basis/horizon | Workload/concurrency | Storage/index | Retention/partition |
|---|---|---|---|---|
| Location records | 1,200,000 national five-year planning horizon | public lookup QPS 50 baseline / 500 surge | 1.5KB canonical + 4KB evidence average | retain release snapshots permanently; restricted raw per legal retention |
| Geometry observations | 3,000,000 observations | operator/GIS validation and proximity search | PostGIS GiST + subject/role current indexes | partition candidate by province/effective year above 10M rows |
| Evidence/archive | 2-8 TB depending media capture | restricted object storage, hash manifest in DB | object lifecycle + legal hold | encrypt; retain by classification |
