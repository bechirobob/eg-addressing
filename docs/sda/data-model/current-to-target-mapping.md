# Current-to-Target Transformation Registry

Review-owned typed transformation registry for every current pg_catalog field. Unknown current fields fail CI; the pipeline does not manufacture default mappings. Each row states source semantics, source keys, exact targets/crosswalks, value preservation, controlled translation, authority, exception handling, validation SQL and no-loss proof.

| Current field | Source keys | Source value semantics | Target field | Target ID/crosswalk | Value preservation | Controlled-value translation | Archive object | Authority prerequisite | Classification | Exception owner/handling | Validation SQL | No-loss proof | Compatibility | Retirement | Review decision |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| address_corrections.address_id | ["address_corrections.address_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_corrections WHERE address_id IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_corrections WHERE address_id IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-corrections-address-id |
| address_corrections.correction_type | ["address_corrections.correction_type"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_corrections; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM address_corrections; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-corrections-correction-type |
| address_corrections.created_at | ["address_corrections.created_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_corrections WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_corrections WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-corrections-created-at |
| address_corrections.id | ["address_corrections.id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_corrections WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_corrections WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-corrections-id |
| address_corrections.note | ["address_corrections.note"] | target-review-required for address_corrections.note | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_corrections WHERE note IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_corrections WHERE note IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-corrections-note |
| address_corrections.public_code | ["address_corrections.public_code"] | copy into public alias only after release-authority approval | public_code_alias.public_code | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_corrections WHERE public_code IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_corrections WHERE public_code IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-corrections-public-code |
| address_corrections.query | ["address_corrections.query"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_corrections; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM address_corrections; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-corrections-query |
| address_corrections.reason | ["address_corrections.reason"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_corrections; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM address_corrections; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-corrections-reason |
| address_corrections.reporter_contact | ["address_corrections.reporter_contact"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_corrections; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM address_corrections; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-corrections-reporter-contact |
| address_corrections.reporter_name | ["address_corrections.reporter_name"] | target-review-required for address_corrections.reporter_name | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_corrections WHERE reporter_name IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_corrections WHERE reporter_name IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-corrections-reporter-name |
| address_corrections.reviewer_note | ["address_corrections.reviewer_note"] | target-review-required for address_corrections.reviewer_note | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_corrections WHERE reviewer_note IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_corrections WHERE reviewer_note IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-corrections-reviewer-note |
| address_corrections.status | ["address_corrections.status"] | translate via registry; unknown values create exception | migration_exception.exception_type | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | translate through controlled vocabulary registry; unknown value -> migration_exception, never free-text canonical state | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_corrections WHERE status IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_corrections WHERE status IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-corrections-status |
| address_corrections.updated_at | ["address_corrections.updated_at"] | copy as recorded update event; not resolved_at unless resolved status | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_corrections WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_corrections WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-corrections-updated-at |
| address_points.accuracy_meters | ["address_points.accuracy_meters"] | combine coordinate/accuracy fields into geometry observation | geometry_observation.observed_geom | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_points WHERE accuracy_meters IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows | SELECT COUNT(*) FROM address_points WHERE accuracy_meters IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-points-accuracy-meters |
| address_points.address_id | ["address_points.address_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_points WHERE address_id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_points WHERE address_id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-points-address-id |
| address_points.created_at | ["address_points.created_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_points WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_points WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-points-created-at |
| address_points.id | ["address_points.id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_points WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_points WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-points-id |
| address_points.is_active | ["address_points.is_active"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_points; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM address_points; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-points-is-active |
| address_points.latitude | ["address_points.latitude"] | combine coordinate/accuracy fields into geometry observation | geometry_observation.observed_geom | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_points WHERE latitude IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows | SELECT COUNT(*) FROM address_points WHERE latitude IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-points-latitude |
| address_points.longitude | ["address_points.longitude"] | combine coordinate/accuracy fields into geometry observation | geometry_observation.observed_geom | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_points WHERE longitude IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows | SELECT COUNT(*) FROM address_points WHERE longitude IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-points-longitude |
| address_points.source_method | ["address_points.source_method"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_points; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM address_points; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-points-source-method |
| address_points.updated_at | ["address_points.updated_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_points WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_points WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-points-updated-at |
| address_record_events.actor_id | ["address_record_events.actor_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_record_events WHERE actor_id IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_record_events WHERE actor_id IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-record-events-actor-id |
| address_record_events.actor_role | ["address_record_events.actor_role"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_record_events; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM address_record_events; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-record-events-actor-role |
| address_record_events.actor_username | ["address_record_events.actor_username"] | target-review-required for address_record_events.actor_username | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_record_events WHERE actor_username IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_record_events WHERE actor_username IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-record-events-actor-username |
| address_record_events.address_record_id | ["address_record_events.address_record_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_record_events WHERE address_record_id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_record_events WHERE address_record_id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-record-events-address-record-id |
| address_record_events.created_at | ["address_record_events.created_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_record_events WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_record_events WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-record-events-created-at |
| address_record_events.details | ["address_record_events.details"] | target-review-required for address_record_events.details | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_record_events WHERE details IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_record_events WHERE details IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-record-events-details |
| address_record_events.event_type | ["address_record_events.event_type"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_record_events; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM address_record_events; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-record-events-event-type |
| address_record_events.id | ["address_record_events.id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_record_events WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_record_events WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-record-events-id |
| address_records.accuracy_meters | ["address_records.accuracy_meters"] | combine coordinate/accuracy fields into geometry observation | geometry_observation.observed_geom | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_records WHERE accuracy_meters IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows | SELECT COUNT(*) FROM address_records WHERE accuracy_meters IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-records-accuracy-meters |
| address_records.address_code | ["address_records.address_code"] | copy only after release-authority check; otherwise exception | public_code_alias.public_code | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | public-after-release | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_records WHERE address_code IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_records WHERE address_code IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-records-address-code |
| address_records.address_label | ["address_records.address_label"] | target-review-required for address_records.address_label | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | public-after-release | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_records WHERE address_label IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_records WHERE address_label IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-records-address-label |
| address_records.created_at | ["address_records.created_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_records WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_records WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-records-created-at |
| address_records.geom | ["address_records.geom"] | preserve approved geometry if valid | geometry_version.geom | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_records WHERE geom IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_records WHERE geom IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-records-geom |
| address_records.id | ["address_records.id"] | preserve legacy id in crosswalk; generate new ULID location_record_id | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_records WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_records WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-records-id |
| address_records.is_archived | ["address_records.is_archived"] | false->active; true->retired with required decision exception if timestamp absent | location_record_version.lifecycle_state | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_records WHERE is_archived IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_records WHERE is_archived IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-records-is-archived |
| address_records.latitude | ["address_records.latitude"] | combine latitude+longitude into Point SRID 4326 | geometry_observation.observed_geom | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_records WHERE latitude IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows | SELECT COUNT(*) FROM address_records WHERE latitude IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-records-latitude |
| address_records.longitude | ["address_records.longitude"] | combine latitude+longitude into Point SRID 4326 | geometry_observation.observed_geom | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_records WHERE longitude IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows | SELECT COUNT(*) FROM address_records WHERE longitude IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-records-longitude |
| address_records.province_code | ["address_records.province_code"] | copy into public alias only after release-authority approval | public_code_alias.public_code | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_records WHERE province_code IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_records WHERE province_code IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-records-province-code |
| address_records.publication_state | ["address_records.publication_state"] | target-review-required for address_records.publication_state | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | translate through controlled vocabulary registry; unknown value -> migration_exception, never free-text canonical state | not required unless exception captures raw source payload | Publication Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_records WHERE publication_state IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_records WHERE publication_state IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-records-publication-state |
| address_records.record_bundle | ["address_records.record_bundle"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_records; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM address_records; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-records-record-bundle |
| address_records.search_text | ["address_records.search_text"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_records; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM address_records; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-records-search-text |
| address_records.source_submission_id | ["address_records.source_submission_id"] | target-review-required for address_records.source_submission_id | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_records WHERE source_submission_id IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_records WHERE source_submission_id IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-records-source-submission-id |
| address_records.status | ["address_records.status"] | translate via registry; unknown values create exception | migration_exception.exception_type | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | translate through controlled vocabulary registry; unknown value -> migration_exception, never free-text canonical state | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_records WHERE status IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_records WHERE status IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-records-status |
| address_records.territory_id | ["address_records.territory_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_records WHERE territory_id IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_records WHERE territory_id IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-records-territory-id |
| address_records.updated_at | ["address_records.updated_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM address_records WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM address_records WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-address-records-updated-at |
| addresses.building_id | ["addresses.building_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM addresses WHERE building_id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM addresses WHERE building_id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-addresses-building-id |
| addresses.created_at | ["addresses.created_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM addresses WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM addresses WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-addresses-created-at |
| addresses.formatted | ["addresses.formatted"] | target-review-required for addresses.formatted | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | public-after-release | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM addresses WHERE formatted IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM addresses WHERE formatted IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-addresses-formatted |
| addresses.id | ["addresses.id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM addresses WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM addresses WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-addresses-id |
| addresses.is_archived | ["addresses.is_archived"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM addresses; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM addresses; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-addresses-is-archived |
| addresses.issuance_method | ["addresses.issuance_method"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM addresses; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM addresses; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-addresses-issuance-method |
| addresses.province_code | ["addresses.province_code"] | copy into public alias only after release-authority approval | public_code_alias.public_code | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM addresses WHERE province_code IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM addresses WHERE province_code IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-addresses-province-code |
| addresses.public_code | ["addresses.public_code"] | copy into public alias only after release-authority approval | public_code_alias.public_code | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM addresses WHERE public_code IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM addresses WHERE public_code IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-addresses-public-code |
| addresses.publication_state | ["addresses.publication_state"] | translate via registry; unknown values create exception | migration_exception.exception_type | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | translate through controlled vocabulary registry; unknown value -> migration_exception, never free-text canonical state | not required unless exception captures raw source payload | Publication Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM addresses WHERE publication_state IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM addresses WHERE publication_state IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-addresses-publication-state |
| addresses.road_id | ["addresses.road_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM addresses WHERE road_id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM addresses WHERE road_id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-addresses-road-id |
| addresses.source | ["addresses.source"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM addresses; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM addresses; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-addresses-source |
| addresses.status | ["addresses.status"] | translate via registry; unknown values create exception | migration_exception.exception_type | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | translate through controlled vocabulary registry; unknown value -> migration_exception, never free-text canonical state | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM addresses WHERE status IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM addresses WHERE status IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-addresses-status |
| addresses.superseded_by_address_id | ["addresses.superseded_by_address_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM addresses WHERE superseded_by_address_id IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM addresses WHERE superseded_by_address_id IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-addresses-superseded-by-address-id |
| addresses.territory_id | ["addresses.territory_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM addresses WHERE territory_id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM addresses WHERE territory_id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-addresses-territory-id |
| addresses.updated_at | ["addresses.updated_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM addresses WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM addresses WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-addresses-updated-at |
| addresses.verification_status | ["addresses.verification_status"] | translate via registry; unknown values create exception | migration_exception.exception_type | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | translate through controlled vocabulary registry; unknown value -> migration_exception, never free-text canonical state | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM addresses WHERE verification_status IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM addresses WHERE verification_status IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-addresses-verification-status |
| admin_units.code | ["admin_units.code"] | copy into effective-dated administrative code history | administrative_code_history.official_code | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | GIS/Data Authority | public-after-release | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM admin_units WHERE code IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM admin_units WHERE code IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-admin-units-code |
| admin_units.created_at | ["admin_units.created_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | GIS/Data Authority | public-after-release | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM admin_units WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM admin_units WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-admin-units-created-at |
| admin_units.id | ["admin_units.id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | GIS/Data Authority | public-after-release | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM admin_units WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM admin_units WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-admin-units-id |
| admin_units.level | ["admin_units.level"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | GIS/Data Authority | public-after-release | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM admin_units; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM admin_units; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-admin-units-level |
| admin_units.name_en | ["admin_units.name_en"] | target-review-required for admin_units.name_en | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | GIS/Data Authority | public-after-release | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM admin_units WHERE name_en IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM admin_units WHERE name_en IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-admin-units-name-en |
| admin_units.name_es | ["admin_units.name_es"] | target-review-required for admin_units.name_es | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | GIS/Data Authority | public-after-release | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM admin_units WHERE name_es IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM admin_units WHERE name_es IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-admin-units-name-es |
| admin_units.parent_id | ["admin_units.parent_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | GIS/Data Authority | public-after-release | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM admin_units WHERE parent_id IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM admin_units WHERE parent_id IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-admin-units-parent-id |
| admin_units.province_code | ["admin_units.province_code"] | copy into effective-dated administrative code history | administrative_code_history.official_code | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | GIS/Data Authority | public-after-release | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM admin_units WHERE province_code IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM admin_units WHERE province_code IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-admin-units-province-code |
| admin_units.sort_order | ["admin_units.sort_order"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | GIS/Data Authority | public-after-release | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM admin_units; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM admin_units; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-admin-units-sort-order |
| admin_units.status | ["admin_units.status"] | translate via registry; unknown values create exception | migration_exception.exception_type | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | translate through controlled vocabulary registry; unknown value -> migration_exception, never free-text canonical state | not required unless exception captures raw source payload | GIS/Data Authority | public-after-release | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM admin_units WHERE status IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM admin_units WHERE status IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-admin-units-status |
| admin_units.updated_at | ["admin_units.updated_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | GIS/Data Authority | public-after-release | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM admin_units WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM admin_units WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-admin-units-updated-at |
| audit_logs.action | ["audit_logs.action"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM audit_logs; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM audit_logs; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-audit-logs-action |
| audit_logs.actor_role | ["audit_logs.actor_role"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM audit_logs; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM audit_logs; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-audit-logs-actor-role |
| audit_logs.actor_user_id | ["audit_logs.actor_user_id"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM audit_logs; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM audit_logs; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-audit-logs-actor-user-id |
| audit_logs.actor_username | ["audit_logs.actor_username"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM audit_logs; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM audit_logs; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-audit-logs-actor-username |
| audit_logs.created_at | ["audit_logs.created_at"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM audit_logs; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM audit_logs; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-audit-logs-created-at |
| audit_logs.details | ["audit_logs.details"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM audit_logs; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM audit_logs; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-audit-logs-details |
| audit_logs.entity_id | ["audit_logs.entity_id"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM audit_logs; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM audit_logs; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-audit-logs-entity-id |
| audit_logs.entity_type | ["audit_logs.entity_type"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM audit_logs; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM audit_logs; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-audit-logs-entity-type |
| audit_logs.id | ["audit_logs.id"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM audit_logs; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM audit_logs; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-audit-logs-id |
| auth_tokens.created_at | ["auth_tokens.created_at"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Security Authority | security-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM auth_tokens; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM auth_tokens; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-auth-tokens-created-at |
| auth_tokens.expires_at | ["auth_tokens.expires_at"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Security Authority | security-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM auth_tokens; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM auth_tokens; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-auth-tokens-expires-at |
| auth_tokens.last_seen_at | ["auth_tokens.last_seen_at"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Security Authority | security-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM auth_tokens; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM auth_tokens; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-auth-tokens-last-seen-at |
| auth_tokens.revoked_at | ["auth_tokens.revoked_at"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Security Authority | security-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM auth_tokens; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM auth_tokens; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-auth-tokens-revoked-at |
| auth_tokens.token | ["auth_tokens.token"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Security Authority | security-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM auth_tokens; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM auth_tokens; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-auth-tokens-token |
| auth_tokens.user_id | ["auth_tokens.user_id"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Security Authority | security-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM auth_tokens; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM auth_tokens; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-auth-tokens-user-id |
| buildings.created_at | ["buildings.created_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM buildings WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM buildings WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-buildings-created-at |
| buildings.id | ["buildings.id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM buildings WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM buildings WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-buildings-id |
| buildings.is_archived | ["buildings.is_archived"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM buildings; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM buildings; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-buildings-is-archived |
| buildings.label | ["buildings.label"] | target-review-required for buildings.label | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM buildings WHERE label IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM buildings WHERE label IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-buildings-label |
| buildings.road_id | ["buildings.road_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM buildings WHERE road_id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM buildings WHERE road_id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-buildings-road-id |
| buildings.spatial_evidence | ["buildings.spatial_evidence"] | validate SRID/type and preserve raw evidence | geometry_observation.observed_geom | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM buildings WHERE spatial_evidence IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows | SELECT COUNT(*) FROM buildings WHERE spatial_evidence IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-buildings-spatial-evidence |
| buildings.status | ["buildings.status"] | translate via registry; unknown values create exception | migration_exception.exception_type | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | translate through controlled vocabulary registry; unknown value -> migration_exception, never free-text canonical state | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM buildings WHERE status IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM buildings WHERE status IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-buildings-status |
| buildings.territory_id | ["buildings.territory_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM buildings WHERE territory_id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM buildings WHERE territory_id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-buildings-territory-id |
| buildings.updated_at | ["buildings.updated_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM buildings WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM buildings WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-buildings-updated-at |
| buildings.usage | ["buildings.usage"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM buildings; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM buildings; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-buildings-usage |
| citizen_geotag_submissions.accuracy_meters | ["citizen_geotag_submissions.accuracy_meters"] | combine coordinate/accuracy fields into geometry observation | geometry_observation.observed_geom | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE accuracy_meters IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE accuracy_meters IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-accuracy-meters |
| citizen_geotag_submissions.address_label | ["citizen_geotag_submissions.address_label"] | target-review-required for citizen_geotag_submissions.address_label | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | public-after-release | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE address_label IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE address_label IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-address-label |
| citizen_geotag_submissions.capture_method | ["citizen_geotag_submissions.capture_method"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM citizen_geotag_submissions; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-capture-method |
| citizen_geotag_submissions.citizen_contact | ["citizen_geotag_submissions.citizen_contact"] | encrypt and archive raw; expose masked metadata only | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry + Field Operations Authority | highly-restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM citizen_geotag_submissions; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-citizen-contact |
| citizen_geotag_submissions.citizen_name | ["citizen_geotag_submissions.citizen_name"] | encrypt and archive raw; expose masked metadata only | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry + Field Operations Authority | highly-restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM citizen_geotag_submissions; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-citizen-name |
| citizen_geotag_submissions.created_at | ["citizen_geotag_submissions.created_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-created-at |
| citizen_geotag_submissions.dip_last4 | ["citizen_geotag_submissions.dip_last4"] | archive restricted identity fragment with access policy | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry + Field Operations Authority | highly-restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM citizen_geotag_submissions; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-dip-last4 |
| citizen_geotag_submissions.duplicate_hint | ["citizen_geotag_submissions.duplicate_hint"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM citizen_geotag_submissions; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-duplicate-hint |
| citizen_geotag_submissions.field_note | ["citizen_geotag_submissions.field_note"] | target-review-required for citizen_geotag_submissions.field_note | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE field_note IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE field_note IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-field-note |
| citizen_geotag_submissions.field_status | ["citizen_geotag_submissions.field_status"] | translate via registry; unknown values create exception | migration_exception.exception_type | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | translate through controlled vocabulary registry; unknown value -> migration_exception, never free-text canonical state | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE field_status IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE field_status IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-field-status |
| citizen_geotag_submissions.field_submission_id | ["citizen_geotag_submissions.field_submission_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE field_submission_id IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE field_submission_id IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-field-submission-id |
| citizen_geotag_submissions.field_verified_at | ["citizen_geotag_submissions.field_verified_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE field_verified_at IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE field_verified_at IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-field-verified-at |
| citizen_geotag_submissions.grid_code | ["citizen_geotag_submissions.grid_code"] | copy into public alias only after release-authority approval | public_code_alias.public_code | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE grid_code IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE grid_code IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-grid-code |
| citizen_geotag_submissions.id | ["citizen_geotag_submissions.id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-id |
| citizen_geotag_submissions.identity_document_verified | ["citizen_geotag_submissions.identity_document_verified"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry + Field Operations Authority | highly-restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM citizen_geotag_submissions; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-identity-document-verified |
| citizen_geotag_submissions.identity_verification_status | ["citizen_geotag_submissions.identity_verification_status"] | translate via registry; unknown values create exception | migration_exception.exception_type | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | translate through controlled vocabulary registry; unknown value -> migration_exception, never free-text canonical state | not required unless exception captures raw source payload | Registry + Field Operations Authority | highly-restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE identity_verification_status IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE identity_verification_status IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-identity-verification-status |
| citizen_geotag_submissions.identity_verified_at | ["citizen_geotag_submissions.identity_verified_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | highly-restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE identity_verified_at IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE identity_verified_at IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-identity-verified-at |
| citizen_geotag_submissions.landmark | ["citizen_geotag_submissions.landmark"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM citizen_geotag_submissions; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-landmark |
| citizen_geotag_submissions.latitude | ["citizen_geotag_submissions.latitude"] | combine coordinate/accuracy fields into geometry observation | geometry_observation.observed_geom | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE latitude IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE latitude IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-latitude |
| citizen_geotag_submissions.longitude | ["citizen_geotag_submissions.longitude"] | combine coordinate/accuracy fields into geometry observation | geometry_observation.observed_geom | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE longitude IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE longitude IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-longitude |
| citizen_geotag_submissions.map_display_name | ["citizen_geotag_submissions.map_display_name"] | target-review-required for citizen_geotag_submissions.map_display_name | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE map_display_name IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE map_display_name IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-map-display-name |
| citizen_geotag_submissions.reviewed_road_name | ["citizen_geotag_submissions.reviewed_road_name"] | target-review-required for citizen_geotag_submissions.reviewed_road_name | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE reviewed_road_name IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE reviewed_road_name IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-reviewed-road-name |
| citizen_geotag_submissions.reviewer_note | ["citizen_geotag_submissions.reviewer_note"] | target-review-required for citizen_geotag_submissions.reviewer_note | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE reviewer_note IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE reviewer_note IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-reviewer-note |
| citizen_geotag_submissions.road_suggestion_attribution | ["citizen_geotag_submissions.road_suggestion_attribution"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM citizen_geotag_submissions; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-road-suggestion-attribution |
| citizen_geotag_submissions.road_suggestion_source | ["citizen_geotag_submissions.road_suggestion_source"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM citizen_geotag_submissions; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-road-suggestion-source |
| citizen_geotag_submissions.road_suggestion_status | ["citizen_geotag_submissions.road_suggestion_status"] | translate via registry; unknown values create exception | migration_exception.exception_type | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | translate through controlled vocabulary registry; unknown value -> migration_exception, never free-text canonical state | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE road_suggestion_status IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE road_suggestion_status IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-road-suggestion-status |
| citizen_geotag_submissions.signage_batch | ["citizen_geotag_submissions.signage_batch"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM citizen_geotag_submissions; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-signage-batch |
| citizen_geotag_submissions.status | ["citizen_geotag_submissions.status"] | translate via registry; unknown values create exception | migration_exception.exception_type | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | translate through controlled vocabulary registry; unknown value -> migration_exception, never free-text canonical state | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE status IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE status IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-status |
| citizen_geotag_submissions.suggested_local_area | ["citizen_geotag_submissions.suggested_local_area"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM citizen_geotag_submissions; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-suggested-local-area |
| citizen_geotag_submissions.suggested_place_name | ["citizen_geotag_submissions.suggested_place_name"] | target-review-required for citizen_geotag_submissions.suggested_place_name | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE suggested_place_name IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE suggested_place_name IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-suggested-place-name |
| citizen_geotag_submissions.suggested_road_name | ["citizen_geotag_submissions.suggested_road_name"] | target-review-required for citizen_geotag_submissions.suggested_road_name | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE suggested_road_name IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE suggested_road_name IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-suggested-road-name |
| citizen_geotag_submissions.territory_id | ["citizen_geotag_submissions.territory_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE territory_id IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE territory_id IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-territory-id |
| citizen_geotag_submissions.updated_at | ["citizen_geotag_submissions.updated_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM citizen_geotag_submissions WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-citizen-geotag-submissions-updated-at |
| development_fixture_batches.batch_id | ["development_fixture_batches.batch_id"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM development_fixture_batches; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM development_fixture_batches; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-development-fixture-batches-batch-id |
| development_fixture_batches.cleaned_at | ["development_fixture_batches.cleaned_at"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM development_fixture_batches; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM development_fixture_batches; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-development-fixture-batches-cleaned-at |
| development_fixture_batches.environment | ["development_fixture_batches.environment"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM development_fixture_batches; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM development_fixture_batches; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-development-fixture-batches-environment |
| development_fixture_batches.execution_context | ["development_fixture_batches.execution_context"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM development_fixture_batches; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM development_fixture_batches; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-development-fixture-batches-execution-context |
| development_fixture_batches.fixture_version | ["development_fixture_batches.fixture_version"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM development_fixture_batches; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM development_fixture_batches; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-development-fixture-batches-fixture-version |
| development_fixture_batches.loaded_at | ["development_fixture_batches.loaded_at"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM development_fixture_batches; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM development_fixture_batches; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-development-fixture-batches-loaded-at |
| development_fixture_records.batch_id | ["development_fixture_records.batch_id"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM development_fixture_records; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM development_fixture_records; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-development-fixture-records-batch-id |
| development_fixture_records.created_by_batch | ["development_fixture_records.created_by_batch"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM development_fixture_records; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM development_fixture_records; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-development-fixture-records-created-by-batch |
| development_fixture_records.record_id | ["development_fixture_records.record_id"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM development_fixture_records; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM development_fixture_records; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-development-fixture-records-record-id |
| development_fixture_records.recorded_at | ["development_fixture_records.recorded_at"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM development_fixture_records; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM development_fixture_records; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-development-fixture-records-recorded-at |
| development_fixture_records.table_name | ["development_fixture_records.table_name"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM development_fixture_records; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM development_fixture_records; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-development-fixture-records-table-name |
| field_assignments.assignment_id | ["field_assignments.assignment_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM field_assignments WHERE assignment_id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM field_assignments WHERE assignment_id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-field-assignments-assignment-id |
| field_assignments.created_at | ["field_assignments.created_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM field_assignments WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM field_assignments WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-field-assignments-created-at |
| field_assignments.priority | ["field_assignments.priority"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM field_assignments; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM field_assignments; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-field-assignments-priority |
| field_assignments.task | ["field_assignments.task"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM field_assignments; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM field_assignments; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-field-assignments-task |
| field_assignments.team | ["field_assignments.team"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM field_assignments; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM field_assignments; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-field-assignments-team |
| field_assignments.territory | ["field_assignments.territory"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM field_assignments; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM field_assignments; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-field-assignments-territory |
| field_assignments.territory_id | ["field_assignments.territory_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM field_assignments WHERE territory_id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM field_assignments WHERE territory_id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-field-assignments-territory-id |
| field_submissions.assignment_id | ["field_submissions.assignment_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM field_submissions WHERE assignment_id IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM field_submissions WHERE assignment_id IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-field-submissions-assignment-id |
| field_submissions.candidate_name | ["field_submissions.candidate_name"] | target-review-required for field_submissions.candidate_name | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM field_submissions WHERE candidate_name IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM field_submissions WHERE candidate_name IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-field-submissions-candidate-name |
| field_submissions.candidate_status | ["field_submissions.candidate_status"] | translate via registry; unknown values create exception | migration_exception.exception_type | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | translate through controlled vocabulary registry; unknown value -> migration_exception, never free-text canonical state | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM field_submissions WHERE candidate_status IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM field_submissions WHERE candidate_status IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-field-submissions-candidate-status |
| field_submissions.created_at | ["field_submissions.created_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM field_submissions WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM field_submissions WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-field-submissions-created-at |
| field_submissions.id | ["field_submissions.id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM field_submissions WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM field_submissions WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-field-submissions-id |
| field_submissions.notes | ["field_submissions.notes"] | target-review-required for field_submissions.notes | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM field_submissions WHERE notes IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM field_submissions WHERE notes IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-field-submissions-notes |
| field_submissions.registry_entity_id | ["field_submissions.registry_entity_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM field_submissions WHERE registry_entity_id IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM field_submissions WHERE registry_entity_id IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-field-submissions-registry-entity-id |
| field_submissions.review_status | ["field_submissions.review_status"] | translate via registry; unknown values create exception | migration_exception.exception_type | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | translate through controlled vocabulary registry; unknown value -> migration_exception, never free-text canonical state | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM field_submissions WHERE review_status IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM field_submissions WHERE review_status IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-field-submissions-review-status |
| field_submissions.reviewer_note | ["field_submissions.reviewer_note"] | target-review-required for field_submissions.reviewer_note | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM field_submissions WHERE reviewer_note IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM field_submissions WHERE reviewer_note IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-field-submissions-reviewer-note |
| field_submissions.spatial_evidence | ["field_submissions.spatial_evidence"] | validate SRID/type and preserve raw evidence | geometry_observation.observed_geom | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM field_submissions WHERE spatial_evidence IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows | SELECT COUNT(*) FROM field_submissions WHERE spatial_evidence IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-field-submissions-spatial-evidence |
| field_submissions.submission_type | ["field_submissions.submission_type"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM field_submissions; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM field_submissions; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-field-submissions-submission-type |
| field_submissions.submitted_by | ["field_submissions.submitted_by"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM field_submissions; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM field_submissions; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-field-submissions-submitted-by |
| field_submissions.territory_id | ["field_submissions.territory_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM field_submissions WHERE territory_id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM field_submissions WHERE territory_id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-field-submissions-territory-id |
| field_submissions.updated_at | ["field_submissions.updated_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry + Field Operations Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM field_submissions WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM field_submissions WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-field-submissions-updated-at |
| import_jobs.created_at | ["import_jobs.created_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM import_jobs WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM import_jobs WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-import-jobs-created-at |
| import_jobs.id | ["import_jobs.id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM import_jobs WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM import_jobs WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-import-jobs-id |
| import_jobs.imported_count | ["import_jobs.imported_count"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM import_jobs; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM import_jobs; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-import-jobs-imported-count |
| import_jobs.name | ["import_jobs.name"] | target-review-required for import_jobs.name | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM import_jobs WHERE name IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM import_jobs WHERE name IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-import-jobs-name |
| import_jobs.source_name | ["import_jobs.source_name"] | target-review-required for import_jobs.source_name | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM import_jobs WHERE source_name IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM import_jobs WHERE source_name IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-import-jobs-source-name |
| import_jobs.status | ["import_jobs.status"] | translate via registry; unknown values create exception | migration_exception.exception_type | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | translate through controlled vocabulary registry; unknown value -> migration_exception, never free-text canonical state | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM import_jobs WHERE status IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM import_jobs WHERE status IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-import-jobs-status |
| import_jobs.updated_at | ["import_jobs.updated_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM import_jobs WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM import_jobs WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-import-jobs-updated-at |
| import_rows.candidate_name | ["import_rows.candidate_name"] | target-review-required for import_rows.candidate_name | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM import_rows WHERE candidate_name IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM import_rows WHERE candidate_name IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-import-rows-candidate-name |
| import_rows.candidate_status | ["import_rows.candidate_status"] | translate via registry; unknown values create exception | migration_exception.exception_type | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | translate through controlled vocabulary registry; unknown value -> migration_exception, never free-text canonical state | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM import_rows WHERE candidate_status IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM import_rows WHERE candidate_status IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-import-rows-candidate-status |
| import_rows.committed_submission_id | ["import_rows.committed_submission_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM import_rows WHERE committed_submission_id IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM import_rows WHERE committed_submission_id IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-import-rows-committed-submission-id |
| import_rows.job_id | ["import_rows.job_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM import_rows WHERE job_id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM import_rows WHERE job_id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-import-rows-job-id |
| import_rows.notes | ["import_rows.notes"] | target-review-required for import_rows.notes | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM import_rows WHERE notes IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM import_rows WHERE notes IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-import-rows-notes |
| import_rows.row_number | ["import_rows.row_number"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM import_rows; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM import_rows; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-import-rows-row-number |
| import_rows.submission_type | ["import_rows.submission_type"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM import_rows; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM import_rows; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-import-rows-submission-type |
| import_rows.territory_id | ["import_rows.territory_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM import_rows WHERE territory_id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM import_rows WHERE territory_id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-import-rows-territory-id |
| import_rows.validation_message | ["import_rows.validation_message"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM import_rows; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM import_rows; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-import-rows-validation-message |
| import_rows.validation_status | ["import_rows.validation_status"] | translate via registry; unknown values create exception | migration_exception.exception_type | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | translate through controlled vocabulary registry; unknown value -> migration_exception, never free-text canonical state | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM import_rows WHERE validation_status IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM import_rows WHERE validation_status IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-import-rows-validation-status |
| provinces.code | ["provinces.code"] | copy into effective-dated administrative code history | administrative_code_history.official_code | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | GIS/Data Authority | public-after-release | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM provinces WHERE code IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM provinces WHERE code IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-provinces-code |
| provinces.created_at | ["provinces.created_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | GIS/Data Authority | public-after-release | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM provinces WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM provinces WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-provinces-created-at |
| provinces.name | ["provinces.name"] | target-review-required for provinces.name | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | GIS/Data Authority | public-after-release | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM provinces WHERE name IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM provinces WHERE name IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-provinces-name |
| publication_pack_addresses.address_id | ["publication_pack_addresses.address_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Publication Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM publication_pack_addresses WHERE address_id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM publication_pack_addresses WHERE address_id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-publication-pack-addresses-address-id |
| publication_pack_addresses.publication_pack_id | ["publication_pack_addresses.publication_pack_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Publication Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM publication_pack_addresses WHERE publication_pack_id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM publication_pack_addresses WHERE publication_pack_id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-publication-pack-addresses-publication-pack-id |
| publication_packs.audience | ["publication_packs.audience"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Publication Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM publication_packs; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM publication_packs; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-publication-packs-audience |
| publication_packs.created_at | ["publication_packs.created_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Publication Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM publication_packs WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM publication_packs WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-publication-packs-created-at |
| publication_packs.id | ["publication_packs.id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Publication Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM publication_packs WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM publication_packs WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-publication-packs-id |
| publication_packs.name | ["publication_packs.name"] | target-review-required for publication_packs.name | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Publication Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM publication_packs WHERE name IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM publication_packs WHERE name IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-publication-packs-name |
| publication_packs.status | ["publication_packs.status"] | translate via registry; unknown values create exception | migration_exception.exception_type | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | translate through controlled vocabulary registry; unknown value -> migration_exception, never free-text canonical state | not required unless exception captures raw source payload | Publication Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM publication_packs WHERE status IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM publication_packs WHERE status IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-publication-packs-status |
| publication_packs.updated_at | ["publication_packs.updated_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Publication Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM publication_packs WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM publication_packs WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-publication-packs-updated-at |
| reference_data_load_history.authority_status | ["reference_data_load_history.authority_status"] | translate via registry; unknown values create exception | migration_exception.exception_type | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | translate through controlled vocabulary registry; unknown value -> migration_exception, never free-text canonical state | not required unless exception captures raw source payload | Registry Authority | security-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM reference_data_load_history WHERE authority_status IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM reference_data_load_history WHERE authority_status IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-reference-data-load-history-authority-status |
| reference_data_load_history.execution_context | ["reference_data_load_history.execution_context"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM reference_data_load_history; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM reference_data_load_history; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-reference-data-load-history-execution-context |
| reference_data_load_history.id | ["reference_data_load_history.id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM reference_data_load_history WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM reference_data_load_history WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-reference-data-load-history-id |
| reference_data_load_history.loaded_at | ["reference_data_load_history.loaded_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM reference_data_load_history WHERE loaded_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM reference_data_load_history WHERE loaded_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-reference-data-load-history-loaded-at |
| reference_data_load_history.package_checksum | ["reference_data_load_history.package_checksum"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM reference_data_load_history; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM reference_data_load_history; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-reference-data-load-history-package-checksum |
| reference_data_load_history.package_id | ["reference_data_load_history.package_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM reference_data_load_history WHERE package_id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM reference_data_load_history WHERE package_id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-reference-data-load-history-package-id |
| reference_data_load_history.package_version | ["reference_data_load_history.package_version"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM reference_data_load_history; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM reference_data_load_history; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-reference-data-load-history-package-version |
| reference_data_load_history.source | ["reference_data_load_history.source"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM reference_data_load_history; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM reference_data_load_history; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-reference-data-load-history-source |
| reference_data_loads.authority_status | ["reference_data_loads.authority_status"] | translate via registry; unknown values create exception | migration_exception.exception_type | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | translate through controlled vocabulary registry; unknown value -> migration_exception, never free-text canonical state | not required unless exception captures raw source payload | Registry Authority | security-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM reference_data_loads WHERE authority_status IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM reference_data_loads WHERE authority_status IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-reference-data-loads-authority-status |
| reference_data_loads.execution_context | ["reference_data_loads.execution_context"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM reference_data_loads; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM reference_data_loads; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-reference-data-loads-execution-context |
| reference_data_loads.loaded_at | ["reference_data_loads.loaded_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM reference_data_loads WHERE loaded_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM reference_data_loads WHERE loaded_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-reference-data-loads-loaded-at |
| reference_data_loads.package_checksum | ["reference_data_loads.package_checksum"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM reference_data_loads; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM reference_data_loads; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-reference-data-loads-package-checksum |
| reference_data_loads.package_id | ["reference_data_loads.package_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM reference_data_loads WHERE package_id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM reference_data_loads WHERE package_id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-reference-data-loads-package-id |
| reference_data_loads.package_version | ["reference_data_loads.package_version"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM reference_data_loads; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM reference_data_loads; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-reference-data-loads-package-version |
| reference_data_loads.source | ["reference_data_loads.source"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM reference_data_loads; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM reference_data_loads; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-reference-data-loads-source |
| roads.created_at | ["roads.created_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM roads WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM roads WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-roads-created-at |
| roads.id | ["roads.id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM roads WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM roads WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-roads-id |
| roads.is_archived | ["roads.is_archived"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM roads; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM roads; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-roads-is-archived |
| roads.length_km | ["roads.length_km"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM roads; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM roads; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-roads-length-km |
| roads.name | ["roads.name"] | target-review-required for roads.name | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM roads WHERE name IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM roads WHERE name IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-roads-name |
| roads.spatial_evidence | ["roads.spatial_evidence"] | validate SRID/type and preserve raw evidence | geometry_observation.observed_geom | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | restricted | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM roads WHERE spatial_evidence IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows | SELECT COUNT(*) FROM roads WHERE spatial_evidence IS NOT NULL; -- compare to created valid SRID 4326 observations and exception rows ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-roads-spatial-evidence |
| roads.status | ["roads.status"] | translate via registry; unknown values create exception | migration_exception.exception_type | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | translate through controlled vocabulary registry; unknown value -> migration_exception, never free-text canonical state | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM roads WHERE status IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM roads WHERE status IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-roads-status |
| roads.territory_id | ["roads.territory_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM roads WHERE territory_id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM roads WHERE territory_id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-roads-territory-id |
| roads.updated_at | ["roads.updated_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM roads WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM roads WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-roads-updated-at |
| schema_migrations.applied_at | ["schema_migrations.applied_at"] | copy | source_record.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | SDA/Migration Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM schema_migrations WHERE applied_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM schema_migrations WHERE applied_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-schema-migrations-applied-at |
| schema_migrations.checksum | ["schema_migrations.checksum"] | copy-integrity-hash | source_record.raw_payload_hash | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | SDA/Migration Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM schema_migrations; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM schema_migrations; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-schema-migrations-checksum |
| schema_migrations.execution_context | ["schema_migrations.execution_context"] | archive-json-payload | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | SDA/Migration Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM schema_migrations; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM schema_migrations; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-schema-migrations-execution-context |
| schema_migrations.filename | ["schema_migrations.filename"] | target-review-required for schema_migrations.filename | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | SDA/Migration Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM schema_migrations WHERE filename IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM schema_migrations WHERE filename IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-schema-migrations-filename |
| schema_migrations.version | ["schema_migrations.version"] | ledger-crosswalk | source_record.source_record_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | SDA/Migration Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM schema_migrations WHERE version IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM schema_migrations WHERE version IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-schema-migrations-version |
| territories.admin_unit_id | ["territories.admin_unit_id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM territories WHERE admin_unit_id IS NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM territories WHERE admin_unit_id IS NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-territories-admin-unit-id |
| territories.created_at | ["territories.created_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM territories WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM territories WHERE created_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-territories-created-at |
| territories.id | ["territories.id"] | preserve legacy id; generate target id; record crosswalk | legacy_crosswalk.legacy_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM territories WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM territories WHERE id IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-territories-id |
| territories.is_archived | ["territories.is_archived"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM territories; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM territories; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-territories-is-archived |
| territories.name | ["territories.name"] | target-review-required for territories.name | migration_exception.migration_exception_id | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM territories WHERE name IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM territories WHERE name IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-territories-name |
| territories.province_code | ["territories.province_code"] | copy into effective-dated administrative code history | administrative_code_history.official_code | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM territories WHERE province_code IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM territories WHERE province_code IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-territories-province-code |
| territories.readiness | ["territories.readiness"] | translate via registry; unknown values create exception | migration_exception.exception_type | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | translate through controlled vocabulary registry; unknown value -> migration_exception, never free-text canonical state | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM territories WHERE readiness IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM territories WHERE readiness IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-territories-readiness |
| territories.type | ["territories.type"] | preserve raw current value in controlled source archive | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM territories; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM territories; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-territories-type |
| territories.updated_at | ["territories.updated_at"] | preserve as recorded event timestamp | decision_event.recorded_at | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | copy typed value | not a controlled value; preserve exact value or archive raw payload | not required unless exception captures raw source payload | Registry Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM territories WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts | SELECT COUNT(*) FROM territories WHERE updated_at IS NOT NULL; -- reconcile with target/crosswalk counts ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-territories-updated-at |
| users.created_at | ["users.created_at"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Security Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM users; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM users; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-users-created-at |
| users.full_name | ["users.full_name"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Security Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM users; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM users; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-users-full-name |
| users.id | ["users.id"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Security Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM users; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM users; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-users-id |
| users.is_active | ["users.is_active"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Security Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM users; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM users; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-users-is-active |
| users.password_hash | ["users.password_hash"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Security Authority | security-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM users; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM users; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-users-password-hash |
| users.role | ["users.role"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Security Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM users; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM users; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-users-role |
| users.username | ["users.username"] | archive-restricted-raw-value | source_payload_archive.payload_uri | Generate target ULID where target owns identity; always store legacy table/field/pk in legacy_crosswalk for reversible lookup. | store raw restricted value in source_payload_archive.payload_uri plus hash; hash alone is not the archive | not a controlled value; preserve exact value or archive raw payload | source_payload_archive | Security Authority | government-internal | write migration_exception with source key, reason, proposed target and required owner decision; do not silently drop or fabricate | SELECT COUNT(*) FROM users; -- must equal archived/crosswalked source rows plus exceptions | SELECT COUNT(*) FROM users; -- must equal archived/crosswalked source rows plus exceptions ; plus legacy_crosswalk/source_payload_archive/migration_exception reconciliation by source row key. | dual-read/compatibility projection until WO-002B cutover gate validates zero unmapped required fields and approved exception backlog | retire current field dependency after app version NLI-WO-002B+2 reads canonical target and legacy crosswalk/archive audit passes | WO002-R04-TX-users-username |
