# Current-to-Target Field Map — One Row Per Current Field

**Status:** Review 01 remediation artifact.  
**Generation:** Catalog-derived from executable migrations and reconciled against `target-model.json`.  

Disposition values: `canonical-target`, `reference-target`, `evidence-target`, `geometry-evidence-target`, `publication-target`, `operational-target`, `compatibility-source`, `source/evidence-target`, `outside-location-model`, `non-production-fixture`.

| Current table.field | Type/null/default | Target entity.field or disposition | Transformation | Default/provisional handling | Authority | Classification | Ambiguity/loss risk | Validation query | Compatibility period | Retirement condition |
|---|---|---|---|---|---|---|---|---|---|---|
| `address_corrections.address_id` | `TEXT` / NULL / `` | `correction_case.target_legacy_address_id` (correction-case-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | correction authority | `government-internal` | low | `SELECT COUNT(*) FROM address_corrections WHERE address_id IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `address_corrections.correction_type` | `TEXT` / NOT NULL / `` | `correction_case.correction_type` (correction-case-target) | copy with audit | required source value; reject/exception if missing in backfill | correction authority | `government-internal` | low | `SELECT COUNT(*) FROM address_corrections WHERE correction_type IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_corrections.created_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `correction_case.created_at` (correction-case-target) | copy with audit | required source value; reject/exception if missing in backfill | correction authority | `government-internal` | low | `SELECT COUNT(*) FROM address_corrections WHERE created_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_corrections.id` | `TEXT` / NOT NULL / `` | `correction_case.id` (correction-case-target) | copy with audit | required source value; reject/exception if missing in backfill | correction authority | `government-internal` | low | `SELECT COUNT(*) FROM address_corrections WHERE id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_corrections.note` | `TEXT` / NOT NULL / `''` | `correction_case.note` (correction-case-target) | copy with audit | required source value; reject/exception if missing in backfill | correction authority | `government-internal` | low | `SELECT COUNT(*) FROM address_corrections WHERE note IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_corrections.public_code` | `TEXT` / NULL / `` | `correction_case.target_public_code` (correction-case-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | correction authority | `public-after-release` | low | `SELECT COUNT(*) FROM address_corrections WHERE public_code IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `address_corrections.query` | `TEXT` / NOT NULL / `` | `correction_case.submitted_query` (correction-case-target) | copy with audit | required source value; reject/exception if missing in backfill | correction authority | `government-internal` | low | `SELECT COUNT(*) FROM address_corrections WHERE query IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_corrections.reason` | `TEXT` / NOT NULL / `` | `correction_case.reason` (correction-case-target) | copy with audit | required source value; reject/exception if missing in backfill | correction authority | `government-internal` | low | `SELECT COUNT(*) FROM address_corrections WHERE reason IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_corrections.reporter_contact` | `TEXT` / NULL / `` | `correction_case.reporter_contact` (correction-case-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | correction authority | `restricted` | low | `SELECT COUNT(*) FROM address_corrections WHERE reporter_contact IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `address_corrections.reporter_name` | `TEXT` / NULL / `` | `correction_case.reporter_name` (correction-case-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | correction authority | `restricted` | low | `SELECT COUNT(*) FROM address_corrections WHERE reporter_name IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `address_corrections.reviewer_note` | `TEXT` / NOT NULL / `''` | `correction_case.reviewer_note` (correction-case-target) | copy with audit | required source value; reject/exception if missing in backfill | correction authority | `government-internal` | low | `SELECT COUNT(*) FROM address_corrections WHERE reviewer_note IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_corrections.status` | `TEXT` / NOT NULL / `'submitted'` | `correction_case.status` (correction-case-target) | copy with audit | required source value; reject/exception if missing in backfill | correction authority | `government-internal` | low | `SELECT COUNT(*) FROM address_corrections WHERE status IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_corrections.updated_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `correction_case.updated_at` (correction-case-target) | copy with audit | required source value; reject/exception if missing in backfill | correction authority | `government-internal` | low | `SELECT COUNT(*) FROM address_corrections WHERE updated_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_points.accuracy_meters` | `DOUBLE PRECISION` / NULL / `` | `geometry_observation/geometry_version.accuracy_meters` (geometry-evidence-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | legacy geometry | `government-internal` | low | `SELECT COUNT(*) FROM address_points WHERE accuracy_meters IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `address_points.address_id` | `TEXT` / NOT NULL / `` | `geometry_observation/geometry_version.source_record.source_key` (geometry-evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | legacy geometry | `government-internal` | low | `SELECT COUNT(*) FROM address_points WHERE address_id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_points.created_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `geometry_observation/geometry_version.created_at` (geometry-evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | legacy geometry | `government-internal` | low | `SELECT COUNT(*) FROM address_points WHERE created_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_points.id` | `TEXT` / NOT NULL / `` | `geometry_observation/geometry_version.id` (geometry-evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | legacy geometry | `government-internal` | low | `SELECT COUNT(*) FROM address_points WHERE id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_points.is_active` | `BOOLEAN` / NOT NULL / `TRUE` | `geometry_observation/geometry_version.is_current_candidate` (geometry-evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | legacy geometry | `government-internal` | low | `SELECT COUNT(*) FROM address_points WHERE is_active IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_points.latitude` | `DOUBLE PRECISION` / NOT NULL / `` | `geometry_observation/geometry_version.observed_latitude` (geometry-evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | legacy geometry | `restricted` | low | `SELECT COUNT(*) FROM address_points WHERE latitude IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_points.longitude` | `DOUBLE PRECISION` / NOT NULL / `` | `geometry_observation/geometry_version.observed_longitude` (geometry-evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | legacy geometry | `restricted` | low | `SELECT COUNT(*) FROM address_points WHERE longitude IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_points.source_method` | `TEXT` / NOT NULL / `'manual'` | `geometry_observation/geometry_version.capture_method` (geometry-evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | legacy geometry | `government-internal` | low | `SELECT COUNT(*) FROM address_points WHERE source_method IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_points.updated_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `geometry_observation/geometry_version.updated_at` (geometry-evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | legacy geometry | `government-internal` | low | `SELECT COUNT(*) FROM address_points WHERE updated_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_record_events.actor_id` | `TEXT` / NULL / `` | `location_record_event.actor_id` (event-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | registry audit | `government-internal` | low | `SELECT COUNT(*) FROM address_record_events WHERE actor_id IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `address_record_events.actor_role` | `TEXT` / NULL / `` | `location_record_event.actor_role` (event-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | registry audit | `government-internal` | low | `SELECT COUNT(*) FROM address_record_events WHERE actor_role IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `address_record_events.actor_username` | `TEXT` / NULL / `` | `location_record_event.actor_username` (event-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | registry audit | `government-internal` | low | `SELECT COUNT(*) FROM address_record_events WHERE actor_username IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `address_record_events.address_record_id` | `TEXT` / NOT NULL / `` | `location_record_event.location_record_id` (event-target) | copy with audit | required source value; reject/exception if missing in backfill | registry audit | `government-internal` | low | `SELECT COUNT(*) FROM address_record_events WHERE address_record_id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_record_events.created_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `location_record_event.created_at` (event-target) | copy with audit | required source value; reject/exception if missing in backfill | registry audit | `government-internal` | low | `SELECT COUNT(*) FROM address_record_events WHERE created_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_record_events.details` | `JSONB` / NOT NULL / `'{}'::jsonb` | `location_record_event.details_json` (event-target) | copy with audit | required source value; reject/exception if missing in backfill | registry audit | `government-internal` | low | `SELECT COUNT(*) FROM address_record_events WHERE details IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_record_events.event_type` | `TEXT` / NOT NULL / `` | `location_record_event.event_type` (event-target) | copy with audit | required source value; reject/exception if missing in backfill | registry audit | `government-internal` | low | `SELECT COUNT(*) FROM address_record_events WHERE event_type IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_record_events.id` | `TEXT` / NOT NULL / `` | `location_record_event.id` (event-target) | copy with audit | required source value; reject/exception if missing in backfill | registry audit | `government-internal` | low | `SELECT COUNT(*) FROM address_record_events WHERE id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_records.accuracy_meters` | `DOUBLE PRECISION` / NULL / `` | `location_record/location_record_version.accuracy_meters` (canonical-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | canonical registry | `government-internal` | low | `SELECT COUNT(*) FROM address_records WHERE accuracy_meters IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `address_records.address_code` | `TEXT` / NOT NULL / `` | `location_record/location_record_version.public_code_alias.public_code` (canonical-target) | copy with audit | required source value; reject/exception if missing in backfill | canonical registry | `public-after-release` | low | `SELECT COUNT(*) FROM address_records WHERE address_code IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_records.address_label` | `TEXT` / NOT NULL / `` | `location_record/location_record_version.display_label_es` (canonical-target) | copy with audit | required source value; reject/exception if missing in backfill | canonical registry | `government-internal` | low | `SELECT COUNT(*) FROM address_records WHERE address_label IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_records.created_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `location_record/location_record_version.created_at` (canonical-target) | copy with audit | required source value; reject/exception if missing in backfill | canonical registry | `government-internal` | low | `SELECT COUNT(*) FROM address_records WHERE created_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_records.geom` | `geography(Point, 4326)` / NULL / `` | `location_record/location_record_version.geometry_version.geom` (canonical-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | canonical registry | `restricted` | low | `SELECT COUNT(*) FROM address_records WHERE geom IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `address_records.id` | `TEXT` / NOT NULL / `` | `location_record/location_record_version.location_record_id` (canonical-target) | copy with audit | required source value; reject/exception if missing in backfill | canonical registry | `government-internal` | low | `SELECT COUNT(*) FROM address_records WHERE id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_records.is_archived` | `BOOLEAN` / NOT NULL / `FALSE` | `location_record/location_record_version.is_archived` (canonical-target) | copy with audit | required source value; reject/exception if missing in backfill | canonical registry | `government-internal` | low | `SELECT COUNT(*) FROM address_records WHERE is_archived IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_records.latitude` | `DOUBLE PRECISION` / NOT NULL / `` | `location_record/location_record_version.approved_geometry.latitude` (canonical-target) | copy with audit | required source value; reject/exception if missing in backfill | canonical registry | `restricted` | low | `SELECT COUNT(*) FROM address_records WHERE latitude IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_records.longitude` | `DOUBLE PRECISION` / NOT NULL / `` | `location_record/location_record_version.approved_geometry.longitude` (canonical-target) | copy with audit | required source value; reject/exception if missing in backfill | canonical registry | `restricted` | low | `SELECT COUNT(*) FROM address_records WHERE longitude IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_records.province_code` | `TEXT` / NULL / `` | `location_record/location_record_version.province_code` (canonical-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | canonical registry | `government-internal` | low | `SELECT COUNT(*) FROM address_records WHERE province_code IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `address_records.publication_state` | `TEXT` / NOT NULL / `'not-public'` | `location_record/location_record_version.publication_state` (canonical-target) | copy with audit | required source value; reject/exception if missing in backfill | canonical registry | `government-internal` | low | `SELECT COUNT(*) FROM address_records WHERE publication_state IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_records.record_bundle` | `JSONB` / NOT NULL / `'{}'::jsonb` | `location_record/location_record_version.normalized assertions/evidence/events` (canonical-target) | copy with audit | required source value; reject/exception if missing in backfill | canonical registry | `restricted` | low | `SELECT COUNT(*) FROM address_records WHERE record_bundle IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_records.search_text` | `TEXT` / NOT NULL / `''` | `location_record/location_record_version.search_text` (canonical-target) | copy with audit | required source value; reject/exception if missing in backfill | canonical registry | `government-internal` | low | `SELECT COUNT(*) FROM address_records WHERE search_text IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_records.source_submission_id` | `TEXT` / NULL / `` | `location_record/location_record_version.source_record.source_key` (canonical-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | canonical registry | `government-internal` | low | `SELECT COUNT(*) FROM address_records WHERE source_submission_id IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `address_records.status` | `TEXT` / NOT NULL / `` | `location_record/location_record_version.lifecycle_state` (canonical-target) | copy with audit | required source value; reject/exception if missing in backfill | canonical registry | `government-internal` | low | `SELECT COUNT(*) FROM address_records WHERE status IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `address_records.territory_id` | `TEXT` / NULL / `` | `location_record/location_record_version.territory_id` (canonical-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | canonical registry | `government-internal` | low | `SELECT COUNT(*) FROM address_records WHERE territory_id IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `address_records.updated_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `location_record/location_record_version.updated_at` (canonical-target) | copy with audit | required source value; reject/exception if missing in backfill | canonical registry | `government-internal` | low | `SELECT COUNT(*) FROM address_records WHERE updated_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `addresses.building_id` | `TEXT` / NOT NULL / `` | `location_record/version/public_code_alias.building_id` (compatibility-source) | copy with audit | required source value; reject/exception if missing in backfill | legacy compatibility | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM addresses WHERE building_id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `addresses.created_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `location_record/version/public_code_alias.created_at` (compatibility-source) | copy with audit | required source value; reject/exception if missing in backfill | legacy compatibility | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM addresses WHERE created_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `addresses.formatted` | `TEXT` / NOT NULL / `` | `location_record/version/public_code_alias.location_record_version.display_label_es` (compatibility-source) | copy with audit | required source value; reject/exception if missing in backfill | legacy compatibility | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM addresses WHERE formatted IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `addresses.id` | `TEXT` / NOT NULL / `` | `location_record/version/public_code_alias.source_record.source_key` (compatibility-source) | copy with audit | required source value; reject/exception if missing in backfill | legacy compatibility | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM addresses WHERE id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `addresses.is_archived` | `BOOLEAN` / NOT NULL / `FALSE` | `location_record/version/public_code_alias.is_archived` (compatibility-source) | copy with audit | required source value; reject/exception if missing in backfill | legacy compatibility | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM addresses WHERE is_archived IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `addresses.issuance_method` | `TEXT` / NOT NULL / `'manual'` | `location_record/version/public_code_alias.issuance_method` (compatibility-source) | copy with audit | required source value; reject/exception if missing in backfill | legacy compatibility | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM addresses WHERE issuance_method IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `addresses.province_code` | `TEXT` / NOT NULL / `` | `location_record/version/public_code_alias.province_code` (compatibility-source) | copy with audit | required source value; reject/exception if missing in backfill | legacy compatibility | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM addresses WHERE province_code IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `addresses.public_code` | `TEXT` / NULL / `` | `location_record/version/public_code_alias.public_code_alias.public_code` (compatibility-source) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | legacy compatibility | `public-after-release` | medium: authority or semantic split required | `SELECT COUNT(*) FROM addresses WHERE public_code IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `addresses.publication_state` | `TEXT` / NOT NULL / `'draft'` | `location_record/version/public_code_alias.publication_state` (compatibility-source) | copy with audit | required source value; reject/exception if missing in backfill | legacy compatibility | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM addresses WHERE publication_state IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `addresses.road_id` | `TEXT` / NOT NULL / `` | `location_record/version/public_code_alias.road_id` (compatibility-source) | copy with audit | required source value; reject/exception if missing in backfill | legacy compatibility | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM addresses WHERE road_id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `addresses.source` | `TEXT` / NOT NULL / `'admin-portal'` | `location_record/version/public_code_alias.source` (compatibility-source) | copy with audit | required source value; reject/exception if missing in backfill | legacy compatibility | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM addresses WHERE source IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `addresses.status` | `TEXT` / NOT NULL / `` | `location_record/version/public_code_alias.status` (compatibility-source) | copy with audit | required source value; reject/exception if missing in backfill | legacy compatibility | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM addresses WHERE status IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `addresses.superseded_by_address_id` | `TEXT` / NULL / `` | `location_record/version/public_code_alias.record_relationship.successor_record_id` (compatibility-source) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | legacy compatibility | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM addresses WHERE superseded_by_address_id IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `addresses.territory_id` | `TEXT` / NOT NULL / `` | `location_record/version/public_code_alias.territory_id` (compatibility-source) | copy with audit | required source value; reject/exception if missing in backfill | legacy compatibility | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM addresses WHERE territory_id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `addresses.updated_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `location_record/version/public_code_alias.updated_at` (compatibility-source) | copy with audit | required source value; reject/exception if missing in backfill | legacy compatibility | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM addresses WHERE updated_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `addresses.verification_status` | `TEXT` / NOT NULL / `'provisional'` | `location_record/version/public_code_alias.location_record_assertion.verification_state` (compatibility-source) | copy with audit | required source value; reject/exception if missing in backfill | legacy compatibility | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM addresses WHERE verification_status IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `admin_units.code` | `TEXT` / NOT NULL / `` | `administrative_unit.official_code` (reference-target) | copy with audit | required source value; reject/exception if missing in backfill | GIS/admin authority | `public-after-approval` | low | `SELECT COUNT(*) FROM admin_units WHERE code IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `admin_units.created_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `administrative_unit.created_at` (reference-target) | copy with audit | required source value; reject/exception if missing in backfill | GIS/admin authority | `public-after-approval` | low | `SELECT COUNT(*) FROM admin_units WHERE created_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `admin_units.id` | `TEXT` / NOT NULL / `` | `administrative_unit.id` (reference-target) | copy with audit | required source value; reject/exception if missing in backfill | GIS/admin authority | `public-after-approval` | low | `SELECT COUNT(*) FROM admin_units WHERE id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `admin_units.level` | `TEXT` / NOT NULL / `` | `administrative_unit.admin_level` (reference-target) | copy with audit | required source value; reject/exception if missing in backfill | GIS/admin authority | `public-after-approval` | low | `SELECT COUNT(*) FROM admin_units WHERE level IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `admin_units.name_en` | `TEXT` / NOT NULL / `` | `administrative_unit.official_name_en` (reference-target) | copy with audit | required source value; reject/exception if missing in backfill | GIS/admin authority | `public-after-approval` | low | `SELECT COUNT(*) FROM admin_units WHERE name_en IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `admin_units.name_es` | `TEXT` / NOT NULL / `` | `administrative_unit.official_name_es` (reference-target) | copy with audit | required source value; reject/exception if missing in backfill | GIS/admin authority | `public-after-approval` | low | `SELECT COUNT(*) FROM admin_units WHERE name_es IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `admin_units.parent_id` | `TEXT` / NULL / `` | `administrative_unit.parent_admin_unit_id` (reference-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | GIS/admin authority | `public-after-approval` | low | `SELECT COUNT(*) FROM admin_units WHERE parent_id IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `admin_units.province_code` | `TEXT` / NULL / `` | `administrative_unit.parent/province link` (reference-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | GIS/admin authority | `public-after-approval` | low | `SELECT COUNT(*) FROM admin_units WHERE province_code IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `admin_units.sort_order` | `INTEGER` / NOT NULL / `0` | `administrative_unit.sort_order` (reference-target) | copy with audit | required source value; reject/exception if missing in backfill | GIS/admin authority | `public-after-approval` | low | `SELECT COUNT(*) FROM admin_units WHERE sort_order IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `admin_units.status` | `TEXT` / NOT NULL / `'active'` | `administrative_unit.lifecycle_state` (reference-target) | copy with audit | required source value; reject/exception if missing in backfill | GIS/admin authority | `public-after-approval` | low | `SELECT COUNT(*) FROM admin_units WHERE status IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `admin_units.updated_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `administrative_unit.updated_at` (reference-target) | copy with audit | required source value; reject/exception if missing in backfill | GIS/admin authority | `public-after-approval` | low | `SELECT COUNT(*) FROM admin_units WHERE updated_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `audit_logs.action` | `TEXT` / NOT NULL / `` | `audit_logs.action` (operational-target) | copy with audit | required source value; reject/exception if missing in backfill | operational | `government-internal` | low | `SELECT COUNT(*) FROM audit_logs WHERE action IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `audit_logs.actor_role` | `TEXT` / NULL / `` | `audit_logs.actor_role` (operational-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | operational | `government-internal` | low | `SELECT COUNT(*) FROM audit_logs WHERE actor_role IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `audit_logs.actor_user_id` | `TEXT` / NULL / `` | `audit_logs.actor_user_id` (operational-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | operational | `government-internal` | low | `SELECT COUNT(*) FROM audit_logs WHERE actor_user_id IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `audit_logs.actor_username` | `TEXT` / NULL / `` | `audit_logs.actor_username` (operational-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | operational | `government-internal` | low | `SELECT COUNT(*) FROM audit_logs WHERE actor_username IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `audit_logs.created_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `audit_logs.created_at` (operational-target) | copy with audit | required source value; reject/exception if missing in backfill | operational | `government-internal` | low | `SELECT COUNT(*) FROM audit_logs WHERE created_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `audit_logs.details` | `TEXT` / NOT NULL / `'{}'` | `audit_logs.details` (operational-target) | copy with audit | required source value; reject/exception if missing in backfill | operational | `government-internal` | low | `SELECT COUNT(*) FROM audit_logs WHERE details IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `audit_logs.entity_id` | `TEXT` / NOT NULL / `` | `audit_logs.entity_id` (operational-target) | copy with audit | required source value; reject/exception if missing in backfill | operational | `government-internal` | low | `SELECT COUNT(*) FROM audit_logs WHERE entity_id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `audit_logs.entity_type` | `TEXT` / NOT NULL / `` | `audit_logs.entity_type` (operational-target) | copy with audit | required source value; reject/exception if missing in backfill | operational | `government-internal` | low | `SELECT COUNT(*) FROM audit_logs WHERE entity_type IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `audit_logs.id` | `BIGSERIAL` / NOT NULL / `` | `audit_logs.id` (operational-target) | copy with audit | required source value; reject/exception if missing in backfill | operational | `government-internal` | low | `SELECT COUNT(*) FROM audit_logs WHERE id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `auth_tokens.created_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `actor/session reference only` (outside-location-model) | copy with audit | required source value; reject/exception if missing in backfill | security authority | `highly-restricted` | low | `SELECT COUNT(*) FROM auth_tokens WHERE created_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `auth_tokens.expires_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `actor/session reference only` (outside-location-model) | copy with audit | required source value; reject/exception if missing in backfill | security authority | `highly-restricted` | low | `SELECT COUNT(*) FROM auth_tokens WHERE expires_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `auth_tokens.last_seen_at` | `TIMESTAMPTZ` / NULL / `` | `actor/session reference only` (outside-location-model) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | security authority | `highly-restricted` | low | `SELECT COUNT(*) FROM auth_tokens WHERE last_seen_at IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `auth_tokens.revoked_at` | `TIMESTAMPTZ` / NULL / `` | `actor/session reference only` (outside-location-model) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | security authority | `highly-restricted` | low | `SELECT COUNT(*) FROM auth_tokens WHERE revoked_at IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `auth_tokens.token` | `TEXT` / NOT NULL / `` | `actor/session reference only` (outside-location-model) | copy with audit | required source value; reject/exception if missing in backfill | security authority | `highly-restricted` | low | `SELECT COUNT(*) FROM auth_tokens WHERE token IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `auth_tokens.user_id` | `TEXT` / NOT NULL / `` | `actor/session reference only` (outside-location-model) | copy with audit | required source value; reject/exception if missing in backfill | security authority | `highly-restricted` | low | `SELECT COUNT(*) FROM auth_tokens WHERE user_id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `buildings.created_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `building/entrance.created_at` (canonical/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | registry/field | `government-internal` | low | `SELECT COUNT(*) FROM buildings WHERE created_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `buildings.id` | `TEXT` / NOT NULL / `` | `building/entrance.building_id` (canonical/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | registry/field | `government-internal` | low | `SELECT COUNT(*) FROM buildings WHERE id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `buildings.is_archived` | `BOOLEAN` / NOT NULL / `FALSE` | `building/entrance.is_archived` (canonical/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | registry/field | `government-internal` | low | `SELECT COUNT(*) FROM buildings WHERE is_archived IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `buildings.label` | `TEXT` / NOT NULL / `` | `building/entrance.building_name.name_text` (canonical/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | registry/field | `government-internal` | low | `SELECT COUNT(*) FROM buildings WHERE label IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `buildings.road_id` | `TEXT` / NOT NULL / `` | `building/entrance.building_road_context.road_segment_id` (canonical/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | registry/field | `government-internal` | low | `SELECT COUNT(*) FROM buildings WHERE road_id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `buildings.spatial_evidence` | `JSONB` / NOT NULL / `'{}'::jsonb` | `building/entrance.geometry_observation.raw_payload` (canonical/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | registry/field | `restricted` | low | `SELECT COUNT(*) FROM buildings WHERE spatial_evidence IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `buildings.status` | `TEXT` / NOT NULL / `` | `building/entrance.status` (canonical/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | registry/field | `government-internal` | low | `SELECT COUNT(*) FROM buildings WHERE status IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `buildings.territory_id` | `TEXT` / NOT NULL / `` | `building/entrance.territory_id` (canonical/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | registry/field | `government-internal` | low | `SELECT COUNT(*) FROM buildings WHERE territory_id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `buildings.updated_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `building/entrance.updated_at` (canonical/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | registry/field | `government-internal` | low | `SELECT COUNT(*) FROM buildings WHERE updated_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `buildings.usage` | `TEXT` / NOT NULL / `` | `building/entrance.usage` (canonical/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | registry/field | `government-internal` | low | `SELECT COUNT(*) FROM buildings WHERE usage IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.accuracy_meters` | `DOUBLE PRECISION` / NULL / `` | `intake_case/geometry_observation/source_record.geometry_observation.accuracy_meters` (evidence-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE accuracy_meters IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.address_label` | `TEXT` / NOT NULL / `` | `intake_case/geometry_observation/source_record.submitted_label` (evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE address_label IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.capture_method` | `TEXT` / NOT NULL / `'browser-gps'` | `intake_case/geometry_observation/source_record.capture_method` (evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE capture_method IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.citizen_contact` | `TEXT` / NULL / `` | `intake_case/geometry_observation/source_record.party_contact.contact_restricted` (evidence-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | citizen intake / registry review | `restricted` | high: sensitive identity field requires privacy authority | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE citizen_contact IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.citizen_name` | `TEXT` / NULL / `` | `intake_case/geometry_observation/source_record.party_contact.name_restricted` (evidence-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | citizen intake / registry review | `restricted` | high: sensitive identity field requires privacy authority | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE citizen_name IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.created_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `intake_case/geometry_observation/source_record.created_at` (evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE created_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.dip_last4` | `TEXT` / NULL / `` | `intake_case/geometry_observation/source_record.identity_assertion.masked_value` (evidence-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | citizen intake / registry review | `highly-restricted` | high: sensitive identity field requires privacy authority | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE dip_last4 IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.duplicate_hint` | `TEXT` / NOT NULL / `'none'` | `intake_case/geometry_observation/source_record.duplicate_hint` (evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE duplicate_hint IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.field_note` | `TEXT` / NOT NULL / `''` | `intake_case/geometry_observation/source_record.field_note` (evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE field_note IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.field_status` | `TEXT` / NOT NULL / `'assigned'` | `intake_case/geometry_observation/source_record.field_verification_state` (evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE field_status IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.field_submission_id` | `TEXT` / NULL / `` | `intake_case/geometry_observation/source_record.field_submission_id` (evidence-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE field_submission_id IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.field_verified_at` | `TIMESTAMPTZ` / NULL / `` | `intake_case/geometry_observation/source_record.field_verified_at` (evidence-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE field_verified_at IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.grid_code` | `TEXT` / NOT NULL / `` | `intake_case/geometry_observation/source_record.provisional_code` (evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE grid_code IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.id` | `TEXT` / NOT NULL / `` | `intake_case/geometry_observation/source_record.intake_case_id` (evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.identity_document_verified` | `BOOLEAN` / NOT NULL / `FALSE` | `intake_case/geometry_observation/source_record.identity_document_verified` (evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | citizen intake / registry review | `restricted` | high: sensitive identity field requires privacy authority | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE identity_document_verified IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.identity_verification_status` | `TEXT` / NOT NULL / `'unverified'` | `intake_case/geometry_observation/source_record.identity_verification_status` (evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | citizen intake / registry review | `restricted` | high: sensitive identity field requires privacy authority | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE identity_verification_status IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.identity_verified_at` | `TIMESTAMPTZ` / NULL / `` | `intake_case/geometry_observation/source_record.identity_verified_at` (evidence-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | citizen intake / registry review | `restricted` | high: sensitive identity field requires privacy authority | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE identity_verified_at IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.landmark` | `TEXT` / NOT NULL / `''` | `intake_case/geometry_observation/source_record.landmark` (evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE landmark IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.latitude` | `DOUBLE PRECISION` / NOT NULL / `` | `intake_case/geometry_observation/source_record.geometry_observation.observed_latitude` (evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | citizen intake / registry review | `restricted` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE latitude IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.longitude` | `DOUBLE PRECISION` / NOT NULL / `` | `intake_case/geometry_observation/source_record.geometry_observation.observed_longitude` (evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | citizen intake / registry review | `restricted` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE longitude IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.map_display_name` | `TEXT` / NULL / `` | `intake_case/geometry_observation/source_record.map_display_name` (evidence-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE map_display_name IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.reviewed_road_name` | `TEXT` / NULL / `` | `intake_case/geometry_observation/source_record.reviewed_road_name` (evidence-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE reviewed_road_name IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.reviewer_note` | `TEXT` / NOT NULL / `''` | `intake_case/geometry_observation/source_record.reviewer_note` (evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE reviewer_note IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.road_suggestion_attribution` | `TEXT` / NULL / `` | `intake_case/geometry_observation/source_record.road_suggestion_attribution` (evidence-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE road_suggestion_attribution IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.road_suggestion_source` | `TEXT` / NULL / `` | `intake_case/geometry_observation/source_record.road_suggestion_source` (evidence-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE road_suggestion_source IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.road_suggestion_status` | `TEXT` / NOT NULL / `'not-suggested'` | `intake_case/geometry_observation/source_record.road_suggestion_status` (evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE road_suggestion_status IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.signage_batch` | `TEXT` / NULL / `` | `intake_case/geometry_observation/source_record.publication_release_item.legacy_batch_ref` (evidence-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE signage_batch IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.status` | `TEXT` / NOT NULL / `'submitted'` | `intake_case/geometry_observation/source_record.intake_state` (evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE status IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.suggested_local_area` | `TEXT` / NULL / `` | `intake_case/geometry_observation/source_record.suggested_local_area` (evidence-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE suggested_local_area IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.suggested_place_name` | `TEXT` / NULL / `` | `intake_case/geometry_observation/source_record.suggested_place_name` (evidence-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE suggested_place_name IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.suggested_road_name` | `TEXT` / NULL / `` | `intake_case/geometry_observation/source_record.suggested_road_name` (evidence-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE suggested_road_name IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.territory_id` | `TEXT` / NULL / `` | `intake_case/geometry_observation/source_record.territory_id` (evidence-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE territory_id IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `citizen_geotag_submissions.updated_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `intake_case/geometry_observation/source_record.updated_at` (evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | citizen intake / registry review | `government-internal` | medium: authority or semantic split required | `SELECT COUNT(*) FROM citizen_geotag_submissions WHERE updated_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `development_fixture_batches.batch_id` | `TEXT` / NOT NULL / `` | `excluded from canonical; retained in fixture ledger` (non-production-fixture) | copy with audit | required source value; reject/exception if missing in backfill | fixture only | `government-internal` | low | `SELECT COUNT(*) FROM development_fixture_batches WHERE batch_id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `development_fixture_batches.cleaned_at` | `TIMESTAMPTZ` / NULL / `` | `excluded from canonical; retained in fixture ledger` (non-production-fixture) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | fixture only | `government-internal` | low | `SELECT COUNT(*) FROM development_fixture_batches WHERE cleaned_at IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `development_fixture_batches.environment` | `TEXT` / NOT NULL / `` | `excluded from canonical; retained in fixture ledger` (non-production-fixture) | copy with audit | required source value; reject/exception if missing in backfill | fixture only | `government-internal` | low | `SELECT COUNT(*) FROM development_fixture_batches WHERE environment IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `development_fixture_batches.execution_context` | `TEXT` / NOT NULL / `'{}'` | `excluded from canonical; retained in fixture ledger` (non-production-fixture) | copy with audit | required source value; reject/exception if missing in backfill | fixture only | `government-internal` | low | `SELECT COUNT(*) FROM development_fixture_batches WHERE execution_context IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `development_fixture_batches.fixture_version` | `TEXT` / NOT NULL / `` | `excluded from canonical; retained in fixture ledger` (non-production-fixture) | copy with audit | required source value; reject/exception if missing in backfill | fixture only | `government-internal` | low | `SELECT COUNT(*) FROM development_fixture_batches WHERE fixture_version IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `development_fixture_batches.loaded_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `excluded from canonical; retained in fixture ledger` (non-production-fixture) | copy with audit | required source value; reject/exception if missing in backfill | fixture only | `government-internal` | low | `SELECT COUNT(*) FROM development_fixture_batches WHERE loaded_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `development_fixture_records.batch_id` | `TEXT` / NOT NULL / `` | `excluded from canonical; retained in fixture ledger` (non-production-fixture) | copy with audit | required source value; reject/exception if missing in backfill | fixture only | `government-internal` | low | `SELECT COUNT(*) FROM development_fixture_records WHERE batch_id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `development_fixture_records.created_by_batch` | `BOOLEAN` / NOT NULL / `` | `excluded from canonical; retained in fixture ledger` (non-production-fixture) | copy with audit | required source value; reject/exception if missing in backfill | fixture only | `government-internal` | low | `SELECT COUNT(*) FROM development_fixture_records WHERE created_by_batch IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `development_fixture_records.record_id` | `TEXT` / NOT NULL / `` | `excluded from canonical; retained in fixture ledger` (non-production-fixture) | copy with audit | required source value; reject/exception if missing in backfill | fixture only | `government-internal` | low | `SELECT COUNT(*) FROM development_fixture_records WHERE record_id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `development_fixture_records.recorded_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `excluded from canonical; retained in fixture ledger` (non-production-fixture) | copy with audit | required source value; reject/exception if missing in backfill | fixture only | `government-internal` | low | `SELECT COUNT(*) FROM development_fixture_records WHERE recorded_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `development_fixture_records.table_name` | `TEXT` / NOT NULL / `` | `excluded from canonical; retained in fixture ledger` (non-production-fixture) | copy with audit | required source value; reject/exception if missing in backfill | fixture only | `government-internal` | low | `SELECT COUNT(*) FROM development_fixture_records WHERE table_name IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `field_assignments.assignment_id` | `TEXT` / NOT NULL / `` | `field_assignment.assignment_id` (operational/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | field operations | `government-internal` | low | `SELECT COUNT(*) FROM field_assignments WHERE assignment_id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `field_assignments.created_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `field_assignment.created_at` (operational/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | field operations | `government-internal` | low | `SELECT COUNT(*) FROM field_assignments WHERE created_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `field_assignments.priority` | `TEXT` / NOT NULL / `` | `field_assignment.priority` (operational/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | field operations | `government-internal` | low | `SELECT COUNT(*) FROM field_assignments WHERE priority IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `field_assignments.task` | `TEXT` / NOT NULL / `` | `field_assignment.task` (operational/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | field operations | `government-internal` | low | `SELECT COUNT(*) FROM field_assignments WHERE task IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `field_assignments.team` | `TEXT` / NOT NULL / `` | `field_assignment.team` (operational/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | field operations | `government-internal` | low | `SELECT COUNT(*) FROM field_assignments WHERE team IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `field_assignments.territory` | `TEXT` / NOT NULL / `` | `field_assignment.territory` (operational/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | field operations | `government-internal` | low | `SELECT COUNT(*) FROM field_assignments WHERE territory IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `field_assignments.territory_id` | `TEXT` / NOT NULL / `` | `field_assignment.territory_id` (operational/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | field operations | `government-internal` | low | `SELECT COUNT(*) FROM field_assignments WHERE territory_id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `field_submissions.assignment_id` | `TEXT` / NULL / `` | `field_observation.assignment_id` (operational/evidence-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | field operations | `government-internal` | low | `SELECT COUNT(*) FROM field_submissions WHERE assignment_id IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `field_submissions.candidate_name` | `TEXT` / NOT NULL / `` | `field_observation.candidate_name` (operational/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | field operations | `government-internal` | low | `SELECT COUNT(*) FROM field_submissions WHERE candidate_name IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `field_submissions.candidate_status` | `TEXT` / NOT NULL / `` | `field_observation.candidate_status` (operational/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | field operations | `government-internal` | low | `SELECT COUNT(*) FROM field_submissions WHERE candidate_status IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `field_submissions.created_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `field_observation.created_at` (operational/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | field operations | `government-internal` | low | `SELECT COUNT(*) FROM field_submissions WHERE created_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `field_submissions.id` | `TEXT` / NOT NULL / `` | `field_observation.id` (operational/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | field operations | `government-internal` | low | `SELECT COUNT(*) FROM field_submissions WHERE id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `field_submissions.notes` | `TEXT` / NOT NULL / `''` | `field_observation.notes` (operational/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | field operations | `government-internal` | low | `SELECT COUNT(*) FROM field_submissions WHERE notes IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `field_submissions.registry_entity_id` | `TEXT` / NULL / `` | `field_observation.registry_entity_id` (operational/evidence-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | field operations | `government-internal` | low | `SELECT COUNT(*) FROM field_submissions WHERE registry_entity_id IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `field_submissions.review_status` | `TEXT` / NOT NULL / `'submitted'` | `field_observation.review_status` (operational/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | field operations | `government-internal` | low | `SELECT COUNT(*) FROM field_submissions WHERE review_status IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `field_submissions.reviewer_note` | `TEXT` / NOT NULL / `''` | `field_observation.reviewer_note` (operational/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | field operations | `government-internal` | low | `SELECT COUNT(*) FROM field_submissions WHERE reviewer_note IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `field_submissions.spatial_evidence` | `JSONB` / NOT NULL / `'{}'::jsonb` | `field_observation.spatial_evidence` (operational/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | field operations | `restricted` | low | `SELECT COUNT(*) FROM field_submissions WHERE spatial_evidence IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `field_submissions.submission_type` | `TEXT` / NOT NULL / `` | `field_observation.submission_type` (operational/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | field operations | `government-internal` | low | `SELECT COUNT(*) FROM field_submissions WHERE submission_type IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `field_submissions.submitted_by` | `TEXT` / NOT NULL / `` | `field_observation.submitted_by` (operational/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | field operations | `government-internal` | low | `SELECT COUNT(*) FROM field_submissions WHERE submitted_by IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `field_submissions.territory_id` | `TEXT` / NOT NULL / `` | `field_observation.territory_id` (operational/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | field operations | `government-internal` | low | `SELECT COUNT(*) FROM field_submissions WHERE territory_id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `field_submissions.updated_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `field_observation.updated_at` (operational/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | field operations | `government-internal` | low | `SELECT COUNT(*) FROM field_submissions WHERE updated_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `import_jobs.created_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `source_package/source_record.created_at` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM import_jobs WHERE created_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `import_jobs.id` | `TEXT` / NOT NULL / `` | `source_package/source_record.id` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM import_jobs WHERE id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `import_jobs.imported_count` | `INTEGER` / NOT NULL / `0` | `source_package/source_record.imported_count` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM import_jobs WHERE imported_count IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `import_jobs.name` | `TEXT` / NOT NULL / `` | `source_package/source_record.name` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM import_jobs WHERE name IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `import_jobs.source_name` | `TEXT` / NOT NULL / `` | `source_package/source_record.source_name` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM import_jobs WHERE source_name IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `import_jobs.status` | `TEXT` / NOT NULL / `'draft'` | `source_package/source_record.status` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM import_jobs WHERE status IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `import_jobs.updated_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `source_package/source_record.updated_at` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM import_jobs WHERE updated_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `import_rows.candidate_name` | `TEXT` / NOT NULL / `` | `source_package/source_record.candidate_name` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM import_rows WHERE candidate_name IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `import_rows.candidate_status` | `TEXT` / NOT NULL / `` | `source_package/source_record.candidate_status` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM import_rows WHERE candidate_status IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `import_rows.committed_submission_id` | `TEXT` / NULL / `` | `source_package/source_record.committed_submission_id` (source/evidence-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | source package | `government-internal` | low | `SELECT COUNT(*) FROM import_rows WHERE committed_submission_id IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `import_rows.job_id` | `TEXT` / NOT NULL / `` | `source_package/source_record.job_id` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM import_rows WHERE job_id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `import_rows.notes` | `TEXT` / NOT NULL / `''` | `source_package/source_record.notes` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM import_rows WHERE notes IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `import_rows.row_number` | `INTEGER` / NOT NULL / `` | `source_package/source_record.row_number` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM import_rows WHERE row_number IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `import_rows.submission_type` | `TEXT` / NOT NULL / `` | `source_package/source_record.submission_type` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM import_rows WHERE submission_type IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `import_rows.territory_id` | `TEXT` / NOT NULL / `` | `source_package/source_record.territory_id` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM import_rows WHERE territory_id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `import_rows.validation_message` | `TEXT` / NOT NULL / `` | `source_package/source_record.validation_message` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM import_rows WHERE validation_message IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `import_rows.validation_status` | `TEXT` / NOT NULL / `` | `source_package/source_record.validation_status` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM import_rows WHERE validation_status IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `provinces.code` | `TEXT` / NOT NULL / `` | `administrative_unit.official_code` (reference-target) | copy with audit | required source value; reject/exception if missing in backfill | GIS/admin authority | `public-after-approval` | low | `SELECT COUNT(*) FROM provinces WHERE code IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `provinces.created_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `administrative_unit.created_at` (reference-target) | copy with audit | required source value; reject/exception if missing in backfill | GIS/admin authority | `public-after-approval` | low | `SELECT COUNT(*) FROM provinces WHERE created_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `provinces.name` | `TEXT` / NOT NULL / `` | `administrative_unit.official_name_es` (reference-target) | copy with audit | required source value; reject/exception if missing in backfill | GIS/admin authority | `public-after-approval` | low | `SELECT COUNT(*) FROM provinces WHERE name IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `publication_pack_addresses.address_id` | `TEXT` / NOT NULL / `` | `publication_release/release_item.address_id` (publication-target) | copy with audit | required source value; reject/exception if missing in backfill | publication authority | `government-internal/public-release-metadata` | low | `SELECT COUNT(*) FROM publication_pack_addresses WHERE address_id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `publication_pack_addresses.publication_pack_id` | `TEXT` / NOT NULL / `` | `publication_release/release_item.publication_pack_id` (publication-target) | copy with audit | required source value; reject/exception if missing in backfill | publication authority | `government-internal/public-release-metadata` | low | `SELECT COUNT(*) FROM publication_pack_addresses WHERE publication_pack_id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `publication_packs.audience` | `TEXT` / NOT NULL / `` | `publication_release/release_item.audience` (publication-target) | copy with audit | required source value; reject/exception if missing in backfill | publication authority | `government-internal/public-release-metadata` | low | `SELECT COUNT(*) FROM publication_packs WHERE audience IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `publication_packs.created_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `publication_release/release_item.created_at` (publication-target) | copy with audit | required source value; reject/exception if missing in backfill | publication authority | `government-internal/public-release-metadata` | low | `SELECT COUNT(*) FROM publication_packs WHERE created_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `publication_packs.id` | `TEXT` / NOT NULL / `` | `publication_release/release_item.id` (publication-target) | copy with audit | required source value; reject/exception if missing in backfill | publication authority | `government-internal/public-release-metadata` | low | `SELECT COUNT(*) FROM publication_packs WHERE id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `publication_packs.name` | `TEXT` / NOT NULL / `` | `publication_release/release_item.name` (publication-target) | copy with audit | required source value; reject/exception if missing in backfill | publication authority | `government-internal/public-release-metadata` | low | `SELECT COUNT(*) FROM publication_packs WHERE name IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `publication_packs.status` | `TEXT` / NOT NULL / `` | `publication_release/release_item.status` (publication-target) | copy with audit | required source value; reject/exception if missing in backfill | publication authority | `government-internal/public-release-metadata` | low | `SELECT COUNT(*) FROM publication_packs WHERE status IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `publication_packs.updated_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `publication_release/release_item.updated_at` (publication-target) | copy with audit | required source value; reject/exception if missing in backfill | publication authority | `government-internal/public-release-metadata` | low | `SELECT COUNT(*) FROM publication_packs WHERE updated_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `reference_data_load_history.authority_status` | `TEXT` / NOT NULL / `` | `source_package/source_record.authority_status` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM reference_data_load_history WHERE authority_status IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `reference_data_load_history.execution_context` | `TEXT` / NOT NULL / `'{}'` | `source_package/source_record.execution_context` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM reference_data_load_history WHERE execution_context IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `reference_data_load_history.id` | `BIGSERIAL` / NOT NULL / `` | `source_package/source_record.id` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM reference_data_load_history WHERE id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `reference_data_load_history.loaded_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `source_package/source_record.loaded_at` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM reference_data_load_history WHERE loaded_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `reference_data_load_history.package_checksum` | `TEXT` / NOT NULL / `` | `source_package/source_record.package_checksum` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM reference_data_load_history WHERE package_checksum IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `reference_data_load_history.package_id` | `TEXT` / NOT NULL / `` | `source_package/source_record.package_id` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM reference_data_load_history WHERE package_id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `reference_data_load_history.package_version` | `TEXT` / NOT NULL / `` | `source_package/source_record.package_version` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM reference_data_load_history WHERE package_version IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `reference_data_load_history.source` | `TEXT` / NOT NULL / `` | `source_package/source_record.source` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM reference_data_load_history WHERE source IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `reference_data_loads.authority_status` | `TEXT` / NOT NULL / `` | `source_package/source_record.authority_status` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM reference_data_loads WHERE authority_status IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `reference_data_loads.execution_context` | `TEXT` / NOT NULL / `'{}'` | `source_package/source_record.execution_context` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM reference_data_loads WHERE execution_context IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `reference_data_loads.loaded_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `source_package/source_record.loaded_at` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM reference_data_loads WHERE loaded_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `reference_data_loads.package_checksum` | `TEXT` / NOT NULL / `` | `source_package/source_record.package_checksum` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM reference_data_loads WHERE package_checksum IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `reference_data_loads.package_id` | `TEXT` / NOT NULL / `` | `source_package/source_record.package_id` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM reference_data_loads WHERE package_id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `reference_data_loads.package_version` | `TEXT` / NOT NULL / `` | `source_package/source_record.package_version` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM reference_data_loads WHERE package_version IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `reference_data_loads.source` | `TEXT` / NOT NULL / `` | `source_package/source_record.source` (source/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | source package | `government-internal` | low | `SELECT COUNT(*) FROM reference_data_loads WHERE source IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `roads.created_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `road/road_segment/road_name.created_at` (canonical/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | registry/GIS | `government-internal` | low | `SELECT COUNT(*) FROM roads WHERE created_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `roads.id` | `TEXT` / NOT NULL / `` | `road/road_segment/road_name.road_id` (canonical/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | registry/GIS | `government-internal` | low | `SELECT COUNT(*) FROM roads WHERE id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `roads.is_archived` | `BOOLEAN` / NOT NULL / `FALSE` | `road/road_segment/road_name.is_archived` (canonical/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | registry/GIS | `government-internal` | low | `SELECT COUNT(*) FROM roads WHERE is_archived IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `roads.length_km` | `TEXT` / NOT NULL / `` | `road/road_segment/road_name.road_segment.measured_length_m` (canonical/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | registry/GIS | `government-internal` | low | `SELECT COUNT(*) FROM roads WHERE length_km IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `roads.name` | `TEXT` / NOT NULL / `` | `road/road_segment/road_name.road_name.name_text` (canonical/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | registry/GIS | `government-internal` | low | `SELECT COUNT(*) FROM roads WHERE name IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `roads.spatial_evidence` | `JSONB` / NOT NULL / `'{}'::jsonb` | `road/road_segment/road_name.geometry_observation.raw_payload` (canonical/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | registry/GIS | `restricted` | low | `SELECT COUNT(*) FROM roads WHERE spatial_evidence IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `roads.status` | `TEXT` / NOT NULL / `` | `road/road_segment/road_name.status` (canonical/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | registry/GIS | `government-internal` | low | `SELECT COUNT(*) FROM roads WHERE status IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `roads.territory_id` | `TEXT` / NOT NULL / `` | `road/road_segment/road_name.operational_area link` (canonical/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | registry/GIS | `government-internal` | low | `SELECT COUNT(*) FROM roads WHERE territory_id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `roads.updated_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `road/road_segment/road_name.updated_at` (canonical/evidence-target) | copy with audit | required source value; reject/exception if missing in backfill | registry/GIS | `government-internal` | low | `SELECT COUNT(*) FROM roads WHERE updated_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `territories.admin_unit_id` | `TEXT` / NULL / `` | `operational_area.coverage.admin_unit_id` (operational-target) | copy with audit | preserve current value; if null, create explicit unknown/not-provided assertion | operator/programme | `government-internal` | low | `SELECT COUNT(*) FROM territories WHERE admin_unit_id IS NULL;` | until WO-002B contract phase | after parity and replacement approved |
| `territories.created_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `operational_area.created_at` (operational-target) | copy with audit | required source value; reject/exception if missing in backfill | operator/programme | `government-internal` | low | `SELECT COUNT(*) FROM territories WHERE created_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `territories.id` | `TEXT` / NOT NULL / `` | `operational_area.operational_area_id` (operational-target) | copy with audit | required source value; reject/exception if missing in backfill | operator/programme | `government-internal` | low | `SELECT COUNT(*) FROM territories WHERE id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `territories.is_archived` | `BOOLEAN` / NOT NULL / `FALSE` | `operational_area.is_archived` (operational-target) | copy with audit | required source value; reject/exception if missing in backfill | operator/programme | `government-internal` | low | `SELECT COUNT(*) FROM territories WHERE is_archived IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `territories.name` | `TEXT` / NOT NULL / `` | `operational_area.name` (operational-target) | copy with audit | required source value; reject/exception if missing in backfill | operator/programme | `government-internal` | low | `SELECT COUNT(*) FROM territories WHERE name IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `territories.province_code` | `TEXT` / NOT NULL / `` | `operational_area.coverage.admin_unit_id` (operational-target) | copy with audit | required source value; reject/exception if missing in backfill | operator/programme | `government-internal` | low | `SELECT COUNT(*) FROM territories WHERE province_code IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `territories.readiness` | `TEXT` / NOT NULL / `` | `operational_area.readiness_state` (operational-target) | copy with audit | required source value; reject/exception if missing in backfill | operator/programme | `government-internal` | low | `SELECT COUNT(*) FROM territories WHERE readiness IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `territories.type` | `TEXT` / NOT NULL / `` | `operational_area.area_type` (operational-target) | copy with audit | required source value; reject/exception if missing in backfill | operator/programme | `government-internal` | low | `SELECT COUNT(*) FROM territories WHERE type IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `territories.updated_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `operational_area.updated_at` (operational-target) | copy with audit | required source value; reject/exception if missing in backfill | operator/programme | `government-internal` | low | `SELECT COUNT(*) FROM territories WHERE updated_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `users.created_at` | `TIMESTAMPTZ` / NOT NULL / `NOW()` | `actor/session reference only` (outside-location-model) | copy with audit | required source value; reject/exception if missing in backfill | security authority | `security-internal` | low | `SELECT COUNT(*) FROM users WHERE created_at IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `users.full_name` | `TEXT` / NOT NULL / `` | `actor/session reference only` (outside-location-model) | copy with audit | required source value; reject/exception if missing in backfill | security authority | `security-internal` | low | `SELECT COUNT(*) FROM users WHERE full_name IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `users.id` | `TEXT` / NOT NULL / `` | `actor/session reference only` (outside-location-model) | copy with audit | required source value; reject/exception if missing in backfill | security authority | `security-internal` | low | `SELECT COUNT(*) FROM users WHERE id IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `users.is_active` | `BOOLEAN` / NOT NULL / `TRUE` | `actor/session reference only` (outside-location-model) | copy with audit | required source value; reject/exception if missing in backfill | security authority | `security-internal` | low | `SELECT COUNT(*) FROM users WHERE is_active IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `users.password_hash` | `TEXT` / NOT NULL / `` | `actor/session reference only` (outside-location-model) | copy with audit | required source value; reject/exception if missing in backfill | security authority | `highly-restricted` | low | `SELECT COUNT(*) FROM users WHERE password_hash IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `users.role` | `TEXT` / NOT NULL / `` | `actor/session reference only` (outside-location-model) | copy with audit | required source value; reject/exception if missing in backfill | security authority | `security-internal` | low | `SELECT COUNT(*) FROM users WHERE role IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
| `users.username` | `TEXT` / NOT NULL / `` | `actor/session reference only` (outside-location-model) | copy with audit | required source value; reject/exception if missing in backfill | security authority | `security-internal` | low | `SELECT COUNT(*) FROM users WHERE username IS NULL; -- expect 0` | until WO-002B contract phase | after parity and replacement approved |
